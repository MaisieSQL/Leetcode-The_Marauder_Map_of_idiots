# 📂 差分数组经典题单：从入门到大厂 Hard 杀招

## 🟢 1. 入门/热身 (Medium，但实为差分最标准板子)
### **LeetCode 1094. Car Pooling (拼车)**

*   **题目大意：** 
    有一辆车，初始是空的，最多可以坐 `capacity` 个人。给你一个数组 `trips`，其中 `trips[i] = [numPassengers, from, to]` 表示有这么多人从 `from` 站上车，到 `to` 站下车。判断这辆车能不能一路上把所有人顺利接送完（即在任何时刻车上的人数都不超过 `capacity`）。
*   **为什么用差分：** 
    每一趟旅程，相当于把区间 `[from, to - 1]` 里的每一站人数都加上 `numPassengers`。如果用暴力循环给区间加人，时间复杂度是 $O(N \times \text{stations})$。使用差分数组，在 `from` 站 $+num$，在 `to` 站 $-num$，最后顺着车站求一次前缀和，就能瞬间查出每一站车上的实时人数，时间复杂度降到 $O(N)$。

---

## 🟡 2. 进阶/核心 (Medium)
### **LeetCode 1109. Corporate Flight Bookings (航班预订统计)**

*   **题目大意：** 
    有 `n` 个航班，标签从 `1` 到 `n`。给你一个预订记录列表 `bookings`，其中 `bookings[i] = [first, last, seats]` 表示从航班 `first` 到航班 `last`（包含 `first` 和 `last`）的每个航班上都预订了 `seats` 个座位。请返回一个长度为 `n` 的数组，表示每个航班预订的座位总数。
*   **破局关键：** 
    这是最纯粹的**区间高频集体加值**问题。
    *   **暗器招式：**
        1. 创建一个长度为 `n + 2` 的差分数组 `diff`（防止索引越界）。
        2. 遍历 `bookings`：对于每一条记录，让 `diff[first] += seats`，让 `diff[last + 1] -= seats`。
        3. 遍历完所有预订后，从头到尾做一次前缀和累加，恢复出来的数组就是每个航班的最终座位数。

---

## 🔴 3. 终极/蜕变 (Hard)
### **LeetCode 2528. Maximize the Minimum Game Power (最大化城市的最小电量)**

*   **题目大意：** 
    一排有 `n` 个城市，每个城市有一定数量的供电厂。每个供电厂都有一个辐射半径 `r`，这意味着在城市 `i` 的供电厂可以为区间 `[max(0, i-r), min(n-1, i+r)]` 的所有城市供电。一个城市的“电量”是所有能辐射到它的供电厂的电量总和。现在政府给你额外的 `k` 个电厂可以任意安放，请问你能让所有城市中**“电量最少的那个城市”的电量最大**是多少？
*   **为什么它是 Hard：** 
    这道题是一只“披着羊皮的狼”，它缝合了两个大类：**二分查找（求最大化的最小值）** $+$ **贪心** $+$ **差分数组**。
    *   **暗器招式：**
        1. 看到“最大化最小值”，第一反应是用**二分答案**去猜这个最小电量 `target`。
        2. 为了验证猜的 `target` 能否实现，我们需要从左到右扫描每个城市。如果发现某个城市电量不够，我们必须贪心地把新电厂建在它能辐射到的**最右侧位置**，这样才能最大化造福后面的城市。
        3. 在高频模拟“安放新电厂并给一个区间增加电量”的过程中，如果你用暴力加法会超时。必须在二分判别函数（`check`）的内部，利用**差分数组**来动态维护和更新每个城市当前的实时电量。
