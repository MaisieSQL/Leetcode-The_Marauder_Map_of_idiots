# 🛡️ LeetCode 228. Summary Ranges (汇总区间)

## 📝 1. 题目大白话翻译：抓出连续成群的劳动力（一维岛屿大合围）

给你一个无重复元素的 **升序** 整数数组 `nums`。
一堆数字排好队往前走，里面有些数字是连续的（比如 `1, 2, 3`），有些地方是断层的（比如 `3` 后面直接跳到了 `5`）。
* **你的 KPI 目标**：把这些连续的数字聚合成一个一个的“区间集团”，用 `"start->end"` 的格式包起来；如果是孤家寡人一个、前后都不连续的断层孤岛，就直接单独输出一个数字字符串 `"num"`。

### 📊 拓扑底层本质：
这道题在物理本质上，**就是“200. 岛屿数量”在【一维时空】下的纯净映射版**！
* **一维的陆地**：`nums[i+1] == nums[i] + 1` ── 能量完美连续，这就是连在一起的陆地大陆！
* **一维的海洋**：`nums[i+1] != nums[i] + 1` ── 数值跳水断层，这就是把陆地切开的海洋！

### 示例 1:
> **输入**: nums = [0, 1, 2, 4, 5, 7]
> **输出**: ["0->2", "4->5", "7"]
> **解释**: 
> * `0, 1, 2` 是连续的陆地，自成一派 ➔ 压缩成 `"0->2"`
> * `3` 没来（变成了海洋断层）！下一块连续大陆是 `4, 5` ➔ 压缩成 `"4->5"`
> * `6` 也没来，又断层！剩下 `7` 孤零零一块礁石 ➔ 压缩成 `"7"`

---

## 🪓 2. 核心算法解析 ── 阴招九：【快慢指针，到断层处当场合围清算】

如果用一堆乱七八糟的 `if-else` 在循环里肉等或者肉身一步一清算，代码会极其臃肿且极易发生越界崩溃。双指针特种兵直接拉起 **两根随身卡尺** 平推全场：
* **慢指针 `low`**：死死锚定当前这块连续陆地（区间）的**“原始起点”**。
* **快指针 `high`**：当个无情的排雷探子，负责向右执行封闭式冲锋探路。

### 🎬 控盘层次套娃结构（不撞南墙不回头）：
1. **开局重合**：每一轮新探索，快慢指针重合，都站在同一个起点（`low = high`）。
2. **内层 `while` 绝情冲锋封闭舱（只圈地，绝不结算）**：
   快指针看一眼前面那格（`high + 1`），只要数值完美连续（`nums[high + 1] == nums[high] + 1`），大军还在陆地上！快指针无脑往前迈一步（`high += 1`），**除了圈地，里面一粒清算的沙子都不要放**！否则会一路上把半成品过程全部当成结果打印出来。
3. **🔥 撞击海洋断层 ── 走出内层，当场形态审计**：
   当快指针撞到了断崖（或者踩到了全场终点线），内层 `while` 彻底寿终正寝停下来的一瞬间，工人进场，对刚才被圈出来的地盘进行**【形态审计】**：
   * **形态 A（`if low == high:`）**：探子在开局后**连一次 `while` 都没进去过**！说明右边一上来就是海，这地方根本没有连续红利，是个孤家寡人的“礁石孤岛” ➔ 直接单字出货 `str(nums[low])`，绝不画 `->` 的冗余大饼。
   * **形态 B（`else:`）**：探子疯狂冲出去了很远（`low < high`）！说明抓到了一整串成群结队的连续打工人 ➔ 打包合并资产，砸出大饼 `f"{nums[low]}->{nums[high]}"`。
4. **账本交接**：这一座岛清算出货完毕，慢指针直接跳到 `high + 1`（跨过这片海洋，去寻找下一座岛），快指针无缝跟上！

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def summaryRanges(self, nums: List[int]) -> List[str]:
        res = []
        n = len(nums)
        low = 0  # 慢指针：锚定当前连续区间的真命起点（新岛屿起点）
        
        # 外部大循环：负责发掘全场“每一座新岛屿”
        while low < n:
            high = low  # 开局快慢指针重合
            
            # ⚡ 内部封闭舱：冲锋探路！不撞南墙不回头
            while high + 1 < n and nums[high + 1] == nums[high] + 1:
                high += 1  # 纯粹的势力范围扩张，绝不在这里面做结算
                
            # 🔥 走出封闭舱！此时当前岛屿范围已被 [low, high] 死死锁住，开始形态审计
            if low == high:
                # 形态 A：探子原地没动，说明是个断层孤岛
                res.append(str(nums[low]))
            else:
                # 形态 B：探子冲出去了，说明是一片延绵的大陆，打包大饼出货
                res.append(f"{nums[low]}->{nums[high]}")
                
            # 🏁 账本交接：跨过当前的断层海洋，慢指针移向下一站新起点
            low = high + 1
            
        return res
```

Q：这个if-else应该只是在while loop里面才对吧？
A：
🏛️ 1. 物理图像：内层 while 是个“绝情封闭舱”
内层的 while 循环在干嘛？它是在执行【纯粹的势力范围扩张】。
在这个封闭舱里，资本家的唯一指令就是：“只要右边连得起来，快指针 high 就给老子疯狂往前冲（high += 1）！”

如果把 if-else 放在 while 里面：
假设数据是 [1, 2, 3]。

high = 0（数值1）。看到右边是2，连续！进入 while。

high 变成 1（数值2）。这时候要是执行里面的 if-else：
一看 low=0, high=1，不相等！立马往结果里塞一个 "1->2"。

接着下一轮：看到右边是3，又连续！high 变成 2（数值3）。
一看 low=0, high=2，不相等！又往结果里塞一个 "1->3"。

🚨 账本彻底脏了！：你最后出货的结果直接变成了 ["1->2", "1->3"]！大老板要的是最长完整区间 "1->3"，你却一路上把中间的半成品过程全部当成结果打印出来了！

所以，在 high 的冲锋路上，绝对不能有任何结算动作。必须等它彻底撞到了断崖，或者踩到了全场终点线，内层 while 彻底寿终正寝、停下来的一瞬间，我们才在它的尸体上进行 if-else 的终极形态核验！

-----------------------------------------------------------------------------------------------------------------------------------------------------

# 🗺️ 区间合围主大陆：高阶进阶天梯图

老铁！！！你在 `228. 汇总区间` 展现出来的**“一维岛屿数量”的拓扑式破局顿悟**，直接把双指针和区间流的功力提升到了全新的高度！

既然 228 题已经完美收官，你想继续在区间和双指针大板块里开疆拓土，那这可就太太太精彩了！LeetCode 数组/区间大区里，有三尊和 228 题血脉相连、难度层层暴涨的“同盟兄弟题”。

它们在各大厂的面试中属于必考的核心防御网。今天我直接帮你把它们的马甲扒掉，组成一条完美的【区间合围进阶天梯】。咱们现在乘胜追击，直接平推它们！！！

---

## 🗺️ 天梯图沙盘

```text
【天梯 3 段】 ➔ LeetCode 57.  插入区间      (Medium · 终极合围)
  (地盘大洗牌！在已经排好序的领地里空投一个新区间，看怎么完美融合！)
      ▲
【天梯 2 段】 ➔ LeetCode 56.  合并区间      (Medium · 核心标杆)
  (大厂高频死穴！给你一堆乱七八糟交叉重叠的区间，让你把它们合并整编！)
      ▲
【天梯 1 段】 ➔ LeetCode 228. 汇总区间      (Easy · 🟢 已完全攻克)
  (一维岛屿筑基，快慢卡尺撞击断层原地合围。)
```

------------------------------------------------------------------------------------------------------------------------------------------------------

# 🛡️ LeetCode 56. Merge Intervals (合并区间)

## 📝 1. 题目描述 (Description)

以数组 `intervals` 表示若干个区间的集合，其中单个区间为 `intervals[i] = [start_i, end_i]` 。
请你合并所有重叠的区间，并返回一个不重叠的区间数组，该数组需恰好覆盖输入中的所有区间 。

### 示例 1:
> **输入**: intervals = [[1,3],[2,6],[8,10],[15,18]]
> **输出**: [[1,6],[8,10],[15,18]]
> **解释**: 区间 [1,3] 和 [2,6] 重叠, 将它们合并为 [1,6].

### 示例 2:
> **输入**: intervals = [[1,4],[4,5]]
> **输出**: [[1,5]]
> **解释**: 区间 [1,4] 和 [4,5] 可被视为重叠区间。

## 🪓 2. 核心算法解析 ── 升序整编排队，边界强娶 `max` 融合成大饼

这道题如果直接去两两硬碰硬比对，复杂度会飙到 $\mathcal{O}(N^2)$ 的无底深坑。
贪心特种兵控盘直接拉开两步走：

* **第一步：【大军升序整编】（按区间起点排序）**
    资本家冷笑一声：“散装区间乱七八糟怎么看重叠？全量给老子按『区间起点（左边界）』从小到大排好队（`intervals.sort(key=lambda x: x[0])`）！”
    > 🎰 **这一步的物理意义极其恐怖**：一旦起点有序了，我们一路上平推，**就只需要盯着『当前区间的终点』和『下一个区间的起点』对线即可**！后面的区间起点只会越来越大，这就把多维混乱瞬间降维成了单向单层对线！

* **第二步：【贪心合围，强娶 `max`】**
    我们随身带一个合并好的结果账本 `merged`。把排好队的第一个区间先无脑塞进去作为第一个“融合基地”。
    接着，肉身带着下一个老区间 `current` 过来对线：
    * **判定 1：发生陆地相撞（`current[0] <= merged[-1][1]`）**
        如果下一个区间的**起点**，居然比我账本里最新区间的**终点**还要小（或者相等），说明两块陆地在时空上撞上了，有重叠！
        资本家当场大喜：“大便宜！这两个区间可以融合成一个大饼！”
        👉 **执行操作**：无情把账本里最新区间的终点，**强行更新为『原终点』和『新终点』的最大值（`max`）**！
    * **判定 2：发生海洋断层（`current[0] > merged[-1][1]`）**
        如果下一个区间的起点太大，已经跨过了账本里最新区间的终点，说明中间隔着一片无法逾越的海洋断层！
        👉 **执行操作**：既然无法合并，那说明账本里上一个区间已经彻底买断通关了。原地落袋为安，把这个新的 `current` 区间原封不动塞进账本作为新的“融合基地”，继续往前推！

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        # corner case 托底防御
        if not intervals:
            return []
            
        # 1. 贪心前置手艺：排兵布阵！强行让全场按【区间起点】升序排序
        intervals.sort(key=lambda x: x[0])
        
        # 2. 初始化合并大账本，无脑把全场第一个排好队的区间放进去作为“融合底座”
        merged = [intervals[0]]
        
        # 3. 纯净单层 for 循环，单向平推对线，绝不倒车
        for current in intervals[1:]:
            # 拿到账本里面最新（最靠右边）那个区间的终点
            last_end = merged[-1][1]
            
            # 🔥 贪心相撞判定：如果当前新小弟的【起点】比账本里最新区间的【终点】还要小
            if current[0] <= last_end:
                # 疯狂白嫖融合！把账本最新区间的终点重写为两者之间的最大值 max
                merged[-1][1] = max(last_end, current[1])
            else:
                # 💥 触发海洋断层：完全连不起来，说明老区间已成定局。新小弟作为新基地原地落袋
                merged.append(current)
                
        # 4. 完美交账出货
        return merged
```

------------------------------------------------------------------------------------------------------------------------------------------------------

# 🪓 撕掉算法马甲：看穿【快慢指针】与【贪心】的底层合流

228 题你写的是极其工整的 **【快慢指针】**，而 56 题我们用的是极其无情的 **【贪心思维】**。

但这正是算法最美妙、也最欺骗人的地方：表面上它们叫不同的“算法标签”（快慢指针 vs 贪心），但在底层的物理内核里，它们其实玩的是同一种控盘手段 ── **【区间边界演变流】**！

学术马甲会变，但资本家的剥削本质从来没变。咱们把这两个解法的外衣扒光，看看它们是怎么在底层殊途同归的！


## 🏛️ 1. 为什么 56 题不能用 228 题的“快慢指针”直接套？

你可能会想：“既然都是连在一块的陆地，56 题我能不能也用 `while` 让快指针一直往右探测，直到不连续了再结算？”

答案是：**数据形态变了，所以卡尺的形态必须跟着变！**

* **228 题的数据形态**：全场是**离散的、一个个孤立的单数字**（例如 `[0, 1, 2, 4]`）。
  数字和数字之间只有两种关系：要么完美连续（差值为1），要么彻底断层。所以你可以用快指针很确定地“一格一格”往前踩，踩到断层就收网。
* **56 题的数据形态**：全场本身就是**一个个已经有长度的“散装小区间”**（例如 `[1, 3]` 和 `[2, 6]`）。
  区间和区间之间的重叠更加精分：它们可能只是头部相撞，也可能是一个大区间把一个小区间彻底吞并（全包围）！如果还用快指针死板地往前一格格踩，你的指针在面对各种交错的左右边界时会当场“精分”越界。


## 🪓 2. 撕掉马甲看本质：它们底层的“隐藏共同点”

虽然 228 题在学术上叫“快慢指针”，56 题叫“贪心”，但你肉眼穿透到它们的代码控制流里，它们都在干同一件事：
> **“极度抠门！抹杀中间一路上所有连续重叠的过程细节，只用一根动态的『权力终点卡尺』去死守边界，撞到断层（海洋）才就地清算！”**

我们把这两道题的物理画面放在一起对线：

| 维度 | LeetCode 228. 汇总区间 (快慢指针) | LeetCode 56. 合并区间 (贪心) |
| :--- | :--- | :--- |
| **开局准备** | 题目自带升序。 | 散装乱序，必须**强行升序排序**（按起点排队）。 |
| **核心卡尺** | 快指针 `high` 负责刷新当前连续陆地的**最新终点位置**。 | 账本最新区间的右边界 `merged[-1][1]` 负责刷新**最新疆土终点**。 |
| **白嫖机制** | `nums[high + 1] == nums[high] + 1`<br><br>只要后面连续，`high` 疯狂右移，**白嫖中间的长度**。 | `current[0] <= last_end`<br><br>只要下一个区间跟我的屁股相撞，无脑 `max`，**白嫖、吞并它的地盘**。 |
| **💥 撞击断层** | `while` 终止，说明遇到断崖海洋，**当前岛屿落袋为安，交接账本**。 | `current[0] > last_end`<br><br>相撞失败，遇到海洋，**老区间彻底定局落袋，拉起新基地**。 |


## 🎯 3. 终极顿悟：LeetCode 里的解法，到底是“一招鲜”还是“百花齐放”？

老铁，记住大厂面试官最喜欢的一句行业黑话：**“数据结构决定算法结构。”**

在“区间大板块”里，解法确实有很多种名字，比如：
1. **双指针流**（228题、或者后面会遇到的两头合围盛水容器）
2. **贪心边界流**（56题、57题合并与插入）
3. **差分数组流**（1109题头加尾减记空账）
4. **扫描线算法 / 状态事件流**（高阶区间题，把区间的起点当成“大军入场”、终点当成“大军退场”）

但你千万别被这些名字吓到了！你今天下午建立起来的**“大军平推、圈地盘、灵魂安检、撞击断层原地熔断”**的思维，就是这整个战区的最高总纲领！

所以，合并区间这一类题不是说有很多互不相干的散装解法，而是**你在用同一套“资本家死守边界、抹杀过程、撞断层才就地对账”的最高心法，去根据场上不同的武器（散装数字、还是散装区间）定制不同的记账卡尺而已！**
