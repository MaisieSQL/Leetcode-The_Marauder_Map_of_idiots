# 07-30-2026

| 序号 | 题目名称 (Title) | LeetCode 题号 | 难度 | 归属流派 / 核心类型 | 物理直觉合流与核心原理 | 传送门链接 (LeetCode Link) |
| :---: | :--- | :---: | :---: | :--- | :--- | :--- |
| 1 | **Longest Common Prefix** | LeetCode 14 | 🟢 简单 | 字符串 / 矩阵与模拟流 | 字符串数组本质上是字符构成的二维矩阵。采用标准纵向扫描，逐列检查每一列字符是否完全一致。 | [LeetCode 14 Link](https://leetcode.com/problems/longest-common-prefix/) |
| 2 | **Top K Frequent Elements** | LeetCode 347 | 🟡 中等 | 哈希表 / 堆 (Heap) 流 | 利用哈希表对数组元素进行词频统计，再借助小顶堆（Heap）对频率进行体积裁剪，实现高效 Top K。 | [LeetCode 347 Link](https://leetcode.com/problems/top-k-frequent-elements/) |
| 3 | **Encode and Decode Strings** | LintCode 659 / NeetCode | 🟡 中等 | 字符串 / 编解码与协议设计流 | 把字符串列表压扁成单一字符数组，利用“长度前缀 + `#`”在连续内存中精准切片，考验底层下标掌控力。 | [LintCode 659 Link](https://www.lintcode.com/problem/659/) |
| 4 | **Sliding Window Maximum** | LeetCode 239 | 🔴 困难 | 单调队列 / 滑动窗口流 | 完美的单调队列（Monotonic Queue）母题。通过双端队列在数组中滑动，实时维护窗口内的最大值。 | [LeetCode 239 Link](https://leetcode.com/problems/sliding-window-maximum/) |

---

# LeetCode 14. 最长公共前缀 (Longest Common Prefix) 专题通关卡片

## 🎯 一、 核心考点与物理直觉

* **题目大意**：
  编写一个函数来查找字符串数组中的最长公共前缀。如果不存在公共前缀，返回空字符串 `""`。

* **物理直觉与“纵向扫描法”**：
  * 想象一下，我们把字符串数组排成一列。如果要求所有字符串的**最长公共前缀**，最直观的做法是**纵向列扫描**：
    1. 我们以数组中的**第一个字符串 (`strs[0]`)** 为标杆，依次遍历它的每一个字符（从第 0 个字符开始）。
    2. 拿这个字符去和其他所有字符串（`strs[1]`, `strs[2]`...）在相同位置上的字符进行比对。
    3. 如果所有字符串在当前位置的字符都一样，说明这个字符是安全的公共前缀的一部分，继续比对下一列。
    4. **一旦遇到以下两种情况之一，比对立刻终止**：
       * 某个字符串的长度已经走到了尽头（超出了边界）。
       * 某个字符串在当前位置的字符和标杆字符对不上。
    此时，标杆字符串从开头到当前位置之前的切片，就是全场唯一的最长公共前缀！

## 💻 二、 Python3 高效通关代码 (Solution)

```python
from typing import List

class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        if not strs:
            return ""
        
        # 以第一个字符串为标杆，纵向遍历它的每一个字符
        for i in range(len(strs[0])):
            char = strs[0][i]
            
            # 拿这个字符去和其余所有字符串的第 i 个字符比对
            for j in range(1, len(strs)):
                # 边界条件：如果当前索引 i 已经超出了某个字符串的长度
                # 或者字符不匹配，说明公共前缀到此为止，直接截断返回
                if i >= len(strs[j]) or strs[j][i] != char:
                    return strs[0][:i]
                    
        # 如果第一个字符串本身就是最短的且完全匹配，直接返回它自己
        return strs[0]
```

三、 复杂度核验 (Complexity Analysis)

- 时间复杂度：O(S)，其中 S 是所有字符串中字符数量的总和。在最坏情况下（比如所有字符串完全相同，或者前缀很长），我们需要把每个字符串的所有字符都扫描一遍。
- 空间复杂度：O(1)，因为我们在原地进行纵向扫描和指针比对，只需要常数级别的额外变量开销，没有开辟任何额外的大型辅助数据结构。

## Question: 第二个致命错误：“短路求值（Short-circuit Evaluation）”引发的定时炸弹！

你的代码：if strs[i][i] != char or i >= len(strs[j]):

问题所在：

在 Python（以及绝大多数编程语言）中，or 运算符是从左向右执行的。如果左边为真，右边就不看了。

想象一下这个灾难场景：假设外层循环的 i 已经比短字符串 strs[j] 的长度还要大了（比如 i = 3，而 strs[j] 长度是 2）。

如果你把 strs[i][i] != char 写在左边：

Python 会先去执行 strs[j][3]（试图访问第 3 个下标）。

⚠️ 瞬间崩溃！ 因为 strs[j] 根本没有下标 3，Python 直接在执行左边时就抛出 IndexError: string index out of range 崩掉了，根本连走到右边 i >= len(strs[j]) 保护罩的机会都没有！

破解法则：一定要把“防越界保护罩”写在左边！ 先检查长度够不够，如果不够，直接短路熔断，绝不给它去访问 strs[j][i] 越界报错的机会。


# LeetCode 14. 最长公共前缀 —— Dry Run（手动模拟运行）实录

🧪 1. 设定测试用例 (Test Case)
假设输入的字符串数组是：
```python
strs = ["flower", "flow", "flight"]
```

2. Step-by-Step Dry Run（手把手演练）

- 【第一轮：外层循环 i = 0】
  - 锁定标杆字符：`char = strs[0][0]` $\rightarrow$ 即 `'f'`
  - 内层循环：纵向比对其他字符串的第 0 个字符 (`j` 从 1 开始)：
    - 当 `j = 1` 时：检查 `strs[1]` 的第 0 个字符。`strs[1] = "flow"`，其 `strs[1][0]` 是 `'f'`。
      - 比较：`'f' == 'f'`（匹配成功，继续）。
    - 当 `j = 2` 时：检查 `strs[2]` 的第 0 个字符。`strs[2] = "flight"`，其 `strs[2][0]` 是 `'f'`。
      - 比较：`'f' == 'f'`（匹配成功）。
  - 第一列结果：所有字符串的第 0 个字符都是 `'f'`。安全通过，索引 `i` 递增，进入下一列！

- 【第二轮：外层循环 i = 1】
  - 锁定标杆字符：`char = strs[0][1]` $\rightarrow$ 即 `'l'`
  - 内层循环：纵向比对第 1 个字符：
    - 当 `j = 1` 时：`strs[1][1]` 是 `'l'`。比较 `'l' == 'l'`（成功）。
    - 当 `j = 2` 时：`strs[2][1]` 是 `'l'`。比较 `'l' == 'l'`（成功）。
  - 第二列结果：全是 `'l'`。安全通过，索引 `i` 递增，进入下一列！

- 【第三轮：外层循环 i = 2】
  - 锁定标杆字符：`char = strs[0][2]` $\rightarrow$ 即 `'o'`
  - 内层循环：纵向比对第 2 个字符：
    - 当 `j = 1` 时：`strs[1][2]` 是 `'o'`。比较 `'o' == 'o'`（成功）。
    - 当 `j = 2` 时：`strs[2][2]` 是 `'i'`。
      - ⚠️ **高能熔断触发！** 比较：`strs[2][2] ('i') != char ('o')`，字符不匹配！
  - 代码执行动作：
    - 触发终止条件：`return strs[0][:i]`。
    - 此时 `i = 2`，切片操作 `strs[0][:2]` 取出的是标杆从下标 0 到 1 的字符：`"fl"`。
  - 程序直接终止并返回 `"fl"`。后面的 `'ight'`、`'ower'` 甚至连看都不用看，直接跳过！

# 🧩 算法辨析：公共前缀 (Prefix) vs 公共子串 (Substring)

## 🧪 1. 经典测试用例剖析
假设输入的字符串数组是：
```python
strs = ["aflower", "abflow", "flo"]
```
老铁敏锐地发现：这三个字符串的内部，每一个里面都连续包含了字母 `'f'`, `'l'`, `'o'`（也就是它们都含有 `"flo"` 这个子串）。

🔍 **2. 深度拆解：为什么它们的最长公共前缀是 `""` (空字符串)？**

这里有一个极其关键的概念误区，咱们必须瞬间拉直：

- **题目要求的是什么？**
  题目求的是“最长公共前缀 (Longest Common Prefix)”——必须是从下标 0（最开头）开始连续匹配的字符！

- **来看看我们的测试用例在绝对位置上的对齐情况：**
  - 第一个字符串 `strs[0] = "aflower"` $\rightarrow$ 开头第一个字母是 `'a'`
  - 第二个字符串 `strs[1] = "abflow"` $\rightarrow$ 开头第一个字母是 `'a'`
  - 第三个字符串 `strs[2] = "flo"` $\rightarrow$ 开头第一个字母却是 `'f'`

虽然它们内部都长了 `"flo"` 这三个字母，但：
- 第三个字符串 `"flo"` 的开头是 `'f'`，它没有以 `'a'` 开头。
- 因此，从下标 0（开头）这个绝对位置看过去：
  - 第一个字符串从 `'a'` 开始。
  - 第二个字符串从 `'a'` 开始。
  - 第三个字符串从 `'f'` 开始。
- **它们三个在“开头位置”根本没有交集！** 所以它们的最长公共前缀依然是 `""`（空字符串）。

💡 **3. 为什么“子串 (Substring)”和“前缀 (Prefix)”天差地别？**

- **公共子串（Common Substring）**：可以出现在字符串的任何地方（开头、中间、结尾都行）。比如 `"flo"` 确实是这三个字符串的公共子串。
- **公共前缀（Common Prefix）**：必须顶格从第一个字符（index = 0）开始算起，中间断掉一点都不行！

---

# LeetCode 347. 前 K 个高频元素 (Top K Frequent Elements) 专题通关卡片

## 🎯 一、 核心考点与物理直觉

* **题目大意**：
  给你一个整数数组 `nums` 和一个整数 `k`，请你返回其中出现频率前 `k` 高的元素。你可以按任意顺序返回答案。

* **物理直觉与核心流派**：
  这道题是 **“哈希统计 + 极值裁剪（堆/桶排序）”** 的殿堂级母题：
  1. **第一步（统计频次）**：必须先用哈希表（HashMap / Counter）把每个数字及其出现的次数完整统计出来。
  2. **第二步（Top K 裁剪）**：
     * **解法 A（小顶堆 Min-Heap）**：维持一个大小为 `k` 的小顶堆。遍历所有频次，如果堆的大小超过 `k`，立刻把堆顶（频率最小的元素）弹出去！这样堆里面永远留着最大的 `k` 个元素。
     * **解法 B（桶排序 Bucket Sort）**：利用“没有任何元素的出现次数会超过数组总长度 `N`”的物理规律，开辟一个大小为 `N+1` 的桶数组。桶的下标代表**“出现频次”**，桶里面挂着**“在这个频次下出现的所有数字”**。最后从后往前倒序收集前 `k` 个高频元素。

---

## 💻 二、 Python3 高效通关代码 (两种工业级解法)

### 方法一：小顶堆法 (Min-Heap)
```python
from typing import List
import heapq
from collections import Counter

class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        # 1. 统计每个数字的频率
        count = Counter(nums)
        
        # 2. 使用小顶堆维护前 k 个高频元素
        # heapq 默认是小顶堆，里面存入元组 (frequency, num)
        min_heap = []
        for num, freq in count.items():
            heapq.heappush(min_heap, (freq, num))
            # 如果堆的大小超过了 k，弹出频率最小的堆顶元素
            if len(min_heap) > k:
                heapq.heappop(min_heap)
                
        # 3. 提取堆中剩下的数字
        return [num for freq, num in min_heap]
```

### 方法二：桶排序法 (Bucket Sort) ── 绝对的线性时间复杂度神器

```python
from typing import List
from collections import defaultdict

class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        # 1. 统计频次
        count = {}
        for num in nums:
            count[num] = count.get(num, 0) + 1
            
        # 2. 初始化桶：下标代表频次，最大频次不会超过 len(nums)
        bucket = [[] for _ in range(len(nums) + 1)]
        for num, freq in count.items():
            bucket[freq].append(num)
            
        # 3. 从后往前（从高频到低频）倒序遍历桶，收集前 k 个元素
        res = []
        for i in range(len(bucket) - 1, -1, -1):
            for num in bucket[i]:
                res.append(num)
                if len(res) == k:
                    return res
        return res
```

### 三、 复杂度核验 (Complexity Analysis)

| 解法名称 | 时间复杂度 | 空间复杂度 | 说明 |
| :--- | :---: | :---: | :--- |
| **小顶堆法 (Min-Heap)** | $\mathcal{O}(N \log K)$ | $\mathcal{O}(N)$ | $N$ 为不重复元素的总数。每次堆操作耗时 $\log K$，当 $K$ 较小时极度高效。 |
| **桶排序法 (Bucket Sort)** | $\mathcal{O}(N)$ | $\mathcal{O}(N)$ | 彻底避开了堆排序的 $\log$ 开销，利用频次作为索引实现真正的线性时间复杂度。 |

---

刚才咱们聊到 LeetCode 271. Encode and Decode Strings（字符串的编码与解码） 时，这道题之所以让人第一眼看过去觉得“完全看不懂”，根本原因不是你笨，而是它的题目描述自带一种极其抽象的“谜语人”气质：

> *“给你一个字符串列表，把它设计成一个字符串传出去；然后再把它收回来解码成原来的列表。要求不能丢失任何信息。”*

听完这句话，很多人脑子里都会冒出一连串问号：
- 啥叫编码？啥叫解码？
- 字符串本来就是字符串，为啥还要把一堆字符串拼成一个大字符串？
- 为什么要搞什么长度前缀？这跟写代码有什么关系？

---

# LeetCode 271. 字符串的编码与解码 (Encode and Decode Strings) 专题通关卡片

## 🎯 一、 核心考点与物理直觉

* **题目大意**：
  设计一个算法，把一个字符串列表（List of Strings）**编码**成一个单独的字符串（String）；同时，能够把这个单独的字符串**解码**回原来的字符串列表。要求编码和解码必须是完全可逆的，且能处理任何可能出现的特殊字符。

* **物理直觉与“长度前缀 + 显式分隔符”协议**：
  * **为什么普通的拼接会翻车？** 如果用简单的分隔符（比如逗号 `,`）把字符串拼起来：`["hel,lo", "world"]` $\rightarrow$ `"hel,lo,world"`。解码时遇到逗号会直接切错，导致数据污染！
  * **工业级解决方案 —— 长度前缀协议（Length-Prefix Protocol）**：
    为每个字符串设计一个明确的护照：`[字符串长度] + [#] + [字符串本体]`。
    * 例如：长度为 5 的 `"apple"`，我们编码为 **`"5#apple"`**。
    * 这样一来，无论字符串内部包含多少个 `#` 或奇怪的字符，解码器只认开头的数字和 `#` 后面的长度，绝对不会发生错位！

## 💻 二、 Python3 高效通关代码 (Solution)

```python
from typing import List

class Codec:
    def encode(self, strs: List[str]) -> str:
        """将字符串列表编码为一个单一的字符串。
        协议格式：每个单词转为 "长度#单词本体"，然后直接拼接。
        例如：["neet", "code"] -> "4#neet4#code"
        """
        res = []
        for s in strs:
            res.append(f"{len(s)}#{s}")
        return "".join(res)

    def decode(self, s: str) -> List[str]:
        """将编码后的字符串解码回原来的字符串列表。
        """
        res = []
        i = 0
        n = len(s)
        
        while i < n:
            # 1. 寻找分隔符 '#' 的位置
            j = i
            while s[j] != '#':
                j += 1
                
            # 2. 解析出当前单词的长度（从 i 到 j 之间的数字）
            length = int(s[i:j])
            
            # 3. 根据长度精准切片，拿到单词本体
            word = s[j + 1 : j + 1 + length]
            res.append(word)
            
            # 4. 指针 i 跃迁到下一个单词的起点
            i = j + 1 + length
            
        return res
```

### 💡 咱们用最接地气的“快递打包”生活直觉，瞬间把它秒杀！

别去管什么大厂术语和协议，咱们想象一个生活场景：

#### 1. 痛点背景（为什么需要这道题？）
- 假设你网购了 3 件商品：`["apple", "banana", "cat"]`。
- 现在你要把它们装进一个**单一的快递纸箱（也就是一根长字符串）**里寄给朋友。
- 最简单的办法是拿胶水直接粘在一起：`"applebananacat"`。
- **问题来了**：等朋友拆快递时，他看着这一坨连在一起的字母傻眼了——请问这到底是 `["app", "lebanan", "a", "cat"]` 还是 `["apple", "banana", "cat"]`？**边界完全糊掉了！**

#### 2. 暴力想法与翻车点
- 聪明一点的同学会说：“我们在中间加个逗号 `,` 不就行了嘛！变成 `"apple,banana,cat"`。”
- **再次翻车**：如果你的商品名字本身就带逗号呢？比如 `["ap,ple", "banana"]`。朋友在收快递切分时，看到逗号就会直接切错，导致数据彻底污染！

#### 3. 终极解法：给每个商品贴上“长度护照”
- 所以，工业级的做法是什么？我们在每个单词前面，强行加上它的**“字符长度”**和一个**特殊的分隔符 `#`**：
  - `"apple"` 的长度是 5 $\rightarrow$ 变成 **`"5#apple"`**
  - `"banana"` 的长度是 6 $\rightarrow$ 变成 **`"6#banana"`**
- 把它们全部连起来，变成：**`"5#apple6#banana"`**。
- **为什么这回绝对不会错？** - 当解码员拿到 `"5#apple6#banana"` 时，他看到第一个数字是 **5**，后面紧跟 `#`，他就知道：“哦！从 `#` 往后数 5 个字符，绝对就是第一个单词的全部家当！”
  - 于是他精准切出 `"apple"`，然后指针刚好落在了第二个数字 **6** 的头上，继续往后切 6 个字符拿到 `"banana"`。
  - **不管你的单词里面包含多少个逗号、空格、甚至 `#` 号，因为我们是先读数字、靠长度物理截断的，所以绝对不会发生任何误判！**

这就是整道题的全部底层真相。它根本不是什么高深莫测的黑科技，而是一个在计算机网络、文件存储、甚至发微信消息时天天都在用的**“数据序列化（Serialization）与反序列化（Deserialization）”**基础范式。

老铁，这么一用大白话拆解，是不是觉得这道“谜语题”瞬间变得像生活常识一样好理解了？如果觉得哪个细节还是有点卡壳，随时叫我，咱们继续把它盘透！🚀

### 🧪 三、 Step-by-Step Dry Run（手把手演练）

假设输入：
```python
strs = ["neet", "code"]
```

- **编码阶段（Encode）**：
  - 处理 `"neet"`：长度为 4 $\rightarrow$ 变成 `"4#neet"`
  - 处理 `"code"`：长度为 4 $\rightarrow$ 变成 `"4#code"`
  - 最终返回的编码字符串：`s = "4#neet4#code"`

- **解码阶段（Decode）**：
  - 初始指针 `i = 0`：
    - 寻找 `#`：发现 `s[1] == '#'`（此时 `j = 1`）。
    - 提取长度：`s[0:1]` $\rightarrow$ `"4"` $\rightarrow$ `length = 4`。
    - 精准切片：从 `j + 1`（下标 2）开始，向右取 4 个字符 $\rightarrow$ `s[2:6]` 是 `"neet"`。
    - 指针跃迁：`i = j + 1 + length` $\rightarrow$ `1 + 1 + 4 = 6`（指向第二个 `"4#code"` 的开头）。
  - 第二轮循环（`i = 6`）：
    - 类似地解析出 `length = 4`，切片拿到 `"code"`。
  - 指针 `i` 走到头，循环结束，完美还原！

### 📊 四、 复杂度核验 (Complexity Analysis)

| 解法名称 | 时间复杂度 | 空间复杂度 | 说明 |
| :--- | :---: | :---: | :--- |
| **长度前缀协议法** | $\mathcal{O}(N)$ | $\mathcal{O}(N)$ | $N$ 为所有字符串中字符的总长度。无论是编码还是解码，都只需要对整个字符串进行一次线性扫描和切片。 |
