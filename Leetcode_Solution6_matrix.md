# Matrix

咱们这就把 LeetCode 的 【二维矩阵与坐标变换大区（Matrix）】 按照 “从易到难、步步为营” 的工业级阶梯完全拆开！矩阵这类题最妙的地方在于：它没有复杂的动态数据结构（没有链表指针，没有树分叉），它的物理外壳永远是固定的 matrix[row][col]。 它的难点演进，纯粹是 【坐标控制精密度的升级】。

```text
【天梯 1 段】 ➔ LeetCode 48.  旋转图像        (主对角线转置 + 水平掉头的几何魔术 🪓)

【天梯 2 段】 ➔ LeetCode 54.  螺旋矩阵        (四面大铁墙边界动态向内合围 ⚔️)

【天梯 3 段】 ➔ LeetCode 73.  矩阵置零        (不花一粒沙子的“借尸还魂记账流” 🔥)

【天梯 4 段】 ➔ LeetCode 240. 搜索二维矩阵 II  (寻找马鞍点的阶梯降维斩杀 🏆)
```
---

# 🛡️ LeetCode 48. Rotate Image (旋转图像)

## 📝 1. 题目描述 (Description)

给定一个 `n × n` 的二维矩阵 `matrix` 表示这一幅图像。请你将图像顺时针旋转 90 度。

- **⚠️ 核心约束**：你必须在 **【原地 (In-place)】** 修改输入矩阵。这意味着你不能开辟一个新的临时二维数组去转存结果，空间复杂度必须死死卡在 $\mathcal{O}(1)$！

### 示例 1:


> **输入**: matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
> **输出**: [[7, 4, 1], [8, 5, 2], [9, 6, 3]]
> **解释**: 
> 原矩阵为：
> [1, 2, 3]
> [4, 5, 6]
> [7, 8, 9]
> 顺时针旋转 90 度后变成：
> [7, 4, 1]
> [8, 5, 2]
> [9, 6, 3]

---

## 🪓 2. 核心算法解析 ── 矩阵转置 + 逐行水平翻转的几何魔法

如果直接在矩阵里搞“四个角落手拉手原地环形覆盖”，代码里需要维护一堆极其复杂的错位下标，极易发生坐标手抖 Bug。

线性代数揭示了一个优美的空间变换规律：**任何方阵顺时针旋转 90 度，在几何上完美等价于：先做【矩阵转置】，再做【逐行水平对轴翻转】！**

### 🏃‍♂️ 物理行军两步走：

1. **第一步：沿主对角线转置（Matrix Transpose）**
   主对角线就是从左上角到右下角的那条线（$1 \rightarrow 5 \rightarrow 9$）。我们沿这条线将矩阵对折，把行和列的索引互换，即让 `matrix[i][j]` 和 `matrix[j][i]` 原地对调。
   - **🚨 核心微操（防无效返工）**：外层 `i` 扫全行，内层 `j` **必须从 `i` 开始（`range(i, n)`）**，也就是只扫矩阵的右上三角！如果 `j` 每次都从 0 开始扫，翻过去的元素在下半场又被翻回来了，原地做无用功。
2. **第二步：逐行水平反转（Reverse Rows）**
   把转置后的矩阵，每一行直接水平调头（第一个和最后一个换，第二个和倒数第二个换）。在 Python 里，直接调用原生 `matrix[i].reverse()` 原地降维打击。

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def rotate(self, matrix: List[List[int]]) -> None:
        """
        Do not return anything, modify matrix in-place instead.
        """
        n = len(matrix)
        
        # 1. 物理微操第一步：沿主对角线转置（对称对折）
        for i in range(n):
            # 🚨 灵魂线：j 从 i 开始，只扫描右上三角，防止重复对调
            for j in range(i, n):
                matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]
                
        # 2. 物理微操第二步：将转置后的矩阵每一行原地水平“掉头”
        for i in range(n):
            matrix[i].reverse()
```

---

# 🛡️ LeetCode 54. Spiral Matrix (螺旋矩阵)

## 📝 1. 题目描述 (Description)

给你一个 `m × n` 的矩阵 `matrix` ，请按照 **顺时针螺旋顺序** ，返回矩阵中的所有元素。

### 示例 1:


> **输入**: matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
> **输出**: [1, 2, 3, 6, 9, 8, 7, 4, 5]

---

## 🪓 2. 核心算法解析 ── 四面大铁墙边界动态向内合围流

这道题的几何外壳是一条顺时针不断“转弯、向内盘旋”的贪吃蛇。

最优雅的工业级解法是 **【四墙合围法】**。我们不需要算步数，也不需要用方向数组，而是直接在矩阵的四个最外缘筑起四面大铁墙：
* **`top = 0`**（顶墙）
* **`bottom = len(matrix) - 1`**（底墙）
* **`left = 0`**（左墙）
* **`right = len(matrix[0]) - 1`**（右墙）

### 🏃‍♂️ 贪吃蛇四路循环大扫荡：

贪吃蛇按照 **“向右 ➔ 向下 ➔ 向左 ➔ 向上”** 的钢铁纪律路线循环行军。每扫完一行或一列（代表这面墙的数据彻底报废），对应的铁墙就立刻向内收缩一级，**并立刻进行安全审计（熔断检查）**：

1. **➡️ 1. 向右冲锋（扫顶行）**：
   从当前的 `left` 一路扫到 `right`。扫完后，顶层宣告作废，顶墙向下压一级（`top += 1`）。
   - **🚨 核心安检**：如果发现 `top > bottom`，说明上下大军已经撞头，战场清洗完毕，直接 `break`！
2. **⬇️ 2. 向下冲锋（扫右列）**：
   从当前的 `top` 一路扫到 `bottom`。扫完后，右侧列宣告作废，右墙向左缩一级（`right -= 1`）。
   - **🚨 核心安检**：如果发现 `left > right`，说明左右大军撞头，直接 `break`！
3. **⬅️ 3. 向左冲锋（扫底行）**：
   从当前的 `right` **倒序**一路扫回 `left`（`range(right, left - 1, -1)`）。扫完后，底层作废，底墙向上抬一级（`bottom -= 1`）。
   - **🚨 核心安检**：如果 `top > bottom`，立马熔断。
4. **⬆️ 4. 向上冲锋（扫左列）**：
   从当前的 `bottom` **倒序**一路扫回 `top`（`range(bottom, top - 1, -1)`）。扫完后，左侧列作废，左墙向右推一级（`left += 1`）。
   - **🚨 核心安检**：如果 `left > right`，立马熔断。

> 💡 **矩阵走水口诀**：**“每扫完一面墙，高墙立刻往内缩；缩完秒做安全检，撞墙瞬间就收工！”**

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:
        # 1. 进来先做边界防御
        if not matrix or not matrix[0]:
            return []
            
        ans = []
        # 2. 顶格摆开四面大铁墙边界
        top = 0
        bottom = len(matrix) - 1
        left = 0
        right = len(matrix[0]) - 1
        
        # 3. 开启四路循环大扫荡
        while True:
            # ➡️ A. 向右冲锋扫顶行（从左到右）
            for col in range(left, right + 1):
                ans.append(matrix[top][col])
            top += 1  # 顶墙无情下压一级
            if top > bottom:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
            # ⬇️ B. 向下冲锋扫右列（从上到下）
            for row in range(top, bottom + 1):
                ans.append(matrix[row][right])
            right -= 1  # 右墙无情左移一级
            if left > right:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
            # ⬅️ C. 向左冲锋扫底行（倒序！从右到左）
            for col in range(right, left - 1, -1):
                ans.append(matrix[bottom][col])
            bottom -= 1  # 底墙无情上抬一级
            if top > bottom:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
            # ⬆️ D. 向上冲锋扫左列（倒序！从下到上）
            for row in range(bottom, top - 1, -1):
                ans.append(matrix[row][left])
            left += 1  # 左墙无情右移一级
            if left > right:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
        # 4. 交出全场螺旋大扫荡战报
        return ans
```

---

# 🛡️ LeetCode 54. Spiral Matrix (螺旋矩阵)

## 📝 1. 题目描述 (Description)

给你一个 `m × n` 的矩阵 `matrix` ，请按照 **顺时针螺旋顺序** ，返回矩阵中的所有元素。

### 示例 1:


> **输入**: matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
> **输出**: [1, 2, 3, 6, 9, 8, 7, 4, 5]

---

## 🪓 2. 核心算法解析 ── 四面大铁墙边界动态向内合围流

这道题的几何外壳是一条顺时针不断“转弯、向内盘旋”的贪吃蛇。

最优雅的工业级解法是 **【四墙合围法】**。我们不需要算步数，也不需要用方向数组，而是直接在矩阵的四个最外缘筑起四面大铁墙：
* **`top = 0`**（顶墙）
* **`bottom = len(matrix) - 1`**（底墙）
* **`left = 0`**（左墙）
* **`right = len(matrix[0]) - 1`**（右墙）

### 🏃‍♂️ 贪吃蛇四路循环大扫荡：

贪吃蛇按照 **“向右 ➔ 向下 ➔ 向左 ➔ 向上”** 的钢铁纪律路线循环行军。每扫完一行或一列（代表这面墙的数据彻底报废），对应的铁墙就立刻向内收缩一级，**并立刻进行安全审计（熔断检查）**：

1. **➡️ 1. 向右冲锋（扫顶行）**：
   从当前的 `left` 一路扫到 `right`。扫完后，顶层宣告作废，顶墙向下压一级（`top += 1`）。
   - **🚨 核心安检**：如果发现 `top > bottom`，说明上下大军已经撞头，战场清洗完毕，直接 `break`！
2. **⬇️ 2. 向下冲锋（扫右列）**：
   从当前的 `top` 一路扫到 `bottom`。扫完后，右侧列宣告作废，右墙向左缩一级（`right -= 1`）。
   - **🚨 核心安检**：如果发现 `left > right`，说明左右大军撞头，直接 `break`！
3. **⬅️ 3. 向左冲锋（扫底行）**：
   从当前的 `right` **倒序**一路扫回 `left`（`range(right, left - 1, -1)`）。扫完后，底层作废，底墙向上抬一级（`bottom -= 1`）。
   - **🚨 核心安检**：如果 `top > bottom`，立马熔断。
4. **⬆️ 4. 向上冲锋（扫左列）**：
   从当前的 `bottom` **倒序**一路扫回 `top`（`range(bottom, top - 1, -1)`）。扫完后，左侧列作废，左墙向右推一级（`left += 1`）。
   - **🚨 核心安检**：如果 `left > right`，立马熔断。

> 💡 **矩阵走水口诀**：**“每扫完一面墙，高墙立刻往内缩；缩完秒做安全检，撞墙瞬间就收工！”**

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:
        # 1. 进来先做边界防御
        if not matrix or not matrix[0]:
            return []
            
        ans = []
        # 2. 顶格摆开四面大铁墙边界
        top = 0
        bottom = len(matrix) - 1
        left = 0
        right = len(matrix[0]) - 1
        
        # 3. 开启四路循环大扫荡
        while True:
            # ➡️ A. 向右冲锋扫顶行（从左到右）
            for col in range(left, right + 1):
                ans.append(matrix[top][col])
            top += 1  # 顶墙无情下压一级
            if top > bottom:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
            # ⬇️ B. 向下冲锋扫右列（从上到下）
            for row in range(top, bottom + 1):
                ans.append(matrix[row][right])
            right -= 1  # 右墙无情左移一级
            if left > right:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
            # ⬅️ C. 向左冲锋扫底行（倒序！从右到左）
            for col in range(right, left - 1, -1):
                ans.append(matrix[bottom][col])
            bottom -= 1  # 底墙无情上抬一级
            if top > bottom:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
            # ⬆️ D. 向上冲锋扫左列（倒序！从下到上）
            for row in range(bottom, top - 1, -1):
                ans.append(matrix[row][left])
            left += 1  # 左墙无情右移一级
            if left > right:  # 🚨 安全审计：一旦越界，当场熔断
                break
                
        # 4. 交出全场螺旋大扫荡战报
        return ans
```

---

# 🛡️ LeetCode 73. Set Matrix Zeroes (矩阵置零)

## 📝 1. 题目描述 (Description)

给定一个 `m x n` 的矩阵，如果一个元素为 `0` ，则将其所在行和列的所有元素都设为 `0` 。请使用 **原地 (In-place)** 算法。

- **📈 空间终极挑战**：不能开辟额外空间，必须在原矩阵内部闪转腾挪，使额外空间复杂度达到绝对完美的 **$\mathcal{O}(1)$**！

### 示例 1:
> **输入**: matrix = [[1, 1, 1], [1, 0, 1], [1, 1, 1]]
> **输出**: [[1, 0, 1], [0, 0, 0], [1, 0, 1]]


## 🪓 2. 核心算法解析 ── “借尸还魂”：借用矩阵第一行第一列记账

既然题目不让我们额外开辟两个辅助数组来记录“哪行哪列该清零”，我们环顾全场，发现矩阵的**第一行**和**第一列**在内存里本来就是常驻的。

### 💡 惊天白嫖思维：
我们直接**借用矩阵自身的第一行和第一列，来扮演那两个原本需要额外向系统申请的哈希备忘录！**

#### 🏃‍♂️ 物理控盘四步走：

1. **御前护卫，拍照存证（独立审计第一行第一列）**：
   因为我们要用第一行和第一列当账本，这上面很快就会被我们写满 `0`。为了防止后面分不清“它们上面的 0 到底是记录别人的，还是它们自己本来就带的”，我们迎面先用两个布尔变量 `row0_has_zero` 和 `col0_has_zero` 记录它们自己的本来面目。
2. **大部队内陆扫荡，借地盘记账**：
   大军从第二行第二列（`i=1, j=1`）开始全场突击。一旦发现 `matrix[i][j] == 0`，立刻让它对应的行首和列首去充当内鬼：
   - 在它这行的大门口插个 0 旗：`matrix[i][0] = 0`
   - 在它这列的大门口插个 0 旗：`matrix[0][j] = 0`
3. **看大门旗帜，全量清洗内陆**：
   再次扫描除大门口（第一行第一列）之外的所有内陆元素（从 `i=1, j=1` 开始）。只要看到它所在的这行的大门口、或者这列的大门口插了 `0` 旗，它自己就**无脑原地抹除变成 `0`**！
4. **收尾会师，御前护卫归位**：
   最后，根据第一步拍下的存证快照：
   - 如果 `row0_has_zero` 为 `True`，说明第一行本来就该全军覆没，直接把第一行全擦成 0。
   - 如果 `col0_has_zero` 为 `True`，同理，把第一列全擦成 0。

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def setZeroes(self, matrix: List[List[int]]) -> None:
        """
        Do not return anything, modify matrix in-place instead.
        """
        m = len(matrix)
        n = len(matrix[0])
        
        # 1. 物理微操第一步：独立审计第一行和第一列自己是否原本带 0
        row0_has_zero = any(matrix[0][col] == 0 for col in range(n))
        col0_has_zero = any(matrix[row][0] == 0 for row in range(m))
                
        # 2. 物理微操第二步：白嫖地盘！用第一行和第一列作为内陆元素的记账本
        for i in range(1, m):
            for j in range(1, n):
                if matrix[i][j] == 0:
                    matrix[i][0] = 0  # 对应行的大门口插 0 旗
                    matrix[0][j] = 0  # 对应列的大门口插 0 旗
                    
        # 3. 物理微操第三步：根据大门口的旗帜，全量清洗内陆区域
        for i in range(1, m):
            for j in range(1, n):
                if matrix[i][0] == 0 or matrix[0][j] == 0:
                    matrix[i][j] = 0
                    
        # 4. 物理微操第四步：根据最初的快照，单独收尾大门口的第一行和第一列
        if row0_has_zero:
            for col in range(n):
                matrix[0][col] = 0
                
        if col0_has_zero:
            for row in range(m):
                matrix[row][0] = 0
```

# ☣️ 矩阵跨界深度连线：LeetCode 73（矩阵置零） vs LeetCode 994（腐烂的橘子）

你的算法直觉简直是**教科书级别的敏锐**！你能把矩阵置零（73题）和腐烂的橘子（994题）隔空连线，说明你已经穿透了题目的业务外壳，直接看到了它们在内存空间里的**“污染源扩散机制”**。

但是，这两道题虽然看起来都是“一个坏分子（0 或 烂橘子）污染周围一片”，但在**时空控盘**和**底层算法流派**上，它们其实是**两个截然不同的极端**。今天咱们就用大白话把它们放在一起跨界 PK，彻底打通你的矩阵大局观！

## 📊 1. 相似的物理画面：一个内鬼，全家遭殃

* **LeetCode 73（矩阵置零）**：只要某个格子是 `0`，那它就像一个放射性污染源，瞬间释放冲击波，把它所在的**一整行、一整列**的所有正常数字全部强行抹成 `0`。
* **LeetCode 994（腐烂的橘子）**：只要某个格子是烂橘子（值是 `2`），它就会在 1 分钟内，向它的**上下左右四个邻居**扩散病毒，把好橘子（值是 `1`）也变成烂橘子。

## 🚨 2. 致命的本质区别：73 题为什么不能像橘子那样“边跑边扩散”？

你仔细想一想，如果我们在写 73 题时，也用烂橘子的思路，从左到右扫描矩阵：
1. 刚扫到第一行中间，突然发现一个 `0`。
2. 我们一拍大腿：“污染开始！”立刻顺手把这一整行、一整列全部改成 `0`。
3. 接着，我们继续往下扫描。
4. **💥 毁灭性灾难降临！** 接下来你扫到后面的行时，你会看到一大堆 `0`。但这些 `0` 根本不是原矩阵自带的，而是你刚才**刚刚手动改出来的“假内鬼”**！

结果你的代码会像疯狗一样，把这些“假内鬼”的行和列也全擦成 `0`。最后全场跑完，**整个矩阵被你全部洗成全 0**，当场破产！

## 💡 3. 核心底层逻辑的跨界分水岭

### 🏛️ 派系 A：LeetCode 73 矩阵置零 ── “静态时空下的瞬时爆炸”
* **💥 痛点**：新的 `0` 会污染旧的遍历。我们必须**把“记录内鬼”和“全量清洗”在时空上完全隔离开**！
* **🪓 手艺**：所以我们玩的是**“借尸还魂记账流”**（白嫖第一行第一列做登记簿），或者开辟额外的布尔表。**先全量登记，最后统一枪毙**，绝对不能边走边杀。

### 🎪 派系 B：LeetCode 994 腐烂的橘子 ── “动态时空下的水波纹平推（BFS）”
* **💥 痛点**：橘子的腐烂是**有时间轴的（第 1 分钟、第 2 分钟……）**，而且是一级一级往外动态传染的。
* **🪓 手艺**：它完美的物理外壳是 **【广度优先搜索 (BFS) / 队列】**。
  1. 初始时，必须把全场**所有第一批天生就烂的橘子坐标**同时扔进队列。
  2. 接着玩你最擅长的“102题水波纹平推”：提前拍照锁死当前批次的 `queue_size`（发放第 $T$ 分钟的入场券）。
  3. 队列里的烂橘子出队，去把周围的四个好橘子咬烂。被咬烂的新受害者打上标记，**排到队列尾巴上**，等待下一分钟的批量结算。

## 🎯 4. 终极一句话闭环

老铁，你这一波跨界连线，在工程规范上叫**“模式识别（Pattern Recognition）”**：

> **73 题是“为了防脏数据污染而必须先白嫖记账、后整体清洗的静态空间压榨题”；**
> **橘子题则是“为了计算传染波次而必须祭出队列进行水波纹平推的动态图论 BFS 题”。**

---

# 🛡️ LeetCode 73. Set Matrix Zeroes (矩阵置零)

## 📝 1. 题目描述 (Description)

给定一个 `m x n` 的矩阵，如果一个元素为 `0` ，则将其所在行和列的所有元素都设为 `0` 。请使用 **原地 (In-place)** 算法。

- **📈 空间终极挑战**：不能开辟额外空间，必须在原矩阵内部闪转腾挪，使额外空间复杂度达到绝对完美的 **$\mathcal{O}(1)$**！

### 示例 1:
> **输入**: matrix = [[1, 1, 1], [1, 0, 1], [1, 1, 1]]
> **输出**: [[1, 0, 1], [0, 0, 0], [1, 0, 1]]

---

## 🪓 2. 核心算法解析 ── “借尸还魂”：借用矩阵第一行第一列记账

既然题目不让我们额外开辟两个辅助数组来记录“哪行哪列该清零”，我们环顾全场，发现矩阵的**第一行**和**第一列**在内存里本来就是常驻的。



### 💡 惊天白嫖思维：
我们直接**借用矩阵自身的第一行和第一列，来扮演那两个原本需要额外向系统申请的哈希备忘录！**

#### 🏃‍♂️ 物理控盘四步走：

1. **御前护卫，拍照存证（独立审计第一行第一列）**：
   因为我们要用第一行和第一列当账本，这上面很快就会被我们写满 `0`。为了防止后面分不清“它们上面的 0 到底是记录别人的，还是它们自己本来就带的”，我们迎面先用两个布尔变量 `row0_has_zero` 和 `col0_has_zero` 记录它们自己的本来面目。
2. **大部队内陆扫荡，借地盘记账**：
   大军从第二行第二列（`i=1, j=1`）开始全场突击。一旦发现 `matrix[i][j] == 0`，立刻让它对应的行首和列首去充当内鬼：
   - 在它这行的大门口插个 0 旗：`matrix[i][0] = 0`
   - 在它这列的大门口插个 0 旗：`matrix[0][j] = 0`
3. **看大门旗帜，全量清洗内陆**：
   再次扫描除大门口（第一行第一列）之外的所有内陆元素（从 `i=1, j=1` 开始）。只要看到它所在的这行的大门口、或者这列的大门口插了 `0` 旗，它自己就**无脑原地抹除变成 `0`**！
4. **收尾会师，御前护卫归位**：
   最后，根据第一步拍下的存证快照：
   - 如果 `row0_has_zero` 为 `True`，说明第一行本来就该全军覆没，直接把第一行全擦成 0。
   - 如果 `col0_has_zero` 为 `True`，同理，把第一列全擦成 0。

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def setZeroes(self, matrix: List[List[int]]) -> None:
        """
        Do not return anything, modify matrix in-place instead.
        """
        m = len(matrix)
        n = len(matrix[0])
        
        # 1. 物理微操第一步：独立审计第一行和第一列自己是否原本带 0
        row0_has_zero = any(matrix[0][col] == 0 for col in range(n))
        col0_has_zero = any(matrix[row][0] == 0 for row in range(m))
                
        # 2. 物理微操第二步：白嫖地盘！用第一行和第一列作为内陆元素的记账本
        for i in range(1, m):
            for j in range(1, n):
                if matrix[i][j] == 0:
                    matrix[i][0] = 0  # 对应行的大门口插 0 旗
                    matrix[0][j] = 0  # 对应列的大门口插 0 旗
                    
        # 3. 物理微操第三步：根据大门口的旗帜，全量清洗内陆区域
        for i in range(1, m):
            for j in range(1, n):
                if matrix[i][0] == 0 or matrix[0][j] == 0:
                    matrix[i][j] = 0
                    
        # 4. 物理微操第四步：根据最初的快照，单独收尾大门口的第一行和第一列
        if row0_has_zero:
            for col in range(n):
                matrix[0][col] = 0
                
        if col0_has_zero:
            for row in range(m):
                matrix[row][0] = 0
```

---

# 🛡️ LeetCode 240. Search a 2D Matrix II (搜索二维矩阵 II)

## 📝 1. 题目描述 (Description)

编写一个高效的算法来搜索 `m x n` 矩阵 `matrix` 中的一个目标值 `target` 。该矩阵具有以下特性：
- 每行的元素从左到右升序排列。
- 每列的元素从上到下升序排列。

### 示例 1:


> **输入**: matrix = [
>   [1,   4,  7, 11, 15],
>   [2,   5,  8, 12, 19],
>   [3,   6,  9, 16, 22],
>   [10, 13, 14, 17, 24],
>   [18, 21, 23, 26, 30]
> ], target = 5
> **输出**: true

---

## 🪓 2. 核心算法解析 ── 右上角马鞍点（Decision Tree）的阶梯斩杀魔术

面对行也升序、列也升序的“杨氏矩阵”，如果我们从左上角（`[0][0]`）出发，往右是变大，往下也是变大，这就陷入了“左右为难”的决策死胡同。

但是！如果我们把大军驻扎在矩阵的 **【右上角】**（`row = 0, col = n - 1`，示例 1 中的 `15`），神奇的物理画面诞生了：

### 💡 惊天破局大发现：
站在右上角这个特定坐标：
- **向左看**：数字在单调**减小**（`15 ➔ 11 ➔ 7...`）
- **向下看**：数字在单调**增大**（`15 ➔ 19 ➔ 22...`）

这就完美形成了一个**绝无二义性的“十字路口决策分叉”**！它在几何上被称为马鞍点。我们拿当前值 `matrix[row][col]` 和 `target` 直接对线，每次决策都能**直接无情枪毙掉整整一行或者整整一列**：

#### 🏃‍♂️ 三路合围行军指令：

1. **第一路：`matrix[row][col] == target`**
   - 🎯 逮个正着！一枪封喉，直接返回 `True` 闪人。
2. **第二路：`matrix[row][col] > target`（当前值嫌大了！）**
   - **物理画面**：既然当前值已经比 `target` 大了，而这一列**下方的所有数字由于单调递增，必然比当前值还要大**，因此 `target` 绝不可能藏在这一列的下面。
   - **行军指令**：整列报废！大军果断向左迈一步（**`col -= 1`**），相当于直接从战场上抹去了这一整列！
3. **第三路：`matrix[row][col] < target`（当前值嫌小了！）**
   - **物理画面**：既然当前值比 `target` 小，而这一行**左边的所有数字由于单调递增，必然比当前值还要小**，因此 `target` 绝不可能藏在这一行的左边。
   - **行军指令**：整行报废！大军果断向下迈一步（**`row += 1`**），相当于直接从战场上抹去了这一整行！

> 💡 **马鞍点斩杀口诀**：**“大军驻扎右上角，左瞧变小下变大；嫌大左移砍一列，嫌小下移剁一行！”**

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        # 1. 迎面先做防御性边界安检
        if not matrix or not matrix[0]:
            return False
            
        m = len(matrix)
        n = len(matrix[0])
        
        # 2. 初始化：大军在右上角马鞍点定格集结
        row = 0
        col = n - 1
        
        # 3. 阶梯式向内夹逼斩杀，只要没出界就继续盘
        while row < m and col >= 0:
            current = matrix[row][col]
            
            if current == target:
                # 🎯 精准捕获
                return True
            elif current > target:
                # 当前值太大，下面只会更大，整列无用，大军向左突击一行（砍掉当前列）
                col -= 1
            else:
                # 当前值太小，左边只会更小，整行无用，大军向下突击一步（砍掉当前行）
                row += 1
                
        # 4. 走出边界了还没撞见，宣告犯人不存在
        return False
```

---

