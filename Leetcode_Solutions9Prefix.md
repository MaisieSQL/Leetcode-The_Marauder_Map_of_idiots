【天梯 3 段】 ➔ 🌲 LeetCode 208. 实现 Trie (前缀树)   (Medium · 字符统治者)
  (高阶黑科技！把千万个字符串的公共前缀，全部焊进一棵多叉树里，实现 O(L) 级的终极查找！)
      ▲
【天梯 2 段】 ➔ 🌋 LeetCode 560. 和为 K 的子数组    (Medium · 顶级合围)
  (前缀和的斩首核心！前缀和 + 哈希表联动，在 O(N) 的线性闪电极速里抓出全场所有隐藏内鬼！)
      ▲
【天梯 1 段】 ➔ 🪜 LeetCode 303. 区域和检索 - 数组不可变 (Easy · 🟢 筑基前缀和)
  (开局母题！摆开前缀和资产对账单，把 O(N) 的区间查询暴力降维成 O(1) 的啪叽一减！)
------------------------------------------------------------------------------------------------------------------------------------------------------------------

# 🛡️ LeetCode 303. Range Sum Query - Immutable (区域和检索 - 数组不可变)

## 📝 1. 题目描述 (Description)

给定一个整数数组  `nums`，处理以下类型的多个查询：
* 计算索引 `left` 和 `right`（包含 `left` 和 `right`）之间的 `nums` 元素的 **总和** ，其中 `left <= right` 。

实现 `NumArray` 类：
* `NumArray(int[] nums)` 使用数组 `nums` 初始化对象。
* `int sumRange(int left, int right)` 返回数组 `nums` 从索引 `left` 到 `right`（包含 `left` 和 `right`）的的 **总和** 。

### 示例 1:
> **输入**: 
> ["NumArray", "sumRange", "sumRange", "sumRange"]
> [[[-2, 0, 3, -5, 2, -1]], [0, 2], [2, 5], [0, 5]]
>
> **输出**: 
> [null, 1, -1, -3]
>
> **解释**:
> NumArray numArray = new NumArray([-2, 0, 3, -5, 2, -1]);
> numArray.sumRange(0, 2); // 返回 1 ((-2) + 0 + 3)
> numArray.sumRange(2, 5); // 返回 -1 (3 + (-5) + 2 + (-1))
> numArray.sumRange(0, 5); // 返回 -3 ((-2) + 0 + 3 + (-5) + 2 + (-1))

## 🪓 2. 核心算法解析 ── 阴招十一：【开局多开一格建总账，查账啪叽一减吃利差】

这道题如果傻傻地在 `sumRange` 里面写个 `for loop` 累加，面对十万次的疯狂轰炸，绝对会被系统当场超时枪毙。
资本家流派玩的是 **【空间换时间】**，在初始化（`__init__`）的时候，就把全场的大账一步到位全部算死！

### 🎬 控盘两步走：

1. **第一步：多开一格，初始化对账单（`prefix_sums`）**
   如果原始数组 `nums` 的长度是 `N`，我们偏要开辟一个长度为 **`N + 1`** 的空白数组。
   * **为什么要故意多开一格？** 为了放一个 `0` 元素在第 `0` 位托底（`prefix_sums[0] = 0`），代表“开局两手空空，资产为 0”。
     这样不仅可以优雅地处理从 `0` 号位开始的区间查询，还彻底免去了恶心的边界 `if-else` 判断！
   * **公式递推**：`prefix_sums[i + 1] = prefix_sums[i] + nums[i]`
     （今天的前缀和 = 昨天的总和 + 今天的价格。滚雪球式一趟搞定！）

2. **第二步：一枪封喉，$\mathcal{O}(1)$ 极速提款**
   当大老板来问 `left` 到 `right` 的总和时，我们直接通过对账单执行就地扣减：
   $$\text{return } \text{prefix\_sums}[right + 1] - \text{prefix\_sums}[left]$$

### 🎰 物理对账现场：
你想求 `[left, right]` 的利润。
* `prefix_sums[right + 1]` 代表**从全场开局一直到 `right` 这一站的总资产**。
* `prefix_sums[left]` 代表**大军跨进 `left` 这一站之前，历史累计丢掉的成本**。
* 两者相减，生生把前面的累赘成本扣掉，剩下的不就是区间纯利润吗？！

## 💻 3. Python3 完美通关代码

```python
class NumArray:

    def __init__(self, nums: List[int]):
        # 1. 贪心资本家手艺：多开一格（n + 1），防止 left-1 越界，全场 0 托底
        n = len(nums)
        self.prefix_sums = [0] * (n + 1)
        
        # 2. 滚雪球建账：全量提前算大账，单层循环完成初始化
        for i in range(n):
            self.prefix_sums[i + 1] = self.prefix_sums[i] + nums[i]

    def sumRange(self, left: int, right: int) -> int:
        # 3. 🔥 终极 $\mathcal{O}(1)$ 提款：拿右边界的总账，扣掉左边界前面的累赘
        return self.prefix_sums[right + 1] - self.prefix_sums[left]


# Your NumArray object will be instantiated and called as such:
# obj = NumArray(nums)
# param_1 = obj.sumRange(left,right)
```

# 🪓 算法面试终极破局：高工级【三段对线心法】大盘

在算法复习、面试与技术输出中，彻底跳出“为了做题而做题”的底层重体力劳动泥潭。我们将全量贯彻以下**【终极三段论框架】**来审视和拆解每一道经典母题，让每一次复习都沉淀为居高临下的工业级控盘战力：
1. **为什么用这个算法（核心物理图像、破局点、以及对传统算法的防御）**
2. **时空复杂度是多少（硬核工业级算法审计）**
3. **为什么是最少最好的（降维打击、空间平替与落子无悔的终极证明）**

---

## ⚔️ 终极对线模板打样：LeetCode 560. Subarray Sum Equals K (和为 K 的子数组)

### 🏛️ 一、 为什么用【前缀和 + 哈希表】这个算法？

* **物理破局点**：题目求的是**连续子数组的和**。在物理画面上，任何一段区间连续子数组的求和，本质上都等同于“切香肠”──即利用两个历史前缀和做差扣减： 
  $$current\_sum - history\_sum == k$$
* **移项大挪移**：通过纯粹的代数移项，我们把复杂的“区间穷举寻找”降维转换成了“对历史形态的核验”：
  $$history\_sum == current\_sum - k$$
* **雷达防线（为什么绑定哈希表，为什么滑动窗口翻车？）**：因为原始数组里**包含负数和 0**！随着窗口拓宽，里面的总和不仅可能变大，还可能变小，传统的滑动窗口（Sliding Window）由于**彻底失去了单调性控制线**当场瘫痪翻车！
* **解决方案**：所以我们必须拉起一个哈希表作为“历史档案馆（雷达）”。真身指针每向前走一步，绝对不回头倒车，直接用雷达扫描历史档案馆里有没有出现过目标资产 $current\_sum - k$。如果有，说明历史回音在此刻产生时空共鸣，隔空一刀，精准切出合规子数组！

---

### ⏱️ 二、 这个算法的时间和空间复杂度是多少？

* **Time Complexity (时间复杂度)：$\mathcal{O}(N)$** ── 极其清爽的**单趟平推流**！由于哈希表（`count_map`）的查询、对账和写入开销在底层都是恐怖的 $\mathcal{O}(1)$ 常数级，我们的真身指针从左到右扫一遍数组，走一个数，听一次历史回音，绝不倒车、绝不回头！
* **Space Complexity (空间复杂度)：$\mathcal{O}(N)$** ── 极其划算的**空间换时间投资**！我们随身携带了一个哈希表来记录一路上产生过的历史前缀和形态。最坏情况下（例如数组全是正数，前缀和一路上永不重复），哈希表会装满 $N$ 个键值对。

---

### 🪓 三、 为什么说它是最少、最好的解法？

* **对比凡人流（为什么最少最好）**：普通的凡人流只会写两层 `for` 循环硬碰硬去穷举边界，每走到一站，都要冒着风雨肉身回头数一遍历史（时间复杂度 $\mathcal{O}(N^2)$），面对大厂十万级高并发测试数据直接爆仓超时！而我们**通过引入一个哈希表（空间成本），生生把时间复杂度从二维的 $\mathcal{O}(N^2)$ 降维捏爆成了线性的 $\mathcal{O}(N)$ 闪电极速！**
* **骨架压缩的终极变态体现**：这道题最绝的是，它在代码层面上**甚至没有开辟庞大的 `prefix` 数组**！而是玩了极其精妙的空间平替：
  * **局部的一站** ── 强行压缩、压缩成了单个整型变量 `current_sum`，用来动态代表**当下的最新前缀和**；
  * **宏观的历史** ── 全量托管给随身哈希表 `count_map`，用来收集**过去产生过的所有前缀和次数**！
* **局部最优变成全局最优**：用最少的代码骨架和最轻量的变量，完成了跨越时空的宏观清算。属于“走一步，锁定一步，占足便宜，绝不留恋过去”的工业级完美控盘典范！

---------------------------------------------------------------------------------------------------------------

