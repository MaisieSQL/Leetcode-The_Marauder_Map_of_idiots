# 🗂️ 算法面试“小暗器”大归宗

## 1. 大类：线性数据结构 —— 数组与字符串 (Array & String)
> **核心痛点：** 数组的物理结构导致“中间插入/删除/修改”太慢 ($O(N)$)。以下暗器全是为了**降低时间复杂度**或**规避死循环**而生的奇技淫巧。

*   ### 🥷 差分数组 (Difference Array)
    *   **主攻场景：** 频繁对数组的某个**区间**进行集体加减分（比如：把 `[L, R]` 区间的所有数都加 `X`）。
    *   **核心逻辑：** 在 $L$ 位置 $+X$，在 $R+1$ 位置 $-X$，最后求一遍前缀和。把 $O(N)$ 的区间修改优化到 $O(1)$。

*   ### 🥷 前缀和 (Prefix Sum) / 二维前缀和
    *   **主攻场景：** 高频查询数组中任意一段**区间/子矩阵的和**。
    *   **核心逻辑：** 空间换时间，提前算好一堵“累加和墙”，要查区间直接用减法 `P[R] - P[L-1]` 瞬间吐出答案，将查询优化到 $O(1)$。

*   ### 🥷 汇总区间 / 区间合并 (Intervals / Meeting Rooms)
    *   **主攻场景：** 时间段重叠、会议室安排、火车座舱预订。
    *   **核心逻辑：** **“区间问题先排序”**。按 Start 或 End 排序后，问题退化为单遍扫描，核心在考你的边界控制代码（if-else）。

*   ### 🥷 滑动窗口 (Sliding Window)
    *   **主攻场景：** 寻找满足条件的**最长/最短/特定子串或子数组**（如：不含有重复字符的最长子串）。
    *   **核心逻辑：** 双指针的变体，维护一个会伸缩的“框”，右边界负责吃元素，左边界负责吐元素，绝不走回头路。

## 2. 大类：搜索与优化策略 —— 贪心与搜索空间 (Greedy & Search)
> **核心痛点：** 题目看起来可能性太多，暴力枚举会超时。

*   ### 🥷 扫描线算法 (Scan Line / Event Driven)
    *   **主攻场景：** 天际线问题（Skyline）、会议室最多同时有多少人、重叠矩形面积。
    *   **核心逻辑：** 把空间/时间问题变成“事件轴”。遇到“起点”就触发事件 A（比如房间数 $+1$），遇到“终点”就触发事件 B（房间数 $-1$），跟着时间线走格子。

*   ### 🥷 单调栈 / 单调队列 (Monotonic Stack/Queue)
    *   **主攻场景：** 下一个更大的元素、接雨水、滑动窗口最大值、温度升高需要等几天。
    *   **核心逻辑：** 栈/队列内部强行保持从大到小（或从小到大）。新来的人如果破坏了规矩，就把里面的人“踢”出来，专门用来找**“离我最近的更大/更小元素”**。

## 3. 大类：数学与位运算 —— 隐藏的性能怪兽 (Math & Bitwise)
> **核心痛点：** 看起来是逻辑题或数论题，但实际上有 $O(1)$ 的数学解法。

*   ### 🥷 异或运算的特殊性质 (XOR Tricks)
    *   **主攻场景：** 数组里除了一个数出现了奇数次，其余都出现了偶数次，找出这个数（只出现一次的数字）。
    *   **核心逻辑：** 利用 `A ^ A = 0` 和 `0 ^ A = A` 的消消乐特性，把整个数组全部异或一遍，相同的全部消失，留下的就是唯一的落单者。

*   ### 🥷 快慢指针判圈法 (Floyd's Cycle Detection / 龟兔赛跑)
    *   **主攻场景：** 链表是否有环、快乐数（Happy Number）。
    *   **核心逻辑：** 只要有环，跑得快的指针（一步两格）和跑得慢的指针（一步一格）就一定会相遇。

## 4. 大类：高级数据结构 —— 关系与集合 (Relationships & Sets)
> **核心痛点：** 如何快速合并两个圈子，或者快速判断两个人是不是“一伙的”。

*   ### 🥷 并查集 (Union-Find / Disjoint Set)
    *   **主攻场景：** 朋友圈（求省份数量）、网络连通性、岛屿数量（图的连通分量）。
    *   **核心逻辑：** 两个核心动作：`union(A, B)` 让 A 和 B 结盟，`find(A)` 找 A 的帮主是谁。如果两个人的帮主是同一个人，说明是一伙的。

*   ### 🥷 前缀树 / 字典树 (Trie)
    *   **主攻场景：** 搜索框自动补全、敏感词过滤、判断一堆字符串有没有共同前缀。
    *   **核心逻辑：** 26叉树。把单词拆成字母，公共前缀只存一份，大大节省字符串匹配的时间和空间。

---

# 📂 算法江湖暗器大归宗图谱

*   ## 根目录: 算法与数据结构核心体系
    *   ### 📂 [01] 线性数据结构 —— 数组与字符串 (Array & String)
        *   **🥷 差分数组 (Difference Array)** ──► *[主攻]:* 区间频繁批量修改
        *   **🥷 前缀和 (Prefix Sum)** ──► *[主攻]:* $O(1)$ 高频查询区间和
        *   **🥷 汇总区间与合并 (Intervals)** ──► *[主攻]:* 时间重叠与边界控制
        *   **🥷 滑动窗口 (Sliding Window)** ──► *[主攻]:* 最长/最短连续子串或子数组
    *   ### 📂 [02] 搜索与优化策略 —— 贪心与搜索空间 (Greedy & Search)
        *   **🥷 扫描线算法 (Scan Line)** ──► *[主攻]:* 空间/时间维度的事件触发轴
        *   **🥷 单调栈/队列 (Monotonic)** ──► *[主攻]:* 寻找最近的更大或更小元素
    *   ### 📂 [03] 数学与位运算 —— 隐藏的性能怪兽 (Math & Bitwise)
        *   **🥷 异或运算妙用 (XOR Tricks)** ──► *[主攻]:* 偶数次抵消的落单元素查找
        *   **🥷 快慢指针判圈 (Floyd's Cycle)** ──► *[主攻]:* 隐式或显式链表拓扑成环判断
    *   ### 📂 [04] 高级数据结构 —— 关系与集合 (Relationships & Sets)
        *   **🥷 并查集 (Union-Find)** ──► *[主攻]:* 快速合并圈子与连通性判断
        *   **🥷 前缀树/字典树 (Trie)** ──► *[主攻]:* 空间复用与高效前缀匹配
