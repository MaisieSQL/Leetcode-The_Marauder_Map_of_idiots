| 题目 (Problem) | 核心类型 (Type) | 难度 (Difficulty) | LeetCode 传送门 (Link) |
| :--- | :--- | :--- | :--- |
| **26. Remove Duplicates from Sorted Array** | 双指针 / 数组原地修改 | 🟢 简单 (Easy) | [LeetCode 26](https://leetcode.com/problems/remove-duplicates-from-sorted-array/) |
| **143. Reorder List** | 链表综合（中点+反转+合并） | 🟡 中等 (Medium) | [LeetCode 143](https://leetcode.com/problems/reorder-list/) |
| **19. Remove Nth Node From End of List** | 链表 / 前后指针 | 🟡 中等 (Medium) | [LeetCode 19](https://leetcode.com/problems/remove-nth-node-from-end-of-list/) |
| **1095. Find in Mountain Array** | 二分查找 / 山脉数组 | 🔴 困难 (Hard) | [LeetCode 1095](https://leetcode.com/problems/find-in-mountain-array/) |

---

# 1095. 山脉数组中查找目标值 - 题目解析与答案

## 一、 题目描述

给定一个山脉数组 `mountainArr`，返回最小的 `index` 使得 `mountainArr.get(index) == target`。如果不存在这样的 `index`，返回 `-1`。

**什么是山脉数组？**
* 数组长度 $\ge 3$。
* 在算法中，索引 $i$ 满足 $0 < i < \text{length} - 1$ 的限制条件。
  * $arr[0] < arr[1] < \dots < arr[i]$
  * $arr[i] > arr[i + 1] > \dots > arr[\text{length} - 1]$

**限制条件：**
* 你无法直接访问山脉数组，只能通过 `MountainArray` 接口：
  * `MountainArray.get(k)`：返回数组中下标为 $k$ 的元素。
  * `MountainArray.length()`：返回数组的长度。
* 调用 `MountainArray.get` 超过 $100$ 次的提交会被判定为错误答案。

## 二、 解题思路

由于题目限制了 `get` 的调用次数不能超过 $100$ 次，且数组具有单调性（先升后降），我们可以很自然地联想到 **二分查找**。

整个解题过程可以分为以下 **三个步骤**：
1. **寻找峰顶元素**：利用二分查找找到山脉数组的最高点（峰顶）索引 $peakIndex$。
2. **在左侧升序数组中查找**：在 $[0, peakIndex]$ 区间内进行升序二分查找。如果找到 `target`，直接返回对应的索引。
3. **在右侧降序数组中查找**：如果在左侧没有找到，则在 $[peakIndex + 1, \text{length} - 1]$ 区间内进行降序二分查找。如果找到，返回索引；若仍未找到，返回 `-1`。

## 三、 Python 代码实现

```python
# """
# This is MountainArray's API interface.
# You should not implement it, or speculate about its implementation
# """
#class MountainArray(object):
#    def get(self, index):
#        """
#        :type index: int
#        :rtype int
#        """
#
#    def length(self):
#        """
#        :rtype int
# """
class Solution:
    def findInMountainArray(self, target: int, mountainArr: 'MountainArray') -> int:
        n = mountainArr.length()
        
        # 步骤 1：使用二分查找找到峰顶元素的索引
        left, right = 0, n - 1
        while left < right:
            mid = (left + right) // 2
            if mountainArr.get(mid) < mountainArr.get(mid + 1):
                left = mid + 1
            else:
                right = mid
        peak = left
        
        # 步骤 2：在左半部分（升序）进行二分查找
        left, right = 0, peak
        while left <= right:
            mid = (left + right) // 2
            val = mountainArr.get(mid)
            if val == target:
                return mid
            elif val < target:
                left = mid + 1
            else:
                right = mid - 1
                
        # 步骤 3：在右半部分（降序）进行二分查找
        left, right = peak + 1, n - 1
        while left <= right:
            mid = (left + right) // 2
            val = mountainArr.get(mid)
            if val == target:
                return mid
            elif val < target:
                right = mid - 1  # 降序数组中，值变小说明目标在左边（索引更小的方向）
            else:
                left = mid + 1   # 值变大说明目标在右边（索引更大的方向）
                
        return -1
```

## 四、 复杂度分析

* **时间复杂度**：$O(\log N)$。其中 $N$ 是数组的长度。寻找峰顶需要 $O(\log N)$，左右两部分二分查找最多各需要 $O(\log N)$，总共的接口调用次数远小于 $100$ 次（在 $N \le 10^4$ 的情况下，调用次数通常在 $30$ 次左右），完全满足题目限制。
* **空间复杂度**：$O(1)$。只需要常数级别的额外空间来存储指针变量。

其实在算法的世界里，面对这种有特殊形态的数组，思维不仅可以开阔，还能演变出不同的思考路径。咱们一起来开阔一下思路，然后对比看看为什么正统的“三段式二分”才是当之无愧的最优解。

## 🗺️ 解法思路大盘点：从笨办法到绝顶聪明

### 💡 思路一：暴力平推流（Linear Scan / 顺藤摸瓜）
* **怎么想的**：既然管理员说山有那么多格子，那我不动脑子，直接从头（第 0 个）开始，一个一个格子问管理员：“是你吗？不是。那下一个是你吗？”一直摸到头。
* **致命痛点**：
  * **时间复杂度**：$\mathcal{O}(N)$。如果山长 10 万个格子，你最多得问 10 万次。
  * **超次数嫌疑**：管理员有严格的查询限制（不能超过 100 次）。这种暴力平推流在面对 LeetCode 的大规模测试用例时，直接就会被评测机无情地按死在超时（Time Limit Exceeded）的沙滩上。

### 🚀 思路二：三段式二分查找（The Standard Optimal Approach ➔ 正统最优解）
* **怎么想的**：也就是咱们刚刚盘明白的那个逻辑——分而治之。
  * **第 1 次二分**：用二分法找出山顶在哪。
  * **第 2 次二分**：在左边那段“上坡路”（升序区间）里用二分找目标值。
  * **第 3 次二分**：如果在左边没找到，转到右边那段“下坡路”（降序区间）里用二分找。
* **为什么它是工业级最优**：
  * **时间复杂度**：$\mathcal{O}(\log N)$。因为每一次二分都是“对半砍”，哪怕数组长度有 10 亿，总共也只需要问十几次到几十次，绝对不会超过 100 次的上限！
  * **空间复杂度**：$\mathcal{O}(1)$。只用了几个指针变量，没有额外开辟任何大数组。

为什么题目偏偏不让你直接访问整个数组 `nums[i]`，而是非要逼你用 `mountainArr.get(index)` 和 `mountainArr.length()` 呢？

这并不是出题人故意刁难你，而是为了模拟一种极其真实的**工业级业务场景**。它会产生三个非常深远的影响：

### 1. 物理阻断：你拿不到全貌，无法直接用常规的 `for` 循环
* **平时的数组**：在写普通的 Easy 题时，数组就像一本摊开在桌子上的书，你可以随心所欲地用 `for num in nums` 或者 `nums[i]` 一眼看光所有的数字。
* **这里的山脉数组**：它被封装成了一个黑盒对象。在底层，它可能是一个非常巨大的远程文件、一个海量的数据库表、或者一个受保护的 API 接口。你根本没有把整个数组一次性加载到内存里的权限。
* **影响**：你所有的数据获取，必须且简体通过向管理员发送“函数请求”来完成 —— 问一次 `get(i)`，管理员才给你返回第 $i$ 个格子里的数字。

### 2. 算力枷锁：查询次数被锁死在 100 次以内（最关键的致命点）
* **如果用笨办法**：因为你只能用 `get()`，如果你想找一个数字，脑子一热从头到尾用 `for` 循环挨个查（即线性扫描），当数组长度有 10 万时，你得找管理员要 10 万次数据。
* **管理员的惩罚**：管理员规定：**调用 `get` 的总次数绝对不能超过 100 次**！一旦超过，系统直接判定你超时或作弊并亮红灯。
* **影响**：这个限制直接逼死了所有的暴力解法！它在逼迫我们必须使用“每次砍掉一半”的二分查找（Binary Search）。因为 $\log_2(100,000)$ 大约只需要 17 次查询，远远低于 100 次的上限。

### 3. 时间延迟：每次调用 `get` 都是一次“远程开销”
* 在真实的底层架构里，这种通过接口 `.get(i)` 获取数据的方式，往往伴随着网络传输或者内存寻址的开销。
* 频繁地调用 `get` 会比直接在本地内存数组里读变量慢得多。因此，最优的解法不仅要保证二分的次数少，还要尽量避免在循环里重复调用相同的 `get()`（这也是为什么我们在二分时，通常会把查出来的数值用一个变量存起来，而不是反复去问）。

💡 **总结成一句话**：
正因为只能用 `get` 和 `length` 并且限制了 100 次查询，才彻底封死了所有暴力遍历的路，逼着我们必须亮出“三段式二分（先找峰顶，再分左右两边二分）”这套组合拳！

---

