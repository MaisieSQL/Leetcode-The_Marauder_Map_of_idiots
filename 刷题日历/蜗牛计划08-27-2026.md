| 题号 | 题目名称 (LeetCode Title) | 门派类型 (Category / Pattern) | 难易级别 (Difficulty) | LeetCode 传送门链接 (LeetCode Link) |
| :---: | :--- | :--- | :---: | :--- |
| 145 | Binary Tree Postorder Traversal | 二叉树后序遍历 / 迭代变形流 | 🟢 简单 (Easy) | [Binary Tree Postorder Traversal](https://leetcode.com/problems/binary-tree-postorder-traversal/) |
| 417 | Pacific Atlantic Water Flow | 网格图 / 逆向反推流 (DFS/BFS) | 🟡 中等 (Medium) | [Pacific Atlantic Water Flow](https://leetcode.com/problems/pacific-atlantic-water-flow/) |
| 207 | Course Schedule | 有向图 / 拓扑排序与判环 | 🟡 中等 (Medium) | [Course Schedule](https://leetcode.com/problems/course-schedule/) |
| 127 | Word Ladder | 隐式图 / BFS 最短路径广度优先搜索 | 🔴 困难 (Hard) | [Word Ladder](https://leetcode.com/problems/word-ladder/) |

---

# 145. Binary Tree Postorder Traversal (二叉树的后序遍历)

## 题目描述
给你一棵二叉树的根节点 `root` ，返回其节点值的 **后序遍历** 序列。

- **后序遍历顺序**：`左子树 -> 右子树 -> 根节点`（Left -> Right -> Root）

## 核心解法

### 方法一：递归法 (Recursion)

#### 1. 思路分析
递归是树结构遍历最直接的方式。按照后序遍历的定义，先递归访问左子树，再递归访问右子树，最后将当前节点的值存入结果列表。

#### 2. Python 3 代码实现
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def postorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        res = []
        
        def dfs(node: Optional[TreeNode]):
            if not node:
                return
            dfs(node.left)    # 遍历左子树
            dfs(node.right)   # 遍历右子树
            res.append(node.val)  # 访问根节点
            
        dfs(root)
        return res
```

#### 3. 复杂度分析
- **时间复杂度**：$O(n)$，其中 $n$ 为二叉树的节点数，每个节点被访问一次。
- **空间复杂度**：$O(n)$，最坏情况下（树呈链状）递归栈深度为 $O(n)$；平均情况下树高为 $O(\log n)$。

### 方法二：前序遍历变形反转法 (Iterative - Reverse Preorder)

#### 1. 思路分析
- 标准前序遍历顺序为：`根 -> 左 -> 右`
- 如果我们将压栈/遍历顺序微调为：`根 -> 右 -> 左`
- 将得到的结果序列进行整体反转（Reverse），顺序变为：`左 -> 右 -> 根`，恰好对应**后序遍历**。

#### 2. Python 3 代码实现
```python
class Solution:
    def postorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        if not root:
            return []
        
        stack = [root]
        res = []
        
        while stack:
            node = stack.pop()
            res.append(node.val)
            # 先压左节点，再压右节点；弹栈时就是 右节点 先出
            if node.left:
                stack.append(node.left)
            if node.right:
                stack.append(node.right)
                
        # 将 [根 -> 右 -> 左] 反转为 [左 -> 右 -> 根]
        return res[::-1]
```

#### 3. 复杂度分析
- **时间复杂度**：$O(n)$，遍历所有节点耗时 $O(n)$，反转列表耗时 $O(n)$。
- **空间复杂度**：$O(n)$，显式栈保存节点的最大深度为 $O(n)$。

### 方法三：单栈通用迭代法（记录最近访问节点）

#### 1. 思路分析
如果不使用反转技巧，真正的后序遍历迭代需要保证：**只有当一个节点的左子树和右子树都已被访问处理后，该节点才能出栈并访问**。
- 维护指针 `curr` 沿左路一直向下压栈。
- 到达最左端后查看栈顶节点：
  - 如果栈顶节点的右子树存在且**未被访问过**，则将指针移到右子树继续处理。
  - 如果栈顶节点没有右子树，或者右子树**刚刚被访问过**（通过记录 `prev` 节点判断），则说明当前节点的子树已全部处理完毕，可以安全出栈并记录其值。

#### 2. Python 3 代码实现
```python
class Solution:
    def postorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        res = []
        stack = []
        curr = root
        prev = None  # 记录上一个被访问并记录的节点
        
        while curr or stack:
            # 一路向左压栈
            while curr:
                stack.append(curr)
                curr = curr.left
                
            node = stack[-1]
            # 如果右子树存在且未访问，转向右子树
            if node.right and node.right != prev:
                curr = node.right
            else:
                # 右子树为空或已被访问过，弹出当前节点并记录
                stack.pop()
                res.append(node.val)
                prev = node
                
        return res
```

#### 3. 复杂度分析
- **时间复杂度**：$O(n)$，每个节点最多进栈出栈两次。
- **空间复杂度**：$O(n)$，显式栈空间取决于树高。

## 方案对比

| 解法 | 优点 | 缺点 | 面试推荐度 |
| :--- | :--- | :--- | :---: |
| **递归法** | 代码极其精炼，易于书写与理解 | 树极深时存在调用栈溢出风险 | ⭐️⭐️⭐️⭐️ |
| **前序变形反转法** | 易于记忆，代码结构与前序迭代完全一致 | 并非严格的原生自底向上遍历 | ⭐️⭐️⭐️⭐️⭐️ |
| **单栈通用追踪法** | 严格按照真实后序顺序自底向上执行 | 边界与 `prev` 状态转移逻辑较多 | ⭐️⭐️⭐️⭐️ |

---
