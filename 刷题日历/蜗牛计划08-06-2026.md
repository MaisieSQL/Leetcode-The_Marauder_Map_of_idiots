| 题目名称 (Title) | 门派类型 (Category / Pattern) | 难易级别 (Difficulty) | 传送门链接 (Link) |
| :--- | :--- | :--- | :--- |
| **344. Reverse String** | 对撞双指针流 (Two Pointers) | 🟢 **Easy** | [Reverse String](https://leetcode.com/problems/reverse-string/) |
| **3. Longest Substring Without Repeating Characters** | 滑动窗口与毛毛虫伸缩流 (Sliding Window) | 🟡 **Medium** | [Longest Substring Without Repeating Characters](https://leetcode.com/problems/longest-substring-without-repeating-characters/) |
| **853. Car Fleet** | 单调栈与逆向贪心流 (Stack & Greedy) | 🟠 **Medium** | [Car Fleet](https://leetcode.com/problems/car-fleet/) |
| **71. Simplify Path** | 栈模拟流 (Stack & Simulation) | 🟡 **Medium** | [Simplify Path](https://leetcode.com/problems/simplify-path/) |

---

# 🔍 LeetCode 344. 反转字符串 (Reverse String) —— 对撞双指针流全解

## 一、 题目大意与核心约束
- **题目描述**：编写一个函数，其作用是将输入的字符串反转过来。输入字符串以字符数组 `s` 的形式给出。
- **核心约束**：
  - 必须**原地**修改输入数组（In-place）。
  - 额外空间复杂度必须限制在 **O(1)**。

## 二、 门派归类与物理直觉
- **门派归类**：**对撞双指针流 (Two Pointers: Opposing)**。
- **物理直觉**：
  - 让 `left` 指针站在数组头部（起点），`right` 指针站在数组尾部（终点）。
  - 两人同时向中间相向而行，每走一步就把 `s[left]` 和 `s[right]` 的数值进行交换（Swap）。
  - 当 `left >= right` 时，整个字符串正好翻转完毕，完美收工。

## 三、 Python3 高效通关源码

```python
class Solution(object):
    def reverseString(self, s):
        """
        :type s: List[str]
        :rtype: None Do not return anything, modify s in-place instead.
        """
        left = 0
        right = len(s) - 1
        
        while left < right:
            # 原地交换左右指针的字符
            s[left], s[right] = s[right], s[left]
            left += 1
            right -= 1
```
