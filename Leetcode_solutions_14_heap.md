参考讲解网站 https://www.hello-algo.com/chapter_heap/top_k/#833

---

# 🗺️ LeetCode 堆 (Heap / Priority Queue) 专题通关路线图

在这 219 道 Heap 相关题目中，绝大多数都是由几道**“题皇”（母题）**衍生出来的。将这些题目按照**四大核心战术类型**进行归类，并由易到难（Easy ➔ Medium ➔ Hard）分层，可以帮你最快通关这个专题。

## 👑 第一类：前 K 个最值问题（富豪俱乐部淘汰赛）

*   **核心逻辑：** 维护一个固定大小为 $K$ 的堆。求前 $K$ 大用小顶堆（淘汰掉最穷的），求前 $K$ 小用大顶堆。将时间复杂度从全局排序的 $O(N \log N)$ 完美降低到 $O(N \log K)$。

*   🟢 **Easy（打基础）：**
    *   **703.** Kth Largest Element in a Stream（数据流中的第 K 大元素）
    *   **1046.** Last Stone Weight（最后一块石头的重量 —— 体验频繁出入堆的基本功）
*   🟡 **Medium（核心母题）：**
    *   **215.** Kth Largest Element in an Array ── 👑 **【核心母题】**（你已通关 🏆）
    *   **347.** Top K Frequent Elements ── 🔥 **【必刷变形】**（以哈希表统计的“频次”作为最值入堆）
    *   **973.** K Closest Points to Origin ── 🔥 **【必刷变形】**（以计算出来的“几何距离”作为最值入堆）
    *   **692.** Top K Frequent Words（多加了字符串字典序比较的排序变形）
*   🔴 **Hard（极限挑战）：**
    *   **239.** Sliding Window Maximum ── 🌟 **【经典 Hard】**（滑动窗口最大值。虽然最优解是单调队列，但“大顶堆 + 延迟删除”是面试中非常被面试官认可的经典堆解法）

## ⛓️ 第二类：多路归并（多军会师问题）

*   **核心逻辑：** 给你多个已经排好序的序列，把它们各自的“带头大哥”（当前头元素）塞进小顶堆。堆顶是谁，谁就出列，接着从对应的序列中弹出一个“二把手”补充进堆。

*   🟢 **Easy：**
    *   *暂无*（此类型涉及多维结构或多指针，起步即为 Medium）
*   🟡 **Medium（渐入佳境）：**
    *   **378.** Kth Smallest Element in a Sorted Matrix ── 👑 **【小母题】**（矩阵每行都有序，相当于合并 $N$ 个有序数组）
    *   **373.** Find K Pairs with Smallest Sums（两个数组组合出的前 $K$ 个最小对）
*   🔴 **Hard（大厂超高频）：**
    *   **23.** Merge k Sorted Lists ── 👑 **【终极母题】**（大厂面试高频轰炸题！将 $K$ 个升序链表合并成一个）
    *   **632.** Smallest Range Covering Elements from K Lists（区间双指针与堆的结合，难度极高）

## ⚖️ 第三类：双堆流（动态平衡架构）

*   **核心逻辑：** 应对**动态动态数据流**。同时维持两个堆：左边一个大顶堆（存较小的一半数），右边一个小顶堆（存较大的一半数）。通过让两个堆的人数差不超过 1，让中位数随时在两个堆顶之间产生。

*   🟢 **Easy：**
    *   *暂无*
*   🟡 **Medium：**
    *   *暂无*（此核心考点在 LeetCode 中全部直接以 Hard 级别出现）
*   🔴 **Hard（架构设计必考）：**
    *   **295.** Find Median from Data Stream ── 👑 **【唯一母题】**（大厂系统设计类算法题最爱！数据流的中位数）
    *   **480.** Sliding Window Median（动态滑动窗口 + 中位数维护，双堆流的终极进化版）

## ⏱️ 第四类：贪心调度与“后悔药”机制

*   **核心逻辑：** 走一步看一步，每一步都做出当前看起来最好的决定（用大顶堆贪心地拿走最大收益）。如果以后发现走不动了或油不够了，就从堆里 `pop` 出以前路过的最高价值来“反悔”顶替。

*   🟢 **Easy（热身）：**
    *   **2974.** Minimum Number Game（简单的两两出堆配对游戏）
*   🟡 **Medium（核心策略）：**
    *   **621.** Task Scheduler ── 👑 **【核心母题】** (任务调度器，每次优先安排冷却时间结束且频次最高的任务)
    *   **1642.** Furthest Building You Can Reach ── 🔥 **【必刷】**（能到达的最远建筑，用堆贪心地决定什么时候用梯子、什么时候用砖头）
*   🔴 **Hard（封神关卡）：**
    *   **871.** Minimum Number of Refueling Stops ── 🔥 **【经典 Hard】**（最低加油次数，没油时从堆里掏出之前路过却没加的“最大那桶油”）
    *   **502.** IPO（最大化资本的投资项目选择，动态资金门槛限制下的贪心）

## 🎯 竖着刷！黄金通关顺序推荐：

千万不要在一个类型里死磕，建议**按列垂直突破**，每一类攻克 1~2 道核心即可切换：
1. **第一步（第一类）：** 拿下 **347** ➔ **973**（迅速把 215 题练就的“固定堆淘汰制”思路迁移过去）。
2. **第二步（第二类）：** 攻克 **378** ➔ 决战 **23**（彻底搞懂多指针与堆的配合）。
3. **第三步（第三类）：** 死磕 **295**（手写一遍双堆左右倒手的平衡逻辑，极度受用）。
4. **第四步（第四类）：** 挑战 **1642** ➔ **871**（领悟堆在贪心反悔机制中的妙用）。

---

---

# 第 8 章 堆

堆（heap）是一种满足特定条件的**完全二叉树**，主要可分为两种类型：
*   **小顶堆（min heap）：** 任意节点的值 $\le$ 其子节点的值。
*   **大顶堆（max heap）：** 任意节点的值 $\ge$ 其子节点的值。

# 🌲 堆（Heap）核心详解

堆（Heap）是一种满足特定条件的**完全二叉树**。虽然它在逻辑上是一棵树，但在物理存储上，通常使用一个高效的**扁平数组**来实现。

| 堆类型 | 核心死律（堆性质） | 堆顶元素特征 | 常见应用场景 |
| :--- | :--- | :--- | :--- |
| **小顶堆 (Min-Heap)** | 任意节点的值 $\le$ 其子节点的值<br>即：`Parent <= Child` | 全堆**最小值**<br>（位于数组首位 `arr[0]`） | 寻找第 $K$ 大的元素、数据流中位数、Dijkstra 算法优化 |
| **大顶堆 (Max-Heap)** | 任意节点的值 $\ge$ 其子节点的值<br>即：`Parent >= Child` | 全堆**最大值**<br>（位于数组首位 `arr[0]`） | 寻找第 $K$ 小的元素、堆排序（从小到大） |

### 🎨 逻辑结构对比图
```text
      【小顶堆 示例】                     【大顶堆 示例】
            3                                  20
          /   \                              /    \
         5     8                            15    12
        / \   /                            /  \   /
       9  12 14                           9   10 4
   (父亲总是小于或等于儿子)              (父亲总是大于或等于儿子)
```

## 8.1 堆的常用操作与实现

### 8.1.1 堆的常用操作效率

| 方法名 | 描述 | 时间复杂度 |
| :--- | :--- | :--- |
| `push()` | 元素入堆 | $O(\log n)$ |
| `pop()` | 堆顶元素出堆 | $O(\log n)$ |
| `peek()` | 访问堆顶元素（最大 / 最小元素） | $O(1)$ |
| `size()` | 获取堆的元素数量 | $O(1)$ |
| `isEmpty()` | 判断堆是否为空 | $O(1)$ |

> 💡 **优先队列与堆：**
> 许多编程语言提供的是优先队列（priority queue）。在实际应用中，大顶堆相当于元素按从大到小的顺序出队的优先队列。我们可以将“优先队列”和“堆”看作等价的数据结构。

#### Python 基础标准库操作示例 (`heapq`)
```python
import heapq

# 初始化小顶堆
min_heap = []

# 初始化大顶堆（技巧：将“元素取负”后再入堆，从而颠倒大小关系）
max_heap = []
flag = -1  # 1 代表小顶堆，-1 代表大顶堆

# 元素入堆
heapq.heappush(max_heap, flag * 1)
heapq.heappush(max_heap, flag * 3)
heapq.heappush(max_heap, flag * 2)

# 获取堆顶元素 peek
peek_val = flag * max_heap[0]  # 结果为 3

# 堆顶元素出堆
pop_val = flag * heapq.heappop(max_heap)  # 结果为 3

# 输入列表并原地快捷建堆
arr = [1, 3, 2, 5, 4]
heapq.heapify(arr)  # 原地转换为小顶堆
```

### 8.1.2 大顶堆的底层设计与实现

#### 1. 堆的存储与表示
由于堆是一种完全二叉树，因此非常适合采用**扁平数组**来存储。节点指针通过**索引映射公式**来实现：

对于给定索引 $i$：
* **左子节点索引：** $2i + 1$
* **右子节点索引：** $2i + 2$
* **父节点索引：** $(i - 1) // 2$ （向下整除）

```python
def left(self, i: int) -> int:
    return 2 * i + 1

def right(self, i: int) -> int:
    return 2 * i + 2

def parent(self, i: int) -> int:
    return (i - 1) // 2
```

#### 2. 访问堆顶元素
```python
def peek(self) -> int:
    """访问堆顶元素（列表首个元素）"""
    return self.max_heap[0]
```

#### 3. 元素入堆（从底至顶堆化）
将新元素添加至堆底（数组末尾）后，该元素可能破坏了大顶堆的性质。需要执行 `sift_up`（上浮）操作：比较插入节点与其父节点的值，如果插入节点更大，则将它们交换，循环向上直至根节点。

```python
def push(self, val: int):
    """元素入堆"""
    self.max_heap.append(val)  # 添加至末尾
    self.sift_up(self.size() - 1)  # 从底至顶堆化

def sift_up(self, i: int):
    """从节点 i 开始，从底至顶堆化"""
    while True:
        p = self.parent(i)
        # 当越过根节点或当前节点不大于父节点时，结束堆化
        if p < 0 or self.max_heap[i] <= self.max_heap[p]:
            break
        self.swap(i, p)  # 交换位置
        i = p  # 循环向上
```

时间复杂度： $O(\log n)$，循环轮数最多为树的高度。

#### 4. 堆顶元素出堆（从顶至底堆化）
为了尽量减少元素索引的变动，不直接删除首元素，而是：
1. 交换堆顶元素与堆底元素（交换首尾元素）。
2. 从数组中 `pop()` 弹出原堆顶元素。
3. 从根节点开始，执行 `sift_down`（下沉）操作：将当前节点的值与其左右子节点进行比较，将最大的子节点与当前节点交换，循环向下。

```python
def pop(self) -> int:
    """元素出堆"""
    if self.is_empty():
        raise IndexError("堆为空")
    # 交换首尾元素
    self.swap(0, self.size() - 1)
    # 弹出并删除原来的堆顶
    val = self.max_heap.pop()
    # 从根节点开始从顶至底堆化
    self.sift_down(0)
    return val

def sift_down(self, i: int):
    """从节点 i 开始，从顶至底堆化"""
    while True:
        l, r, ma = self.left(i), self.right(i), i
        # 寻找当前节点、左子节点、右子节点中的最大值索引
        if l < self.size() and self.max_heap[l] > self.max_heap[ma]:
            ma = l
        if r < self.size() and self.max_heap[r] > self.max_heap[ma]:
            ma = r
        # 如果最大值就是当前节点，说明已满足堆性质，跳出
        if ma == i:
            break
        self.swap(i, ma)  # 交换
        i = ma  # 循环向下
```

时间复杂度： $O(\log n)$。

### 8.2 高效建堆操作
使用一个已有列表的所有元素来构建堆，通常有两种实现策略。

#### 8.2.1 方法一：借助入堆操作自上而下构建
* **策略：** 创建一个空堆，遍历列表依次执行 `push()` 操作。
* **时间复杂度：** $O(n \log n)$。

#### 8.2.2 方法二：通过自下而上遍历堆化实现（最优解）
* **策略：**
  1. 将列表元素原封不动地放入堆数组中。
  2. 倒序遍历堆，依次对每个非叶节点执行 `sift_down`（从顶至底堆化）。
* **为什么要倒序？** 这样能够保证在堆化当前节点时，它下方的子树已经是合法的子堆。叶节点天然是合法子堆，无须堆化。最后一个非叶节点即为最后一个节点的父节点。

```python
def __init__(self, nums: list[int]):
    """根据输入列表高效建堆"""
    self.max_heap = nums
    # 从最后一个非叶节点开始，倒序遍历到根节点执行下沉
    for i in range(self.parent(self.size() - 1), -1, -1):
        self.sift_down(i)
```

### 8.2.3 复杂度分析

下面，我们来尝试推算第二种建堆方法的时间复杂度。

假设完全二叉树的节点数量为 $n$，则叶节点数量为 $\lfloor (n + 1) / 2 \rfloor$，其中 $\lfloor \dots \rfloor$ 为向下整除。因此需要堆化的节点数量为 $\lfloor n / 2 \rfloor$。
在从顶至底堆化的过程中，每个节点最多堆化到叶节点，因此最大迭代次数为二叉树高度 $h$。
将上述两者相乘，可得到建堆过程的时间复杂度为 $O(n \log n)$。但这个估算结果并不准确，因为我们没有考虑到二叉树底层节点数量远多于顶层节点的性质。

接下来我们来进行更为准确的计算。为了降低计算难度，假设给定一个节点数量为 $n$、高度为 $h$ 的“完美二叉树”，该假设不会影响计算结果的正确性。

节点“从顶至底堆化”的最大迭代次数等于该节点到叶节点的距离，而该距离正是该节点的“高度”。因此，我们可以对各层的“节点数量 $\times$ 节点高度”求和，得到所有节点的堆化迭代次数的总和 $T(h)$：

$$T(h) = 2^0 \cdot h + 2^1 \cdot (h-1) + 2^2 \cdot (h-2) + \dots + 2^{h-1} \cdot 1$$

化简上式需要借助数列知识，先将 $T(h)$ 乘以 $2$，得到：

$$2T(h) = 2^1 \cdot h + 2^2 \cdot (h-1) + 2^3 \cdot (h-2) + \dots + 2^h \cdot 1$$

使用错位相减法，用下式 $2T(h)$ 减去上式 $T(h)$，可得：

$$T(h) = -h + 2^1 + 2^2 + 2^3 + \dots + 2^h$$

观察上式，发现 $2^1 + 2^2 + \dots + 2^h$ 是一个等比数列，可直接使用求和公式，得到：

$$T(h) = 2^{h+1} - h - 2$$

进一步，高度为 $h$ 的完美二叉树的节点数量为 $n = 2^{h+1} - 1$，将 $n$ 代入上式易得：

$$T(h) = n - \log_2(n + 1)$$

以上推算表明，输入列表并建堆的时间复杂度为 $O(n)$，非常高效。

---

# 📂 LeetCode 215. 数组中的第 K 个最大元素 (Kth Largest Element in an Array) ── 堆排序与富豪俱乐部淘汰赛

## 📖 1. 题目描述
给定一个整数数组 `nums` 和一个整数 `k`，请返回数组中第 `k` 个最大的元素。

请注意，你需要找的是数组排序后的第 `k` 个最大元素，而不是第 `k` 个不同的元素。

**你能否在不使用标准排序（即打破 $O(N \log N)$ 复杂度限制）的情况下解决这个问题？**

### 示例 1：
*   **输入:** `nums = [3, 2, 1, 5, 6, 4]`, `k = 2`
*   **输出:** `5`
*   **解释:** 排序后的数组为 `[1, 2, 3, 4, 5, 6]`，第二大的元素是 `5`。

### 示例 2：
*   **输入:** `nums = [3, 2, 3, 1, 2, 4, 5, 5, 6]`, `k = 4`
*   **输出:** `4`

### 提示：
*   $1 \le k \le nums.length \le 10^5$
*   $-10^4 \le nums[i] \le 10^4$

## 💡 2. 破局关键：小顶堆的“富豪俱乐部”机制

如果使用常规的全局排序，时间复杂度是 $O(N \log N)$。为了优化到 $O(N \log K)$，我们可以使用**小顶堆（Min-Heap）**。

### 🧱 核心思维模型：
我们可以把这个小顶堆想象成一个**“只允许容纳 $K$ 个人的顶尖富豪俱乐部”**：
1. **小顶堆的铁律：** 每一个父亲节点都小于等于儿子节点。这意味着，**坐在俱乐部大门口（根节点 `heap[0]`）的，永远是这个俱乐部里“最穷（数值最小）”的那个富豪**。
2. **建队门槛（前 $K$ 个数）：** 先不管三七二十一，把前 `k` 个数强行 `push` 进堆，让俱乐部坐满。
3. **大浪淘沙（剩余数字）：** 从第 `k` 个数开始遍历，每一个新数字想进来，先和大门口最穷的那个富豪（`heap[0]`）比一比：
    * 如果新数字**小于或等于** `heap[0]`，说明它连俱乐部里最穷的人都比不过，根本没资格成为前 $K$ 大，**直接跳过（剪枝优化）**。
    * 如果新数字**大于** `heap[0]`，说明大门口那个人该退位了。我们无情地将堆顶 `pop` 弹出，然后把新数字 `push` 进去，让堆自己去通过“上浮/下沉”重新调整组织架构。
4. **终极清算：** 遍历完所有数字后，留在俱乐部里的 `k` 个人一定是全场底子最雄厚的 $K$ 个大富豪。而坐在大门口 `heap[0]` 的那个“大富豪里最穷的人”，正好就是全场**第 $K$ 大**的元素！

## 💻 3. 核心代码实现 (Python3 & Java)

### Python3 实现（你的高阶优化版本）
这个版本先用 `if` 过滤了没资格进堆的数字，避免了频繁触发树结构调整，运行效率极高。

```python
import heapq

class Solution(object):
    def findKthLargest(self, nums: list[int], k: int) -> int:
        # 1. 声明我们的微型小顶堆账本
        myheap = []
        
        # 2. 先把前 k 个数强行塞满堆，建立初始的俱乐部成员圈
        for i in range(k):
            heapq.heappush(myheap, nums[i])
            
        # 3. 遍历接下来的所有人，进行严格的门槛检查
        for j in range(k, len(nums)):
            # 只有比大门口最穷的那个（myheap[0]）还大，才允许踢人进入
            if nums[j] > myheap[0]:
                heapq.heappop(myheap)       # 淘汰当前最小的
                heapq.heappush(myheap, nums[j]) # 迎入新大哥
                
        # 4. 所有人走完这一遭后，堆顶 myheap[0] 就是第 K 大的巨头，直接返回
        return myheap[0]
```

---

# 📂 LeetCode 1046. 最后一块石头的重量 (Last Stone Weight) ── 大顶堆与碰碰车淘汰赛

## 📖 1. 题目描述
有一堆石头，每块石头的重量都是正整数。

每一回合，从中选出**两块最重的**石头，然后将它们一起粉碎。假设两块石头的重量分别为 `x` 和 `y`，且 `x <= y`。那么粉碎的可能结果如下：
*   如果 `x == y`，那么两块石头都会被完全粉碎；
*   如果 `x != y`，那么重量为 `x` 的石头将会完全粉碎，而重量为 `y` 的石头新重量为 `y - x`。

最后，**最多只会剩下一块**石头。返回此石头的重量。如果没有石头剩下，返回 `0`。

### 示例 1：
*   **输入:** `stones = [2, 7, 4, 1, 8, 1]`
*   **输出:** `1`
*   **解释:** 
    1. 先选出最重的两块 `7` 和 `8` 相撞，得到 `8 - 7 = 1`，石堆变成 `[2, 4, 1, 1, 1]`。
    2. 再选出 `2` 和 `4` 相撞，得到 `4 - 2 = 2`，石堆变成 `[2, 1, 1, 1]`。
    3. 再选出 `2` 和 `1` 相撞，得到 `2 - 1 = 1`，石堆变成 `[1, 1, 1]`。
    4. 再选出 `1` 和 `1` 相撞，两块都碎掉，石堆变成 `[1]`。
    5. 最后剩下 `1`，返回 `1`。

### 示例 2：
*   **输入:** `stones = [1]`
*   **输出:** `1`

## 💡 2. 破局关键：大顶堆的“动态最值提取”

这道题的核心操作是：**每次都要找出并取出当前石堆中最大的两个数**。

如果使用常规数组，每次取出最大值后再插入差值，都需要重新排序，时间复杂度会飙升到 $O(N^2 \log N)$。为了高效地在动态变化的数组中维护最值，**大顶堆（Max-Heap）** 是最完美的数据结构。

### 🚨 Python 的特殊设定：取负法
Python 标准库 `heapq` **只提供小顶堆**。为了实现大顶堆，我们需要施展一个降维打击的魔法：
> 将所有石头的重量**乘以 `-1`** 再存入堆中。
> 例如，原本最大的数 `8` 会变成最小的数 `-8` 呆在堆顶。我们弹出来使用时，再**乘以 `-1`** 还原回来即可。

## 💻 3. 核心代码实现 (Python3 & Java)

### Python3 实现（使用 `heapq` 库）
```python
import heapq

class Solution(object):
    def lastStoneWeight(self, stones: list[int]) -> int:
        # 1. 施展大顶堆魔法：将所有石头重量取负数
        # 这样原本最大的值取负后变成了最小值，会自然浮动到堆顶
        max_heap = [-s for s in stones]
        
        # 2. 原地快速建堆，时间复杂度为 O(N)
        heapq.heapify(max_heap)
        
        # 3. 只要堆里还剩下至少 2 块石头，就继续相撞
        while len(max_heap) > 1:
            # 弹出当前最重的两块石头（取负还原）
            stone1 = -heapq.heappop(max_heap)
            stone2 = -heapq.heappop(max_heap)
            
            # 如果两块石头不一样重，把碎渣（差值）取负后扔回堆里
            if stone1 != stone2:
                heapq.heappush(max_heap, -(stone1 - stone2))
                
        # 4. 如果堆空了说明全碎了，返回 0；如果还剩一个，还原符号返回它
        return -max_heap[0] if max_heap else 0
```

⏱️ 4. 复杂度分析Time (时间复杂度)：$O(N \log N)$建堆阶段： 耗时 $O(N)$。模拟碰撞阶段： while 循环最多执行 $N - 1$ 次。在每次循环中，我们最多执行 $2$ 次弹出操作（heappop）和 $1$ 次插入操作（heappush）。每次堆操作的时间复杂度均为 $O(\log N)$。因此总时间复杂度为 $O(N \log N)$。Space (空间复杂度)：$O(N)$我们需要开辟一个大小为 $N$ 的堆空间来存储这些石头的负值重量。

---

# 📂 LeetCode 703. 数据流中的第 K 大元素 (Kth Largest Element in a Stream) ── 小顶堆与排行榜机制

## 📖 1. 题目描述

设计一个类 `KthLargest`，用于找到数据流中第 `k` 大的元素。注意，它是**排序后**的第 `k` 大元素，而不是第 `k` 个不同的元素。

你必须实现以下方法：
*   `KthLargest(int k, int[] nums)`：使用整数 `k` 和整数流 `nums` 初始化对象。
*   `int add(int val)`：将当前数据流中的新元素 `val` 插入，并返回当前数据流中第 `k` 大的元素。

### 示例：
*   **输入：**
    `["KthLargest", "add", "add", "add", "add", "add"]`
    `[[3, [4, 5, 8, 2]], [3], [5], [10], [9], [4]]`
*   **输出：**
    `[null, 4, 5, 5, 8, 8]`
*   **模拟过程解释：**
    ```python
    kthLargest = KthLargest(3, [4, 5, 8, 2]) # 初始保留最大的 3 个：[4, 5, 8]
    kthLargest.add(3)   # 3 <= 4（当前第3大），不入榜，返回 4
    kthLargest.add(5)   # 5 > 4，剔除 4，加入 5，榜单 [5, 5, 8]，返回 5
    kthLargest.add(10)  # 10 > 5，剔除 5，加入 10，榜单 [5, 8, 10]，返回 5
    kthLargest.add(9)   # 9 > 5，剔除 5，加入 9，榜单 [8, 9, 10]，返回 8
    kthLargest.add(4)   # 4 <= 8，不入榜，返回 8
    ```

## 💡 2. 破局关键：利用小顶堆做“守门员”

如果每次添加新数据都对整个数组进行排序，时间复杂度会达到 $O(N \log N)$，这在持续输入的数据流中效率极低。

### 🧱 核心思维：
我们只需要知道前 $k$ 个最大的数，以及它们之中最小的那个（即第 $k$ 大）。
1.  **限制堆的大小为 $k$：** 我们用一个**小顶堆（Min-Heap）**来充当这个容量为 $k$ 的“高手榜单”。
2.  **小顶堆的性质：** 堆顶元素（`heap[0]`）永远是这 $k$ 个数里**最小**的一个。换句话说，堆顶就是这个榜单的**“守门员”**。
3.  **动态淘汰逻辑：**
    *   如果堆的长度小于 $k$，新数字直接入堆。
    *   如果堆的长度已满 $k$：
        *   新加入的数比堆顶（守门员）还小，说明它绝对无法进入前 $k$ 名，直接丢弃。
        *   新加入的数比堆顶大，说明它有资格上榜。我们把堆顶踢出（`heappop`），再把这个新数塞入（`heappush`）。
    *   在任何时候，**堆顶的元素就是我们要求的第 $k$ 大元素**。

## 💻 3. 核心代码实现 (Python3 & Java)

### Python3 实现（使用 `heapq` 库）
```python
import heapq

class KthLargest(object):

    def __init__(self, k: int, nums: list[int]):
        """
        初始化对象，并维护一个大小最多为 k 的小顶堆
        """
        self.k = k
        self.min_heap = []
        
        # 将初始列表中的元素依次加入堆中
        for num in nums:
            self.add(num)

    def add(self, val: int) -> int:
        """
        将新元素 val 插入，并返回当前第 k 大的元素
        """
        # 1. 如果堆的大小还没到 k，直接入堆
        if len(self.min_heap) < self.k:
            heapq.heappush(self.min_heap, val)
        # 2. 如果堆已满，且新数大于堆顶元素（守门员），则踢出堆顶，放入新数
        elif val > self.min_heap[0]:
            heapq.heappop(self.min_heap)
            heapq.heappush(self.min_heap, val)
            
        # 3. 堆顶永远是这 k 个数中最小的，也就是数据流中第 k 大的元素
        return self.min_heap[0]
```
