
# 🪓 深入骨髓 ── 贪心算法（Greedy Algorithm）究竟是个什么魔术？

别把“贪心”当成什么复杂的数学逻辑，在物理上，它就是**【目光短浅】**和**【占便宜没够】**的代名词。

## 🏛️ 1. 两个大白话核心

* **核心直觉 1：只顾眼前，绝不看未来 👁️**
  贪心算法在执行的时候，有一个钢铁硬性渣男思维：*“我不管明天会发生什么，我只看此时此刻这一秒，哪条路最能让我占到最大的便宜，我就直接扑过去！”*
* **核心直觉 2：永远追求单步的最优 🪙**
  它做决策是一步一步来的。每走一步，它都把当下的利益强行榨干。它相信一个信念：*只要我每一步都赚到了最多的钱，最后总账本加起来，我一定就是全场最富有的人！*

---

## 🎬 2. 两个例子，瞬间开窍

### 🌟 例子 A：去银行换零钱（贪心完全正确 ── 顺理成章）
假设你是银行柜员，现在要给客户找 **36 块钱零钱**。你手里有 `20元、10元、5元、1元` 的纸币。为了让给出去的纸币张数最少，你会怎么做？

1. **第一步（占最大便宜）**：36 元里，能塞下的最大面值是谁？是 **20 元**。无脑拿一张 20 元（剩下 16 元）。
2. **第二步（继续占便宜）**：16 元里，能塞下的最大面值是谁？是 **10 元**。无脑拿一张 10 元（剩下 6 元）。
3. **第三步（继续占便宜）**：6 元里，能塞下的最大面值是谁？是 **5 元**。无脑拿一张 5 元（剩下 1 元）。
4. **第四步**：拿一张 **1 元**。



你最后给出了：`20 + 10 + 5 + 1` 共 **4 张** 纸币。这在现实中绝对是最少张数的正确答案。
这就是贪心算法！你刚才做决策的时候，根本没有去算什么长远的组合，你纯粹是**在每一步里，都挑了当下能选的、最大的那张纸币**！

---

### 💥 例子 B：穿越迷宫抓金币（贪心彻底翻车 ── 看到天生缺陷）
但是，只看眼前的渣男思维，有时候会出惊天大丑。
假设你现在在金字塔迷宫的顶端（起点），你每次只能选择往左下走或者往右下走，目标是让一路上捡到的金币总数最多。

```text
       【 起点 】
         /   \
       [3]   [5]  ➔ 贪心一看：5 比 3 大！无脑往右边扑过去！
       /       \
    [0]         [1]
```

如果迷宫长成上面这样，贪心先看第一层分叉：5 > 3，它连一秒钟都不会犹豫，直接往右边冲，抓到了 5。然后再往下走，抓到了 1。总共抓到了 5 + 1 = 6 个金币。

但是！如果这个迷宫的左边其实长这样呢

```text
         【 起点 】
           /   \
         [3]   [5]
         /       \
     [1000]       [1]
```

如果是正常人有大局观，一定会选择走左边！虽然第一步拿 `3` 亏了，但第二步能拿到 **`1000`** 啊！

但贪心算法说：*“对不起，1000 在太远的未来了，我这个瞎子根本看不见！我就看眼前的这一步，5 就是比 3 大，我必须选 5！”*

## 🎯 3. 顿悟：我们在 LeetCode 里怎么写贪心代码？

看了上面两个例子，你现在应该彻底通透了。

贪心算法**不是一种固定的代码格式**（它不像滑动窗口必须有 `left` 和 `right`），它是一种**【工程省算力的解题策略】**。

只有当题目有一种特殊的物理属性 ── **“只要每一步都占到当下最大的便宜，最后的总账就一定是全局最优”** 时，我们才能用贪心。

我们在写 LeetCode 贪心题时，代码其实就是在写一件事：
> **“怎么在数组里拉起一根卡尺，去实时计算并抓取当下的最大利益。”**

* **比如跳跃游戏（55题）**：你每走到一格，贪心不去看以后怎么跳，只用这一格的能量去疯狂刷新“最远能走到哪里”的疆土范围。
* **比如买卖股票（122题）**：你不需要知道哪天是历史最高、最低点。只要看到明天的价比今天高，你今天买明天卖，原地把这一天的微小利差白嫖到手。全场所有的小便宜加起来，就是宏观的最高利润！

---

# 🛡️ LeetCode 121. Best Time to Buy and Sell Stock (买卖股票的最佳时机)

## 📝 1. 题目描述 (Description)

给定一个数组 `prices` ，它的第 `i` 个元素 `prices[i]` 表示一支给定股票第 `i` 天的价格。

你只能选择 **某一天** 买入这只股票，并选择在 **未来的某一个不同的日子** 卖出该股票。设计一个算法来计算你所能获取的最大利润。返回你可以从这笔交易中获取的最大利润。如果你不能获取任何利润，返回 `0` 。

### 示例 1:
> **输入**: prices = [7, 1, 5, 3, 6, 4]
> **输出**: 5
> **解释**: 在第 2 天（股票价格 = 1）的时候买入，在第 5 天（股票价格 = 6）的时候卖出，最大利润 = 6 - 1 = 5 。🚨 注意，利润不能是 7 - 1 = 6，因为你不能在第 2 天买入的前一天（第 1 天价格 7）去卖出。

---

## 🪓 2. 核心算法解析 ── 滚动进货价卡尺 + 历史最高利润动态刷新流

这道题如果用两层 `for` 循环去穷举“任意一天买、任意一天卖”，那是 $\mathcal{O}(N^2)$ 的暴力流，大数据量下必当场超时枪毙！

### 🚀 贪心投机客直觉（占便宜没够）：
* 宏观上看，我们想在历史绝对最低点买，最高点卖。但在往前行军（遍历天数）时，我们是瞎子，看不到未来的最高点在哪里。
* 贪心破局：**“没关系！我只管随身带一个进货价账本，记录一路上我见过的最便宜的底价。然后一路上假设我是在那一天进的货，看看今天按市场价抛售能占到多大便宜！”**

### 🏃‍♂️ 物理行军控盘两步走：
1. **初始化两尊占便宜记账本**：
   * `min_price`：记录**截止到今天为止，历史最低的进货价**（先设为正无穷大 `float('inf')` 托底）。
   * `max_profit`：记录**截止到今天为止，历史发生过的最高利润战报**。🚨 根据题目保底要求，如果亏钱可以不买不卖，所以**必须初始化为 `0`**，防止在股票一路下跌时返回负数或负无穷大。
2. **单层 `for` 循环单向平推（目光短浅，绝不回头）**：
   * **占进货便宜**：如果今天的价格比我之前记下的最低进货价还要便宜（`price < min_price`），大便宜！赶紧掏出小本本，把进货价刷新成今天的超低价！
   * **占出货便宜**：如果今天价格挺高，我们就顺理成章清算：如果我是在最低进货价 `min_price` 进的货，今天卖掉能赚 `price - min_price`。如果这个利差比历史记录 `max_profit` 还要高，立刻原地覆盖刷新最高战报！

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        # 贪心算法，找小本本记下来最低价和最高利润
        
        # corner case 防御
        if not prices:
            return 0
            
        # 1. 贪心标准格式：摆开我们的两尊“占便宜实时对账单”
        min_price = float('inf')  # 历史最低进货底价
        max_profit = 0            # 历史最高收割利润记录（保底为 0）
        
        # 2. 纯净单层 for 循环，一天一天往前走，绝不回头
        for price in prices:
            # 贪心微操 A：占进货便宜！只要今天的价格创造历史新低，立刻重置进货底线
            if price < min_price:
                min_price = price
            # 贪心微操 B：占出货便宜！今天卖要是能打破历史最高利润战报，原地覆盖刷新最高值
            elif price - min_price > max_profit:
                max_profit = price - min_price
                
        # 3. 循环结束，交出终极利润战报
        return max_profit
```

---

# 🛡️ LeetCode 455. Assign Cookies (分发饼干)

## 📝 1. 题目大白话翻译：幼儿园发零食风波

想象一下你现在是幼儿园的主班老师，手里有一袋大小不一的饼干，面前站着一排嗷嗷待哺的小朋友。

* **需求方：小朋友的胃口数组 `g`（Greed）**
  每个小朋友因为胖瘦不同，吃饱的门槛不一样。
  比如 `g = [1, 2, 3]`：
  - 第 1 个小朋友胃口是 `1`，你必须给他大小 $\ge 1$ 的饼干，他才能吃饱。
  - 第 2 个小朋友胃口是 `2`，你必须给他大小 $\ge 2$ 的饼干，他才能吃饱。
  - 第 3 个小朋友是大胃王，胃口是 `3`，你必须给他大小 $\ge 3$ 的饼干。

* **物资方：你手里的饼干数组 `s`（Size）**
  你口袋里抠抠搜搜摸出了几块饼干，它们的大小也是有编号的。
  比如 `s = [1, 1]`：代表你手里只有两块饼干，它们的尺寸都只有 `1`。

* **🚨 核心冲突法则**：
  - **每个人最多只能拿一块饼干。** （不能把两块 1 凑成 2 给第 2 个小朋友吃，规则锁死！）
  - 如果你给小朋友的饼干**小于**他的胃口，他就吃不饱，判定失败。
  - **你的 KPI 目标**：用手里这袋饼干，让**尽可能多（数量最多）**的小朋友吃饱打卡下班。

---

## 🎬 2. 模拟现场：为什么示例1的答案是 1？

我们把上面这个例子拿到现场去发一发：
* 小朋友们：`[胃口1, 胃口2, 胃口3]`
* 你的零食包：`[尺寸1, 尺寸1]`

1. **第一回合**：你拿出一块 **尺寸1** 的饼干。
   - 喂给胃口 1 的小朋友：`1 >= 1`，**完美！** 第 1 个小朋友吃饱了。
2. **第二回合**：你拿出第二块 **尺寸1** 的饼干。
   - 喂给胃口 2 的小朋友：`1 < 2`，太小了，喂不饱！
   - 喂给胃口 3 的小朋友：`1 < 3`，也喂不饱！
3. **清盘结算**：你手里的饼干已经发光了。全场数一数，只有 **1 个** 小朋友（胃口1的那个）成功吃饱了。
4. 所以，代码最终要交上去的战报数据就是：**`1`**。

---

## 🪓 3. 贪心算法在代码里怎么分饼干？

读懂了题目，那我们怎么用你刚刚顿悟的**【占便宜没够】的贪心格式**去控盘呢？
既然你要追求“吃饱的人数最多”，那你作为资源分配官，必须极度精明。

如果你手里的饼干是 `[1, 10]`，小朋友是 `[1, 2]`。
你要是手贱，把 **尺寸10** 的巨无霸饼干喂给了 **胃口1** 的小朋友。那接下来面对 **胃口2** 的小朋友，你手里只剩下 **尺寸1** 的碎渣了，直接两败俱伤！



所以，最抠门的贪心发法是：
1. **大家都去排队（`sort()`）**：小朋友按胃口从小到大排好，饼干也按尺寸从小到大排好。
2. **拉起两根记账卡尺**：`child = 0`（死盯着当前最容易满足的小朋友），`cookie = 0`（死盯着当前最小的饼干）。
3. **单向流水线对线**：
   - 只要当前饼干能喂饱当前小朋友，太棒了！小便宜占到了！`child += 1`（代表这个人吃饱了，卡尺移向下一个胃口稍大的人）。
   - 不管饼干有没有喂饱人，只要看过了，饼干卡尺就无情往后挪（`cookie += 1`）。

---

## 💻 4. Python3 完美通关代码

```python
class Solution:
    def findContentChildren(self, g: List[int], s: List[int]) -> int:
        # 1. 贪心前置手艺：排兵布阵，强行整编排队（从小到大升序排序）
        g.sort()
        s.sort()
        
        child = 0   # 贪心卡尺：记录当前正在对线的小朋友（同时代表吃饱的总人数）
        cookie = 0  # 饼干卡尺：单向流水线大扫描
        
        # 2. 贪心标准格式：单向全场扫描，绝不回头看
        while child < len(g) and cookie < len(s):
            # 占便宜判定：当下的最小饼干刚好能满足当下最容易满足的小朋友
            if s[cookie] >= g[child]:
                # 局部最优收割成功！这个小朋友吃饱出局，换下一个稍大胃口的人
                child += 1
            
            # 不管这块饼干有没有被吃掉，它都已经接受过安检了，无脑看下一块
            cookie += 1
            
        # 3. 终极账本：child 的步数就是全场占到的小便宜总数
        return child
```

# 📊 LeetCode 121 题 vs 122 题的本质区别

| 维度 | LeetCode 121 （股票 I 代） | LeetCode 122 （股票 II 代） |
| :--- | :--- | :--- |
| **交易次数** | **全场只能买一次，卖一次！** 🎬 | **可以买卖无数次（无限次交易）！** 🔂 |
| **相同约束** | 任何时候手里**最多只能持有一股**。买入后必须先卖出，才能进行下一次买入。 | 任何时候手里**最多只能持有一股**。买入后必须先卖出，才能进行下一次买入。 |
| **贪心物理画面** | **【寻找历史绝对大便宜】** 🪙<br><br>随身带个小本本，记住一路走来见过的**历史最低进货价**，每天清算“如果今天卖能占多大便宜”，刷新历史最高利润。 | **【微观短线疯狂白嫖】** 📈<br><br>砸碎所有宏观长线眼光。只要**明天的价格比今天高，今天买明天卖，原地把这一天的微小利差吃掉**！ |

---

# 🛡️ LeetCode 122. Best Time to Buy and Sell Stock II (买卖股票的最佳时机 II)

## 📝 1. 题目描述 (Description)

给你一个整数数组 `prices` ，其中 `prices[i]` 表示某支股票第 `i` 天的价格。

在每一天，你可以决定是否购买和/或出售股票。你在任何时候 **最多** 只能持有一股股票。你也可以在同一天购买席位然后再出售。返回你能获得的 **最大** 利润。

### 示例 1:
> **输入**: prices = [7, 1, 5, 3, 6, 4]
> **输入**: 7
> **解释**: 
> - 第 2 天（价格 = 1）买入，第 3 天（价格 = 5）卖出, 利润 = 5 - 1 = 4 。
> - 第 4 天（价格 = 3）买入，第 5 天（价格 = 6）卖出, 利润 = 6 - 3 = 3 。
> - 总利润 = 4 + 3 = 7 。

---

## 🪓 2. 核心算法解析 ── 极限微观短线白嫖：吃干抹净所有上升斜坡

这道题和 121 题（只能买卖一次）最大的魔改点在于：**你可以买卖无数次！** 

### 🚀 贪心短线投机客直觉（占便宜没够）：
* 普通人一上来，又开始精分地去预测长线的历史大高点和大低点。
* 但作为拥有极致贪心思维的你，直接把宏观长线大局观砸个粉碎！我们只玩**【极限微观短线操作】**！
* **物理画面**：股票走势无非就是一条高低起伏的山坡。既然可以无限次买卖，**只要明天的价格比今天高，我们就在今天买、明天卖，原地白嫖这段微小的利差（占到眼前的单步小便宜）！**
* 如果它连续 3 天上涨（比如 `1 ➔ 3 ➔ 5`），我们在第 1 天买第 2 天卖赚 `2`，第 2 天再买第 3 天卖赚 `2`，总共赚 `4`。这和你在第 1 天买第 3 天卖赚的 `5 - 1 = 4` 在代数上**完美等价**！
* 如果股票下跌，傻子才操作，我们直接装死。

> 💡 **股票 II 代贪心口诀**：**“不要算长线，只要看明天！明天比今高，今天买明抛；所有小斜坡，利润全刮走！”**

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        # 贪心短线流：只管眼前两天的利差，占到便宜就走人
        max_profit = 0
        
        # 从第 2 天（下标 1）开始，每天和前一天对线
        for i in range(1, len(prices)):
            # 只要今天的价格比昨天高，说明是个单调上升的小斜坡
            if prices[i] > prices[i - 1]:
                # 原地白嫖这段微观利差，化整为零，累加进总账本
                max_profit += prices[i] - prices[i - 1]
                
        return max_profit
```

---

# 🛡️ LeetCode 55. Jump Game (跳跃游戏)

## 📝 1. 题目描述 (Description)

给你一个非负整数数组 `nums` ，你最初位于数组的 **第一个下标** 。数组中的每个元素代表你在该位置可以跳跃的 **最大** 长度。

判断你是否能够到达最后一个下标，如果可以，返回 `true` ；否则，返回 `false` 。

### 示例 1:
> **输入**: nums = [2, 3, 1, 1, 4]
> **输出**: true
> **解释**: 可以先跳 1 步，从下标 0 到达下标 1, 然后再从下标 1 跳 3 步到达最后一个下标。

### 示例 2:
> **输入**: nums = [3, 2, 1, 0, 4]
> **输出**: false
> **解释**: 无论怎样，你总会到达索引为 3 的位置。但该位置的最大跳跃长度是 0 ，所以你永远不可能到达最后一个下标。

## 🔬 2. 避坑指南：为什么“触发跳格子模拟”会当场翻车？

刚学算法的同学，本能地容易掉进**“人肉模拟陷阱”**中。脑子里浮现的画面通常是：“我踩在 2 上，我就去跳 2 步；踩在 3 上，我就去跳 3 步，像玩跳格子触发游戏一样，一步一步走到底。” 

**🚨 这不叫贪心，这叫“复刻案发现场”的暴力模拟！而在本题中，这条路会让你彻底绝望。**

### 💥 痛点剖析：
题目里写的是：“每个元素代表你在该位置可以跳跃的 **最大** 长度”。
意思是，如果这一格写的数字是 `3`，你可以选择跳 `1 步`、`2 步`、或者 `3 步`！它是一个**多选题**，不是强制让你跳 3 步。

拿恶毒用例 `nums = [2, 4, 0, 0, 0]` 来说：
* **如果按“固定跳满”的人肉模拟**：你站在第 0 格（数值 2），直接跳满 2 步踩到第 2 格（数值 0）。完蛋，当场被晃点死，动弹不得，返回 `False`。
* **但正确答案其实是 `True`**：你在第 0 格时**只跳 1 步**，先踩到第 1 格（数值 4），再使出全力跳 4 步，直接大跨步起飞直达终点！

#### 💀 模拟流的绝望结局：
为了找出正确答案，你必须在每一格都去穷举：“我今天是跳 1 步呢？还是跳 2 步呢？还是跳 3 步呢？” 这会分叉出无数的平行宇宙（递归分支），时间复杂度直接暴涨成 **$\mathcal{O}(2^N)$ 恐怖指数级**，服务器会当场烧断线！

## 🪓 3. 顿悟：贪心策略如何实施“圈地盘”降维打击？

贪心算法直接砸碎了这种多维分叉迷宫：**“我根本不关心你一路上到底是怎么跳的、跳了几步！我只管拉起一根卡尺，强行记录『我们当前能占到的最大疆土极限』！”**

这就是贪心的**占便宜思维**：彻底抛弃过程，只锚定“结果”。我们开着重装战车，全场一格一格往前推。每走到一格，我们就把这格的能量全部榨干，看看它最大能把我的势力范围推到多远。

### 🏃‍♂️ 55 题贪心特种兵行军实录 ── 指针与卡尺的降维对线

假设战场是：`nums = [2, 3, 1, 1, 4]`
我们手心里只攥着一根记账卡尺：`max_reach = 0`（我们打下的最远可达疆土边界）。行军指针 `i` 一格一格往前推进：

* **🏁 初始状态**：`max_reach = 0`
* **🚶 走到第 0 格（数值 2）**：`i = 0`
  - **安检线判定**：目前 `i <= max_reach`（`0 <= 0`），安全，可以通过！
  - **贪心占便宜**：我们踩在第 0 格上，最远可以往后轰出 `0 + 2 = 2` 这么远的距离。
  - **覆盖刷新**：跟历史记录对线，`max_reach = max(0, 0 + 2) = 2`。
  - *当前战报*：我们的安全管辖区成功扩大到了第 2 格（`[0, 1, 2]` 安全）。
* **🚶 走到第 1 格（数值 3）**：`i = 1`
  - **安检线判定**：`1 <= max_reach`（`1 <= 2`），1 踩在我们的安全区里，安全放行！
  - **贪心占便宜**：卧槽，这一格能量更猛！踩在第 1 格上，最远可以往后轰出 `1 + 3 = 4` 这么远！
  - **覆盖刷新**：赶紧占这个大便宜，`max_reach = max(2, 1 + 3) = 4`。
  - *当前战报*：最远疆土暴涨到第 4 格！
* **🎯 终极清算，提前下班**：
  - 这时候贪心特种兵突然看了一眼总账本：终点下标不就是 `4`（`len(nums) - 1`）吗？
  - 我们的最远卡尺 `max_reach` 此时已经铁证如山地踩在 4 上面了！
  - 贪心大笑：“后面的格子老子不走了！反正我的最高战力范围已经覆盖了终点，闭着眼睛都能过去！”
  - 轰然直接提前返回 **`True`**！

### 💥 负面反例：贪心是怎么抓出“过不去”的内鬼的？

如果路断了，贪心怎么抓人？再看这个绝望用例：`nums = [3, 2, 1, 0, 4]`

1. `i = 0` (数值 3) ➔ `max_reach = max(0, 0 + 3) = 3`。安全区：`[0, 1, 2, 3]`。
2. `i = 1` (数值 2) ➔ `max_reach = max(3, 1 + 2) = 3`。
3. `i = 2` (数值 1) ➔ `max_reach = max(3, 2 + 1) = 3`。
4. `i = 3` (数值 0) ➔ `max_reach = max(3, 3 + 0) = 3`。
5. **🚶 跨进第 4 格（数值 4）**：`i = 4`
   - **安检线判定**：`i > max_reach`（`4 > 3`）！
   - 报警器瞬间拉响！这说明你的行军指针走得太快，**已经踩到了你的最远安全管辖区（第 3 格）外面去了**！
   - 贪心毫不含糊，当场熔断：“有内鬼，前面能量断层了，根本跳不过来！返回 **`False`**！”

## 💻 4. Python3 完美通关代码

```python
class Solution:
    def canJump(self, nums: List[int]) -> bool:
        max_reach = 0  # 贪心卡尺：记录当前大军能触碰到的最远大后方疆土
        
        # 一步一步往前平推扫描
        for i, jump in enumerate(nums):
            # 🚨 核心熔断线：如果当前位置连最远卡尺都够不着，说明外面的能量无法支撑到这一格
            if i > max_reach:
                return False
                
            # 贪心策略：压榨当下格子的所有潜力，强行刷新全场最远可达极限
            max_reach = max(max_reach, i + jump)
            
            # 提前下班优化：如果最远疆土已经踩到了终点，不用往后看，直接通关
            if max_reach >= len(nums) - 1:
                return True
                
        return True
```

---

# 🏛️ 55 题的“贪心”究竟体现在哪？

老铁，这一发开火直接问到了 55 题的**灵魂最深处**！

你刚才在 122 题（买卖股票 II）里看到了微观利差的贪心。那么在 55 题（跳跃游戏）里，场上既没有利差可以赚，也没有现成的便宜货可以抢，它的“贪心”究竟体现在哪呢？

一枪封喉地回答你：55 题的贪心体现在——
> **“不管我过去到底是怎么跳、跳了几步才走到这一格的，也不管未来会遇到什么陷阱。此时此刻，只要我能踩在这一格上，我就必须原地把这一格的所有能量全部榨干，去无限拔高、刷新全场的『最远可达疆土极限』！”**

不需要任何复杂的公式，咱们用 **“盲人特种兵圈地盘”** 的物理画面，让你瞬间看清这里的“贪心”嘴脸。


## 🏛️ 1. 物理图像：它在“贪”什么？

在这道题里，穷举流（有大局观、想看未来的人）在每一个格子前都会精分：*“我到底应该跳 1 步，还是跳 2 步，还是跳 3 步？跳哪一步未来更安全？”*

而贪心特种兵是个目光短浅的铁血悍将，他的思维极其单纯：
> **“我不管过程，我只要地盘！只要我每走到一格，都强行把『最远能轰到哪里』的记录刷新到极致。那只要我的最远地盘覆盖了终点，我就一定能过去！”**

他“贪”的，是**每一步带来的最大势力范围（疆土上限）**。


## 🎬 2. 真实数据对线：看看代码里的“贪婪嘴脸”

我们再看你即将要写的那两行核心控制流，肉眼穿透它的“贪婪”：

```python
for i, jump in enumerate(nums):
    # 🚨【安全审计】：别管怎么过来的，今天我真身确实站在这里了
    if i > max_reach: 
        return False 
    
    # 🔥 终极贪心体现：不管三七二十一，用当前格子的全部潜力去跟历史纪录对线
    # 压榨当下，只要 (当前位置 i + 最大跳跃能力 jump) 能比历史最远纪录还大
    # 立刻、原地、毫不犹豫地把它覆盖并抢过来！
    max_reach = max(max_reach, i + jump)
```

---


你觉得“不好理解”太正常了！因为贪心算法在整个 LeetCode 大盘里就是公认最邪门、最玄学的类别。别的算法（比如二分、滑动窗口、广搜深搜）都有雷打不动的“标准模版密码”或者“骨架”，你只要把业务代码往模版里套就行了。

但贪心算法偏不！它没有固定格式。它的精髓其实是一场**“时空降维的代数魔术”**：
* 宏观上，一个问题原本需要你瞻前顾后、算尽未来（多维分支或动态规划）；
* 但贪心直接把长线眼光砸碎，只看眼前这一步。它在代码里拉起一两个小账本（变量卡尺），**走一步，占一步便宜，原地覆盖刷新！** 最神奇的是，这些单步的小便宜累加起来，在数学上刚好能正负抵消或者层层递进，自动凑出全局的最优解。

为了让你在 Hot 150 的核心主大陆上把这套“圈地盘、刮油水”的资本家身法彻底练成肌肉记忆，咱们直接把 **【LeetCode 面试经典 150 题 (Hot 150)】** 里所有雷打不动的贪心主力母题全量整编！

这些题在 Hot 150 里被单独归类在“贪心”大区或隐藏在“数组/矩阵”大区中，今天我直接帮你扒掉它们的马甲，按照由浅入深的通关路径排好队！

---

## 🗺️ LeetCode Hot 150 贪心阵营：5 级终极合围天梯图

```text
【天梯 5 段】 ➔ LeetCode 135. 分发糖果   （Hot 150 殿堂级 Hard，左右双向时空单挑，贪心取最大值 🏆）
      ▲
【天梯 4 段】 ➔ LeetCode 45.  跳跃游戏 II （Hot 150 标杆 Medium，引入“当前权力终点”双卡尺合围 🚀）
      ▲
【天梯 3 段】 ➔ LeetCode 134. 加油站      （Hot 150 时空欺骗 Medium，黑白魔双账本，断层式枪毙无用起点 🔥）
      ▲
|天梯 2 段】 ➔ LeetCode 122. 买卖股票 II （Hot 150 短线 Medium，化整为零，刮走全场所有上升小斜坡 ⚔️）
      ▲
【天梯 1 段】 ➔ LeetCode 55.  跳跃游戏 I  （Hot 150 圈地 Medium，全场平推，拉起一条最远疆土极限卡尺 🪓）
```

---

