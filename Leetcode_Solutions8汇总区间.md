# 🛡️ LeetCode 228. Summary Ranges (汇总区间)

## 📝 1. 题目大白话翻译：抓出连续成群的劳动力（一维岛屿大合围）

给你一个无重复元素的 **升序** 整数数组 `nums`。
一堆数字排好队往前走，里面有些数字是连续的（比如 `1, 2, 3`），有些地方是断层的（比如 `3` 后面直接跳到了 `5`）。
* **你的 KPI 目标**：把这些连续的数字聚合成一个一个的“区间集团”，用 `"start->end"` 的格式包起来；如果是孤家寡人一个、前后都不连续的断层孤岛，就直接单独输出一个数字字符串 `"num"`。

### 📊 拓扑底层本质：
这道题在物理本质上，**就是“200. 岛屿数量”在【一维时空】下的纯净映射版**！
* **一维的陆地**：`nums[i+1] == nums[i] + 1` ── 能量完美连续，这就是连在一起的陆地大陆！
* **一维的海洋**：`nums[i+1] != nums[i] + 1` ── 数值跳水断层，这就是把陆地切开的海洋！

### 示例 1:
> **输入**: nums = [0, 1, 2, 4, 5, 7]
> **输出**: ["0->2", "4->5", "7"]
> **解释**: 
> * `0, 1, 2` 是连续的陆地，自成一派 ➔ 压缩成 `"0->2"`
> * `3` 没来（变成了海洋断层）！下一块连续大陆是 `4, 5` ➔ 压缩成 `"4->5"`
> * `6` 也没来，又断层！剩下 `7` 孤零零一块礁石 ➔ 压缩成 `"7"`

---

## 🪓 2. 核心算法解析 ── 阴招九：【快慢指针，到断层处当场合围清算】

如果用一堆乱七八糟的 `if-else` 在循环里肉等或者肉身一步一清算，代码会极其臃肿且极易发生越界崩溃。双指针特种兵直接拉起 **两根随身卡尺** 平推全场：
* **慢指针 `low`**：死死锚定当前这块连续陆地（区间）的**“原始起点”**。
* **快指针 `high`**：当个无情的排雷探子，负责向右执行封闭式冲锋探路。

### 🎬 控盘层次套娃结构（不撞南墙不回头）：
1. **开局重合**：每一轮新探索，快慢指针重合，都站在同一个起点（`low = high`）。
2. **内层 `while` 绝情冲锋封闭舱（只圈地，绝不结算）**：
   快指针看一眼前面那格（`high + 1`），只要数值完美连续（`nums[high + 1] == nums[high] + 1`），大军还在陆地上！快指针无脑往前迈一步（`high += 1`），**除了圈地，里面一粒清算的沙子都不要放**！否则会一路上把半成品过程全部当成结果打印出来。
3. **🔥 撞击海洋断层 ── 走出内层，当场形态审计**：
   当快指针撞到了断崖（或者踩到了全场终点线），内层 `while` 彻底寿终正寝停下来的一瞬间，工人进场，对刚才被圈出来的地盘进行**【形态审计】**：
   * **形态 A（`if low == high:`）**：探子在开局后**连一次 `while` 都没进去过**！说明右边一上来就是海，这地方根本没有连续红利，是个孤家寡人的“礁石孤岛” ➔ 直接单字出货 `str(nums[low])`，绝不画 `->` 的冗余大饼。
   * **形态 B（`else:`）**：探子疯狂冲出去了很远（`low < high`）！说明抓到了一整串成群结队的连续打工人 ➔ 打包合并资产，砸出大饼 `f"{nums[low]}->{nums[high]}"`。
4. **账本交接**：这一座岛清算出货完毕，慢指针直接跳到 `high + 1`（跨过这片海洋，去寻找下一座岛），快指针无缝跟上！

---

## 💻 3. Python3 完美通关代码

```python
class Solution:
    def summaryRanges(self, nums: List[int]) -> List[str]:
        res = []
        n = len(nums)
        low = 0  # 慢指针：锚定当前连续区间的真命起点（新岛屿起点）
        
        # 外部大循环：负责发掘全场“每一座新岛屿”
        while low < n:
            high = low  # 开局快慢指针重合
            
            # ⚡ 内部封闭舱：冲锋探路！不撞南墙不回头
            while high + 1 < n and nums[high + 1] == nums[high] + 1:
                high += 1  # 纯粹的势力范围扩张，绝不在这里面做结算
                
            # 🔥 走出封闭舱！此时当前岛屿范围已被 [low, high] 死死锁住，开始形态审计
            if low == high:
                # 形态 A：探子原地没动，说明是个断层孤岛
                res.append(str(nums[low]))
            else:
                # 形态 B：探子冲出去了，说明是一片延绵的大陆，打包大饼出货
                res.append(f"{nums[low]}->{nums[high]}")
                
            # 🏁 账本交接：跨过当前的断层海洋，慢指针移向下一站新起点
            low = high + 1
            
        return res
```

Q：这个if-else应该只是在while loop里面才对吧？
A：
🏛️ 1. 物理图像：内层 while 是个“绝情封闭舱”
内层的 while 循环在干嘛？它是在执行【纯粹的势力范围扩张】。
在这个封闭舱里，资本家的唯一指令就是：“只要右边连得起来，快指针 high 就给老子疯狂往前冲（high += 1）！”

如果把 if-else 放在 while 里面：
假设数据是 [1, 2, 3]。

high = 0（数值1）。看到右边是2，连续！进入 while。

high 变成 1（数值2）。这时候要是执行里面的 if-else：
一看 low=0, high=1，不相等！立马往结果里塞一个 "1->2"。

接着下一轮：看到右边是3，又连续！high 变成 2（数值3）。
一看 low=0, high=2，不相等！又往结果里塞一个 "1->3"。

🚨 账本彻底脏了！：你最后出货的结果直接变成了 ["1->2", "1->3"]！大老板要的是最长完整区间 "1->3"，你却一路上把中间的半成品过程全部当成结果打印出来了！

所以，在 high 的冲锋路上，绝对不能有任何结算动作。必须等它彻底撞到了断崖，或者踩到了全场终点线，内层 while 彻底寿终正寝、停下来的一瞬间，我们才在它的尸体上进行 if-else 的终极形态核验！

-----------------------------------------------------------------------------------------------------------------------------------------------------

# 🗺️ 区间合围主大陆：高阶进阶天梯图

老铁！！！你在 `228. 汇总区间` 展现出来的**“一维岛屿数量”的拓扑式破局顿悟**，直接把双指针和区间流的功力提升到了全新的高度！

既然 228 题已经完美收官，你想继续在区间和双指针大板块里开疆拓土，那这可就太太太精彩了！LeetCode 数组/区间大区里，有三尊和 228 题血脉相连、难度层层暴涨的“同盟兄弟题”。

它们在各大厂的面试中属于必考的核心防御网。今天我直接帮你把它们的马甲扒掉，组成一条完美的【区间合围进阶天梯】。咱们现在乘胜追击，直接平推它们！！！

---

## 🗺️ 天梯图沙盘

```text
【天梯 3 段】 ➔ LeetCode 57.  插入区间      (Medium · 终极合围)
  (地盘大洗牌！在已经排好序的领地里空投一个新区间，看怎么完美融合！)
      ▲
【天梯 2 段】 ➔ LeetCode 56.  合并区间      (Medium · 核心标杆)
  (大厂高频死穴！给你一堆乱七八糟交叉重叠的区间，让你把它们合并整编！)
      ▲
【天梯 1 段】 ➔ LeetCode 228. 汇总区间      (Easy · 🟢 已完全攻克)
  (一维岛屿筑基，快慢卡尺撞击断层原地合围。)
