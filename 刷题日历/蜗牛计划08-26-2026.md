| LeetCode 题号与标题 | 核心算法类型 | 难度 | LeetCode 直达链接 |
| :--- | :--- | :--- | :--- |
| **144. Binary Tree Preorder Traversal**<br>(二叉树的前序遍历) | 二叉树遍历 / 递归 / 栈 (Stack) | 🟢 Easy | [LeetCode 144](https://leetcode.com/problems/binary-tree-preorder-traversal/) |
| **22. Generate Parentheses**<br>(括号生成) | 回溯 (Backtracking) / 递归 / 剪枝 | 🟡 Medium | [LeetCode 22](https://leetcode.com/problems/generate-parentheses/) |
| **131. Palindrome Partitioning**<br>(分割回文串) | 回溯 (Backtracking) / 动态规划 / 双指针 | 🟡 Medium | [LeetCode 131](https://leetcode.com/problems/palindrome-partitioning/) |
| **133. Clone Graph**<br>(克隆图) | 图 (Graph) / DFS / BFS / 哈希表 | 🟡 Medium | [LeetCode 133](https://leetcode.com/problems/clone-graph/) |

---

## LeetCode 144. 二叉树的前序遍历 - 详细解析与题解

### 题目描述
给你二叉树的根节点 `root`，返回它节点值的 **前序遍历**（Preorder Traversal）。
* **前序遍历规则**：按照 **根节点 $\rightarrow$ 左子树 $\rightarrow$ 右子树** 的顺序访问节点。

### 方法一：递归法 (Recursion) —— 最直观的官方标准解法

#### 1. 核心思路
前序遍历的定义本身具有天然的递归性：
1. 先访问当前根节点（把值加入结果集）。
2. 递归调用函数遍历左子树。
3. 递归调用函数遍历右子树。
当遇到空节点（`None`）时，递归终止并返回。

#### 2. 代码实现 (Python)
```python
class Solution:
    def preorderTraversal(self, root: TreeNode) -> list[int]:
        res = []
        
        def dfs(node):
            # 终止条件：如果节点为空，直接返回
            if not node:
                return
            
            # 1. 根：访问当前节点
            res.append(node.val)
            # 2. 左：递归遍历左子树
            dfs(node.left)
            # 3. 右：递归遍历右子树
            dfs(node.right)
            
        dfs(root)
        return res
```

### 方法二：迭代法 / 显式栈 (Iteration with Stack) —— 硬核底层模拟

#### 1. 核心思路
递归的本质是操作系统在内存中维护了一个“系统调用栈”。我们可以用一个显式的列表 `stack` 来模拟这个栈：
* 前序遍历的访问顺序是“根 $\rightarrow$ 左 $\rightarrow$ 右”。
* 因为栈是**先进后出 (LIFO)** 的：
  * 如果我们想让**左子树**先被弹出来处理，就必须**先把右子树压入栈，再把左子树压入栈**。
* 每次从栈顶弹出当前节点，记录其值，然后重复上述压栈过程。

#### 2. 代码实现 (Python)
```python
class Solution:
    def preorderTraversal(self, root: TreeNode) -> list[int]:
        if not root:
            return []
            
        res = []
        stack = [root]  # 初始化栈，放入根节点
        
        while stack:
            # 1. 弹出当前栈顶节点（相当于访问“根”）
            node = stack.pop()
            res.append(node.val)
            
            # 2. 先压入右子树（确保左子树先出栈）
            if node.right:
                stack.append(node.right)
                
            # 3. 再压入左子树
            if node.left:
                stack.append(node.left)
                
        return res
```

### 两种方法对比

| 维度 | 方法一：递归法 (Recursion) | 方法二：迭代法 (Iteration with Stack) |
| :--- | :--- | :--- |
| **底层原理** | 依赖系统调用栈 (Call Stack) | 依赖程序员自己维护的显式栈 (`stack` 列表) |
| **代码简洁度** | 极简，逻辑与前序定义完全对齐 | 稍显繁琐，需要处理栈的进出顺序 |
| **空间复杂度** | $O(H)$ （$H$ 为树的高度，最坏链状时为 $O(N)$） | $O(H)$ （同样需要用栈存储路径上的节点） |
| **时间复杂度** | $O(N)$ （每个节点恰好被访问一次） | $O(N)$ （每个节点恰好被压入和弹出一次） |
| **安全性** | 树太深（如链状达 $10^5$ 层）时，可能导致**栈溢出 (Stack Overflow)** | 安全，不受系统递归深度限制 |
| **适用场景** | 面试首选，绝大多数常规情况下的最优解 | 面试官明确要求“不用递归”或树极深时的安全解法 |

---

