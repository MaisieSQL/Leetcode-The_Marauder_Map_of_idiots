# 📂 LeetCode 215. 数组中的第 K 个最大元素 (Kth Largest Element in an Array) ── 堆排序与富豪俱乐部淘汰赛

## 📖 1. 题目描述
给定一个整数数组 `nums` 和一个整数 `k`，请返回数组中第 `k` 个最大的元素。

请注意，你需要找的是数组排序后的第 `k` 个最大元素，而不是第 `k` 个不同的元素。

**你能否在不使用标准排序（即打破 $O(N \log N)$ 复杂度限制）的情况下解决这个问题？**

### 示例 1：
*   **输入:** `nums = [3, 2, 1, 5, 6, 4]`, `k = 2`
*   **输出:** `5`
*   **解释:** 排序后的数组为 `[1, 2, 3, 4, 5, 6]`，第二大的元素是 `5`。

### 示例 2：
*   **输入:** `nums = [3, 2, 3, 1, 2, 4, 5, 5, 6]`, `k = 4`
*   **输出:** `4`

### 提示：
*   $1 \le k \le nums.length \le 10^5$
*   $-10^4 \le nums[i] \le 10^4$

---

## 💡 2. 破局关键：小顶堆的“富豪俱乐部”机制

如果使用常规的全局排序，时间复杂度是 $O(N \log N)$。为了优化到 $O(N \log K)$，我们可以使用**小顶堆（Min-Heap）**。

### 🧱 核心思维模型：
我们可以把这个小顶堆想象成一个**“只允许容纳 $K$ 个人的顶尖富豪俱乐部”**：
1. **小顶堆的铁律：** 每一个父亲节点都小于等于儿子节点。这意味着，**坐在俱乐部大门口（根节点 `heap[0]`）的，永远是这个俱乐部里“最穷（数值最小）”的那个富豪**。
2. **建队门槛（前 $K$ 个数）：** 先不管三七二十一，把前 `k` 个数强行 `push` 进堆，让俱乐部坐满。
3. **大浪淘沙（剩余数字）：** 从第 `k` 个数开始遍历，每一个新数字想进来，先和大门口最穷的那个富豪（`heap[0]`）比一比：
    * 如果新数字**小于或等于** `heap[0]`，说明它连俱乐部里最穷的人都比不过，根本没资格成为前 $K$ 大，**直接跳过（剪枝优化）**。
    * 如果新数字**大于** `heap[0]`，说明大门口那个人该退位了。我们无情地将堆顶 `pop` 弹出，然后把新数字 `push` 进去，让堆自己去通过“上浮/下沉”重新调整组织架构。
4. **终极清算：** 遍历完所有数字后，留在俱乐部里的 `k` 个人一定是全场底子最雄厚的 $K$ 个大富豪。而坐在大门口 `heap[0]` 的那个“大富豪里最穷的人”，正好就是全场**第 $K$ 大**的元素！

---

## 💻 3. 核心代码实现 (Python3 & Java)

### Python3 实现（你的高阶优化版本）
这个版本先用 `if` 过滤了没资格进堆的数字，避免了频繁触发树结构调整，运行效率极高。

```python
import heapq

class Solution(object):
    def findKthLargest(self, nums: list[int], k: int) -> int:
        # 1. 声明我们的微型小顶堆账本
        myheap = []
        
        # 2. 先把前 k 个数强行塞满堆，建立初始的俱乐部成员圈
        for i in range(k):
            heapq.heappush(myheap, nums[i])
            
        # 3. 遍历接下来的所有人，进行严格的门槛检查
        for j in range(k, len(nums)):
            # 只有比大门口最穷的那个（myheap[0]）还大，才允许踢人进入
            if nums[j] > myheap[0]:
                heapq.heappop(myheap)       # 淘汰当前最小的
                heapq.heappush(myheap, nums[j]) # 迎入新大哥
                
        # 4. 所有人走完这一遭后，堆顶 myheap[0] 就是第 K 大的巨头，直接返回
        return myheap[0]
```

---

