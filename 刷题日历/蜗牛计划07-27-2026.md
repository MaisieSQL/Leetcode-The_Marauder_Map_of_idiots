| 题号 & 英文名称 | 中文译名 | 核心题型 / 算法标签 | 难度 | LeetCode 官方直达链接 |
| :--- | :--- | :--- | :--- | :--- |
| **LeetCode 217** <br> Contains Duplicate | 存在重复元素 | 哈希表 / 数组去重 | 🟢 简单 (Easy) | [LeetCode 217 传送门](https://leetcode.cn/problems/contains-duplicate/) |
| **LeetCode 912** <br> Sort an Array | 排序数组 | 排序 / 归并排序 / 分治思想 | 🟡 中等 (Medium) | [LeetCode 912 传送门](https://leetcode.cn/problems/sort-an-array/) |
| **LeetCode 75** <br> Sort Colors | 颜色分类 | 数组 / 双指针 / 荷兰国旗问题 | 🟡 中等 (Medium) | [LeetCode 75 传送门](https://leetcode.cn/problems/sort-colors/) |
| **LeetCode 304** <br> Range Sum Query 2D - Immutable | 二维区域和检索 - 矩阵不可变 | 矩阵 / 二维前缀和 / 设计 | 🟡 中等 (Medium) | [LeetCode 304 传送门](https://leetcode.cn/problems/range-sum-query-2d-immutable/) |

---

# 📝 LeetCode 217. 存在重复元素 (Contains Duplicate) 题目解析

## 1. 题目大意
- **原题描述**：给你一个整数数组 `nums`。如果数组中任一值出现**至少两次**，返回 `true`；如果数组中每个元素互不相同，返回 `false`。
- **输入输出示例**：
  - 输入：`nums = [1, 2, 3, 1]` -> 输出：`True`（因为 `1` 重复出现了）
  - 输入：`nums = [1, 2, 3, 4]` -> 输出：`False`（全员各不相同）

## 2. 核心考点与物理直觉
- **核心考点**：哈希表（Hash Map / 键值对映射）、线性扫描。
- **物理直觉**：
  - 最笨的方法是拿每一个数字去和它后面的所有数字做对比（暴力双循环），时间复杂度直接飙升到 $\mathcal{O}(N^2)$，数据量大时铁定超时。
  - **降维打击策略**：利用**标准哈希表（Hash Map）**。我们在遍历数组的过程中，把见过的数字当作 `Key` 存入哈希表中。每遇到一个新数字，先去哈希表里“照镜子”：
    - 如果在哈希表里找到了，说明**撞车（重复）**，直接鸣金收兵返回 `true`。
    - 如果没找到，就把它登记录入哈希表，继续往下走。

## 3. 标准哈希表（Hash Map）解法源码 (Python3)

```python
class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        # 声明一个标准的哈希表（Hash Map / 字典），用于记录数字及其出现状态
        freq_map = {}
        
        for num in nums:
            # 如果当前数字已经在哈希表中，说明之前见过了，触发重复判定
            if num in freq_map:
                return True
            
            # 首次遇到，将其登记进哈希表（赋予一个占位 Value 1）
            freq_map[num] = 1
            
        # 完整遍历完数组都没有触发返回，说明没有任何重复元素
        return False
```

4. 复杂度深度核验
- **时间复杂度**：O(N) —— 只需要对数组进行单次线性扫描（N 为数组长度）。标准哈希表的单次查找 (`in`) 和单次插入操作的均摊时间复杂度均为 O(1)。
- **空间复杂度**：O(N) —— 在最坏情况下（即整个数组完全没有重复元素），我们需要把所有的 N 个元素全部存入哈希表中，因此需要消耗线性的存储空间。

---

# 📝 LeetCode 912. 排序数组 (Sort an Array) 题目解析

## 1. 题目大意
- **原题描述**：给你一个整数数组 `nums`，请你将该数组升序排列。
- **硬性约束**：必须在 O(N log N) 的时间复杂度内完成，且尽量使用较好的空间复杂度。不能直接依赖 Python 自带的无脑 `nums.sort()`（虽然能过，但面试官通常要求手写经典排序算法）。

## 2. 核心考点与物理直觉
- **核心考点**：分治思想 (Divide and Conquer)、归并排序 (Merge Sort)。
- **物理直觉**：
  - 如果数组很乱、很长，直接整体排序很难下手。
  - **“分而治之” (Divide and Conquer)** 的核心哲学：
    1. **分 (Divide)**：把一个大数组从中间一刀切两半（`left` 和 `right`），一直递归切到每个子数组只剩 1 个元素（此时天然有序）。
    2. **治 (Conquer/Merge)**：把两个已经有序的小数组，通过**双指针**像拉拉链一样完美合流成一个更大的有序数组，一路向上合并，直到恢复成完整的大数组。

## 3. 标准归并排序解法源码 (Python3)

```python
class Solution:
    def sortArray(self, nums: List[int]) -> List[int]:
        # 递归出口：当数组长度小于或等于 1 时，本身就是有序的，直接返回
        if len(nums) <= 1:
            return nums
        
        # 1. 分 (Divide)：找到中点，一分为二
        mid = len(nums) // 2
        left = self.sortArray(nums[:mid])
        right = self.sortArray(nums[mid:])
        
        # 2. 治 (Merge)：将左右两个有序数组合流
        return self.merge(left, right)
        
    def merge(self, left: List[int], right: List[int]) -> List[int]:
        res = []
        i = j = 0
        
        # 双指针像拉拉链一样，谁小先拿谁
        while i < len(left) and j < len(right):
            if left[i] <= right[j]:
                res.append(left[i])
                i += 1
            else:
                res.append(right[j])
                j += 1
                
        # 把没走完的尾巴直接拼接到末尾
        res.extend(left[i:])
        res.extend(right[j:])
        return res

```

4. 复杂度深度核验
- **时间复杂度**：O(N log N) —— 递归树的高度为 log N（每次对半拆分），每一层在合并所有元素时需要进行线性的 O(N) 扫描，总时间复杂度稳稳锁定在 O(N log N)。
- **空间复杂度**：O(N) —— 递归调用栈会消耗 O(log N) 的栈空间，同时在每次归并时需要创建临时列表来存储合并后的数据，因此整体空间复杂度主要由临时存储决定，为 O(N)。

---

# ⚖️ 排序算法大乱斗：为什么 LeetCode 912 选“分治/归并”，不用其他算法？

## 一、 为什么选“分治（归并排序）”？它好在哪？
在 LeetCode 912（排序数组）中，归并排序（Merge Sort）之所以是标准推荐解法之一，核心原因有两点：
1. **最坏情况下的绝对稳定性**：它的时间复杂度在**最好、平均、最坏情况下全都是 O(N log N)**，绝不会因为数据本身长得奇葩而退化成 O(N²)。
2. **稳定的排序（Stable Sort）**：如果两个元素值相同，它们排序前后的相对前后顺序不会被打破。这在复杂对象排序中非常关键。

---

## 二、 还有其他算法吗？
当然有！面试和工程中常见的排序算法大体可以分为两派：**通用比较排序**（比如快速排序、堆排序、插入排序）和**非比较排序**（比如计数排序）。

---

## 三、 主流排序算法时间与空间复杂度全景对比表

| 排序算法名称 | 最好时间复杂度 | 平均时间复杂度 | 最坏时间复杂度 | 空间复杂度 | 稳定性 | 为什么不选它 / 适用场景 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **归并排序 (Merge Sort)** <br> *(咱们选的)* | O(N log N) | O(N log N) | **O(N log N)** | O(N) | 稳定 | **优点**：最坏情况稳如老狗，稳定。<br>**缺点**：需要 O(N) 的额外辅助空间。 |
| **快速排序 (Quick Sort)** | O(N log N) | O(N log N) | **O(N²)** | O(log N) | 不稳定 | **为什么不用**：如果主元（Pivot）没选好，最坏情况会退化成 O(N²) 导致超时（LeetCode 上有专门卡快排的恶心测试用例）。 |
| **堆排序 (Heap Sort)** | O(N log N) | O(N log N) | **O(N log N)** | **O(1)** | 不稳定 | **为什么不用**：虽然空间复杂度只要 O(1) 且最坏 O(N log N)，但由于涉及频繁的树节点上下调整，常数系数大、对 CPU 缓存不友好，实际运行速度通常比归并和快排慢。 |
| **插入排序 (Insertion Sort)** | O(N) | O(N²) | **O(N²)** | O(1) | 稳定 | **为什么不用**：面对 LeetCode 这种动辄几十万的数据量，O(N²) 的慢速度会直接超时。 |
| **计数排序 (Counting Sort)** <br> *(非比较排序)* | O(N + K) | O(N + K) | **O(N + K)** | O(K) | 稳定 | **局限性**：这题的数字范围跨度极大（包含负数且绝对值很大），直接用计数排序会耗费天文数字的内存（K 太大），导致内存爆炸。 |

# 🏛️ 主流排序算法的真实门派归属与复杂度全景对比

严格来说，“分治法 (Divide and Conquer)”并不是一个具体的排序算法，而是一种**顶级的算法设计思想/策略**。像归并排序和快速排序，都是**采用了分治思想的具体排序算法**。

## 📊 主流排序算法分类与复杂度表

| 核心设计思想 / 策略 | 具体的排序算法名称 | 最好时间复杂度 | 平均时间复杂度 | 最坏时间复杂度 | 空间复杂度 | 稳定性 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **分治策略 (Divide and Conquer)** | **归并排序 (Merge Sort)** | O(N log N) | O(N log N) | O(N log N) | O(N) | 稳定 |
| **分治策略 (Divide and Conquer)** | **快速排序 (Quick Sort)** | O(N log N) | O(N log N) | O(N²) | O(log N) | 不稳定 |
| **减治/选择策略 (Selection)** | **堆排序 (Heap Sort)** | O(N log N) | O(N log N) | O(N log N) | O(1) | 不稳定 |
| **增量插值策略 (Insertion)** | **插入排序 (Insertion Sort)** | O(N) | O(N²) | O(N²) | O(1) | 稳定 |
| **非比较统计 (Counting/Bucket)** | **计数排序 / 桶排序** | O(N + K) | O(N + K) | O(N + K) | O(K) | 稳定 |

## 💡 为什么 LeetCode 912 推荐使用归并排序？
1. **最坏情况稳如老狗**：归并排序无论面对什么刁钻的数据，其最坏时间复杂度都能稳稳保持在 **O(N log N)**。
2. **规避快排超时风险**：快速排序在最坏情况下会退化成 **O(N²)**。LeetCode 912 拥有专门卡快排的极端测试用例，如果主元（Pivot）选择不当很容易直接触发超时。

---

