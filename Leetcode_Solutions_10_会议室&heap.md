# 🪓 算法面试终极破局：LeetCode 252. Meeting Rooms (会议室 I)

## 📝 一、 题目描述 (Description)

给定一个会议时间安排的数组 `intervals` ，每个会议时间都会包括开始和结束的时间 `intervals[i] = [start_i, end_i]` ，请你判断一个人是否能够参加这里面的全部会议。

### 示例 1:
> **输入**: intervals = [[0,30],[5,10],[15,20]]
> **输出**: false
> **解释**: 因为会议 [0,30] 和 [5,10] 在时间轴上发生了重叠，同一个人不可能同时分身在两个房间开会，判定失败。

### 示例 2:
> **输入**: intervals = [[7,10],[2,4]]
> **输出**: true


## 🏛️ 二、 为什么用【扫描线核心/排序边界】这个算法？

* **核心痛点**：判断多个人力时间区间在时间轴上**是否有任何一丝一毫的交叉重叠**。
* **物理破局点**：乱序散装的会议区间在时间轴上是错乱的。资本家冷笑一声：“全量给老子按**【会议开始时间】**升序排队（`key=lambda x: x[0]`）！”
  * 一旦开始时间有序了，大军向前大平推，我们**只需要盯着『当前会议的结束时间』和『下一个会议的开始时间』对线即可**！
  * 这种“将时间轴排序后单向平推”的策略，就是**扫描线算法 (Sweep Line)** 最基础的开局形态。
* **防御盲点**：如果下一个会议的**开始时间**，居然比上一个会议的**结束时间**还要小（即 `intervals[i][0] < intervals[i-1][1]`），说明这两个会议撞车了！时间产生了断层交叉，直接分身乏术，熔断返回 `False`！

## ⏱️ 三、 这个算法的时间和空间复杂度是多少？

* **Time Complexity (时间复杂度)：$\mathcal{O}(N \log N)$** ── 极其清爽的对数线性级。耗时大头卡在开局的 `sort()` 排序上（$\mathcal{O}(N \log N)$）；后续是一趟极其清爽、绝不倒车的 $\mathcal{O}(N)$ 线性单步检查。
* **Space Complexity (空间复杂度)：$\mathcal{O}(\log N)$ 或 $\mathcal{O}(N)$** ── 纯粹取决于系统内置排序算法在底层递归时吃掉的辅助栈空间，不需要额外开辟任何临时账本，极其廉价纯净！


## 🪓 四、 为什么说它是最少、最好的解法？

* **对比凡人流**：普通的凡人流由于缺乏大局观，会拿着任意两个会议在时间轴上两两去穷举比对（时间复杂度 $\mathcal{O}(N^2)$），高并发测试数据下直接爆仓超时。而我们**通过一行 `sort()` 强行整编排队，生生消灭了一个混乱维度，在对数线性级里一枪封喉，落子无悔！**

## 💻 五、 Python3 完美通关代码

```python
class Solution:
    def canAttendMeetings(self, intervals: List[List[int]]) -> bool:
        # 1. 扫描线前置手艺：让所有会议按【开始时间】从小到大排好队
        intervals.sort(key=lambda x: x[0])
        
        # 2. 单层循环，走一步，看一眼前后边界的交叉重叠
        for i in range(1, len(intervals)):
            # 🚨 灵魂安检线：如果下一个会议的开始时间，比上一个会议的结束时间还要早！
            if intervals[i][0] < intervals[i - 1][1]:
                return False  # 分身乏术，撞车内鬼被抓，当场熔断
                
        # 3. 稳步扫描全场无越界，安全通过
        return True
```

---

# 🪓 算法面试终极破局：LeetCode 253. Meeting Rooms II (会议室 II)

## 📝 一、 题目描述 (Description)

给你一个会议时间安排的数组 `intervals` ，每个会议时间都会包括开始和结束的时间 `intervals[i] = [start_i, end_i]` ，为避免会议冲突，同时考量高效利用率，请你计算**至少需要多少间会议室**才能满足所有会议安排。

### 示例 1:
> **输入**: intervals = [[0,30],[5,10],[15,20]]
> **输出**: 2
> **解释**: 
> * 会议 `[0,30]` 在 0 时刻开始，占用 1 间房。
> * 会议 `[5,10]` 在 5 时刻开始，此时 `[0,30]` 还没结束，被迫多开 1 间房（并发数达到 2）。
> * 会议 `[15,20]` 在 15 时刻开始，此时 `[5,10]` 早就退房了，空出来的房间可以被新会议无缝白嫖复用。全场最大同时需要的房间峰值是 2。

### 示例 2:
> **输入**: intervals = [[7,10],[2,4]]
> **输出**: 1

---

## 🏛 McKay 二、 为什么用【小顶堆动态抽屉/血脉重组进化】这个算法？

* **血脉因果进化（与 252 题的一招鲜连线）**：
  本题与 252 题（会议室 I）共享完全一致的开局控盘手艺 ── **“所有散装区间必须先按开始时间（`key=lambda x: x[0]`）升序排好队！”**。
* **物理破局点（如何把重叠的放到同一个抽屉？）**：
  在物理画面上，**每一个会议室就是一个『抽屉（管道）』，里面存的是这个房间当前的使用结束时间**。
  大军有序进场。当新会议 `[start, end]` 准备进场时，它只想极其贪婪地占便宜：它不需要看全场所有的房间，它**只需要和“全场当前最先开完会（结束时间最早/数值最小）的那个房间”对线**！
* **为什么普通的 Stack 或 Queue 必当场残废漏账？**：
  * **Queue（队列，先进先出）**：死板地卡在“谁先来”。如果最先进来的房是一个要开到 30 点的长会议，后开辟的房在 10 点就开完了。Queue 会无情地把 10 点的空房堵在屁股后面，导致新会议无法复用它，漏账爆仓！
  * **Stack（栈，后进先出）**：死板地卡在“谁后到”。它会把最先空出来的老房间死死压在栈底，整个控容大盘瞬间瘫痪。
  * **Heap（小顶堆）**：不讲先来后到，只讲数值极值！无论房间是什么时候开辟的，小顶堆的树形架构会**让“当前最先开完会（结束时间最小）”的房间永远高能地悬浮在堆顶（`rooms_heap[0]`）**！
* **POP（退房复用）原则**：
  * **如果 `新会议的开始时间 >= 堆顶的结束时间`**：说明堆顶那个最早的房间已经空出来了！太棒了！**立刻执行 POP（`heapq.heappop`）把老房间的时间弹出注销**，代表我们名正言顺地抢占复用了这个老抽屉！
  * **如果 `新会议的开始时间 < 堆顶的结束时间`**：完蛋！连全场最早开完会的房间都还没腾出来，说明这一刻全场房间由于并发而全部满员！**不允许 POP，必须硬生生新开一间房（直接 PUSH 新时间）**！
  * 不管复用还是新开，最后都要把新会议的 `end` 砸进（PUSH）堆里作为这间房的最新寿命，大军平推完后，堆里的最终留下的元素数量（房间数）就是真命答案！

---

## ⏱️ 三、 这个算法的时间和空间复杂度是多少？

* **Time Complexity (时间复杂度)：$\mathcal{O}(N \log N)$** ── 顶格优秀的对数线性流。开局的 `sort()` 排序耗时 $\mathcal{O}(N \log N)$。在后续平推的 $N$ 步中，每走一步利用小顶堆进行 $\mathcal{O}(\log N)$ 的极速查重与调整，总时间完美死锁，大数据量下稳如闪电。
* **Space Complexity (空间复杂度)：$\mathcal{O}(N)$** ── 极其省内存。小顶堆（优先队列）里面最多同时装 $N$ 个房间的退房时刻，空间投资回报率极高。

---

## 💻 四、 Python3 完美通关代码

```python
import heapq

class Solution:
    def minMeetingRooms(self, intervals: List[List[int]]) -> int:
        if not intervals:
            return 0
            
        # 1. 🌟【完完全全一字不改复刻 252 题】！
        # 让全场散装会议按照【开始时间】从小到大严格升序排好队
        intervals.sort(key=lambda x: x[0])
        
        # 2. 备忘录：拉起一个用来存“每个房间当前结束时间”的小顶堆（Priority Queue）
        # 堆顶（rooms_heap[0]）永远会高能自动悬浮出：当前全场“最先开完会/最早退房”的那个房间的结束时间！
        rooms_heap = []
        
        # 先把全场第一个排好队的会议的结束时间砸进堆里，作为我们的第一个物理抽屉（第一间开辟的房）
        heapq.heappush(rooms_heap, intervals[0][1])
        
        # 3. 从第 2 个会议开始单向平推对线，一格一格往前干，绝不走回头路
        for meeting in intervals[1:]:
            current_start = meeting[0]
            current_end = meeting[1]
            
            # 🎰 扫描线与抽屉合围核心对线：看一眼全场最早退房的那个时间标杆
            earliest_end = rooms_heap[0]
            
            # 🌟 POP（退房复用）原则落地：如果当前会议的开始时间，大于或等于最早开完会的那个时间！
            if current_start >= earliest_end:
                # 说明最早的那间房已经空出来了！大便宜！直接抢占，把那个老房间的时间弹出（POP）销户
                heapq.heappop(rooms_heap)
                
            # 🧠 无论是白嫖复用了老抽屉，还是因为满员被迫新开了一个新抽屉，
            # 此时此刻，当前会议的最新结束时间，都必须作为最新的房间寿命打卡塞进（PUSH）堆里！
            heapq.heappush(rooms_heap, current_end)
            
        # 4. 终极大清算：当大军扫完战场，小顶堆里剩下的元素个数，
        # 就铁证如山地代表了整个历史长河中，全场最高同时发生重叠的抽屉（会议室）峰值！
        return len(rooms_heap)
```

# 🪓 数据结构底层终极审计：LeetCode 253 为什么用 Heap，不用 Stack 或 Queue？

## 🏛️ 一、 核心物理图像：我们在管理什么数据？

在 LeetCode 253 题（会议室 II）的排序平推流中，每一个房间在内存里就是一个**“抽屉（管道）”**，里面存放着**“这个房间当前的会议结束时间（退房时刻）”**。

当大军按开始时间升序排好队，一个新会议 `[start, end]` 准备进场时，为了实现最高效的**“空间白嫖复用”**，它不需要看全场所有的房间，他**只需要和“全场当前最先开完会（结束时间最早/数值最小）的那个房间”进行对线核验**！
* 如果 `新会议开始时间 >= 全场最早退房时间` ➔ 老房间空出来了，直接白嫖，刷新这个房间的寿命。
* 如果 `新会议开始时间 < 全场最早退房时间` ➔ 连最先开完的房都没腾出来，全场满员，被迫新开一间房。

---

## 🚨 二、 内鬼对线：为什么 Stack 和 Queue 会当场残废漏账？

决定一个房间能不能被新会议复用的，**根本不是它被开辟的“先后顺序”，而是它当前里面装的“数值极值（谁最小）”**！而 `Stack` 和 `Queue` 身上死板的“先来后到”铁律，会导致空房被无情堵死，造成惊天漏账！

### 1. ❌ Queue (队列) ── 先进先出 (FIFO) 导致“长会议堵死空房间”
假设我们按房间开辟的先后顺序，把它们的退房时刻扔进 `Queue`：
* 房间 A：结束时间是 `30` （先开辟，是个长会议）
* 房间 B：结束时间是 `10` （后开辟，是个短会议）

此时 `Queue` 账本里的顺序是：`[30, 10]`，队头是 `30`。
现在来了一个新会议 `[15, 20]`：
* **人肉审计**：15 点进场，10 点就开完会的房间 B 明明是空着的，完全可以进去白嫖！
* **Queue 翻车现场**：由于先进先出，你执行 `queue[0]` 只能拿到队头的 **`30`**！新会议一比对 `15 < 30`，误以为全场没空房，**被迫向系统申请新开第 3 间房**！而那个早就空出来的房间 B 被 30 死死堵在屁股后面，全线漏账！



### 2. ❌ Stack (栈) ── 后进先出 (LIFO) 导致“老房间惨遭栈底封印”
同样的场景，如果我们把退房时刻压入 `Stack`：
* 房间 A：结束时间是 `10` （先开辟，短会议）
* 房间 B：结束时间是 `30` （后开辟，长会议）

此时 `Stack` 栈顶是最后压入的 **`30`**。
新会议 `[15, 20]` 进场去查栈顶，只能抓到 `30`。一比对 `15 < 30`，再次以为全场爆仓，**被迫又新开一间房**！而那个在 10 点就空出来的优质房间 A，被压在栈底动弹不得，直到地老天荒都无法被复用！



---

## 👑 三、 为什么只有 Heap (小顶堆) 是唯一的真命天子？

小顶堆（Priority Queue）是一尊**彻底砸碎先来后到、在无序动态中只认“最值极值”的顶级黑科技**！

不管这堆会议室是在什么时刻、以什么顺序开辟的，只要它们的结束时间被推进了小顶堆（`rooms_heap`），底层的二叉树树形架构会**让全场【当前结束时间最早（数值最小）】的那间房，永远、实时、动态地自动悬浮在堆顶（`rooms_heap[0]`）**！

当新会议 `[15, 20]` 进场时，它根本不需要扫描全场，直接花 $\mathcal{O}(1)$ 的常数级极速往堆顶瞥一眼：
堆顶自动浮现着 **`10`**！
新会议哈哈大笑：“`15 >= 10`！合规！最早的房空出来了！”
* **立刻执行 POP（`heapq.heappop`）**：把堆顶的老时间 `10` 弹出销户，代表我们无损接管、复用了这间会议室。
* **随后执行 PUSH（`heapq.heappush`）**：把自己的最新结束时间 `20` 砸进堆里更新房间寿命。



---

## 📊 四、 三大基础数据结构物理特性终极清算

| 数据结构 (Data Structure) | 核心控制流 (Rule) | 判定视点 (Focus) | 253题适用性审计 (Result) |
| :--- | :--- | :--- | :--- |
| **Queue (队列)** | 先进先出 (FIFO) | 只看 **“谁来得早”** | ❌ **残废**。长会议若先进队，会将屁股后面早早空出来的短会议房间彻底堵死，导致漏账。 |
| **Stack (栈)** | 后进先出 (LIFO) | 只看 **“谁来得晚”** | ❌ **残废**。先开辟且早早空出来的老房间会被死死压在栈底，永久封印，导致爆仓。 |
| **Heap (小顶堆)** | 最值先出 (Priority) | 动态锁定 **“谁的值最小”** | 👑 **通关真命天子**。堆顶永远悬浮最先退房的时间标杆，以 $\mathcal{O}(\log N)$ 的微小树形内卷代价，完美实现区间白嫖。 |

---

# 🪓 算法面试终极破局：LeetCode 1854. Maximum Population Year (人口最多的年份 - 小顶堆完美版)

## 📝 一、 题目描述 (Description)

给你一个二维整数数组 `logs` ，其中每个 `logs[i] = [birth_i, death_i]` 表示第 `i` 个人的出生和死亡年份。

人口最多的年份是出生到死亡之间，**同时存活** 人数最多的年份。一个人在他在 `birth_i` 当年计入当年的人口，但在 `death_i` 当年 **不计入** 当年的人口。

请你返回人口最多的 **最早年份** 。

### 示例 1:
> **输入**: logs = [[1993,1999],[2000,2010]]
> **输出**: 1993
> **解释**: 两个人的生命周期没有重叠。1993 年人口为 1，2000 年人口也为 1。1993 年更早，返回 1993。

### 示例 2:
> **输入**: logs = [[1950,1961],[1960,1971],[1970,1981]]
> **输出**: 1960
> **解释**: 
> * 1950 - 1959 年：人口为 1（只有第 1 个人活着）。
> * 1960 年：第 2 个人出生，此时第 1 个人还没死，**全场同时存活人数达到最高峰值 2**！
> * 1961 年：第 1 个人死亡注销，人口跌回 1。
> * 1970 年：第 3 个人出生，此时第 2 个人也活着，**全场人口再次达到高峰 2**。
> * 终极清算：1960 年和 1970 年人口都是最高峰 2，题目要求返回最早的年份，1960 < 1970，死锁 1960。

### 提示:
* $1 \le \text{logs.length} \le 100$
* $1950 \le \text{birth\_i} < \text{death\_i} \le 2050$

## 🏛️ 二、 为什么用【小顶堆动态抽屉/253题血脉重组进化】这个算法？

* **血脉因果链条（100% 孪生分身）**：
  本题与 253 题（会议室 II）在物理图像上属于完全相同的“时空并发上限审计模型”。我们直接沿用 253 题的资本家剥削手艺──**“让所有人按出生年份排序，用一个小顶堆来动态装住当前活着的每个人的死亡年份（退房时刻）”**！
* **物理破局点（堆顶悬浮谁？）**：
  在物理画面上，**小顶堆（`myheap`）里面装的是当前活着的所有人的『死期』**。因为是小顶堆，它的树形架构会让**全场当前【最先去世（死亡年份最小）】的那个人，永远、实时、动态地悬浮在堆顶（`myheap[0]`）**！
* **🚨 符号控制死穴（为什么必须是 `>=` ？）**：
  题目硬性规定：**“死亡当年不计入人口”**。物理含义是：如果老人的死亡年份，和今天新出生婴儿的出生年份正好是同一年，那么在这一年，这个老人必须利索注销退房。
  * 我们的核销线必须卡死 **`while myheap and birth >= myheap[0]:`**！只要新人的出生年赶上或者超越了堆顶老人的死期，老人当场被 `heappop` 弹出销户。如果错写成 `>`，会导致同一年死、生的人被强行在内存里重叠算作活人，产生不可容忍的假账！
* **终极结算 KPI（与 253 题唯一的区别）**：
  * **253 题**：大老板只要全场并存过的**最高【并发数量（房间数）】**（`max_rooms = max(max_rooms, len(heap))`）。
  * **1854 题**：大老板不要最高人数，他要的是**打破历史纪录那一瞬间的【时间戳（年份）】**！所以利用严格大于号 `if len(myheap) > max_pop:` 进行防御。后续并列第一的高峰由于等号不成立直接装死跳过，从而跨时空**死锁并保全了最古老的那一年**！


## ⏱️ 三、 这个算法的时间和空间复杂度是多少？

* **Time Complexity (时间复杂度)：$\mathcal{O}(N \log N)$** ── 开局的 `sort()` 排序耗时 $\mathcal{O}(N \log N)$。在后续平推的 $N$ 步中，每走一步利用小顶堆进行 $\mathcal{O}(\log N)$ 的极速查重与核销，总时间复杂度在对数线性级完美锁死，极其高效。
* **Space Complexity (空间复杂度)：$\mathcal{O}(N)$** ── 极其划算。小顶堆（优先队列）里面最多同时装 $N$ 个人的死亡时刻，不浪费一丁点无用内存（`ans_year` 的死数据也已被完全洗净）。


## 💻 四、 Python3 完美通关代码 (手操小顶堆纯净版)

```python
import heapq

class Solution:
    def maximumPopulation(self, logs: List[List[int]]) -> int:
        # 1. 🌟【完完全全、一字不改复制 253 题开局】！
        # 让全场所有人按照【出生年份（起点）】从小到大严格升序排好队
        logs.sort(key=lambda x: x[0])

        myheap = []      # 小顶堆：里面死死装住当前活着的每个人的“死亡年份（退房时刻）”
        max_pop = 0      # 历史最高人口峰值记录卡尺
        res = None       # 🎰 纯净防御性声明，用来捕获最终要返回的真命年份

        # 2. 单层 for 循环单向轰鸣平推，绝不走回头路，绝不倒车
        for birth, death in logs:
            # 🚨 终极核销边界线：必须是 >= 
            # 只要新人的出生年赶上了或者超越了堆顶老人的死期，老人当场利索注销退房（POP弹出）
            while myheap and birth >= myheap[0]:
                heapq.heappop(myheap)
                
            # 新人入场打卡，他的死亡年份 PUSH 进小顶堆内卷，更新寿命
            heapq.heappush(myheap, death)

            # 3. 实时刷新最高并发历史记录
            # 🎰 贪心体现：这里使用严格的大于号 > 
            # 第一轮 1 > 0 必定触发，res 锁定第一个人的出生年。后面如果有并列第一的高峰，
            # 条件不成立直接装死跳过，从而完美在时空长河里死锁了“最早的那一年”！
            if len(myheap) > max_pop:
                max_pop = len(myheap)
                res = birth
                
        return res
```

时间复杂度：第一段sort是nlog(n)，第二段for loop是n，但是因为用到了heap，所以是nlog(n)，所以总共是Nlog(n)

# 🪓 架构师硬核时空审计：为什么第二段 for 循环因为 Heap 变成了 N log N？

很多算法新兵看到“外层一个 `for` 循环走 $N$ 步，内层套了一个 `while` 循环”的代码骨架，本能地就会大喊 $\mathcal{O}(N^2)$ 的地狱复杂度。

其实，第二段循环由于引进了 `Heap`（小顶堆），其底层的真实时间复杂度是极其纯净的 **$\mathcal{O}(N \log N)$**！为了看清真相，我们需要把循环内有关堆的两个动作拆开，用**华尔街大庄家数硬币的总账本流水线思维**进行全量审计：

---

## 🏛️ 账本一：`heappush`（进堆）的无情长周期剥削 ── 稳稳的 $N \log N$

首先看每一步雷打不动必然触发的进堆动作：
```python
for birth, death in logs:
    # ... 省略 while 弹栈
    heapq.heappush(myheap, death)  # 🎰 动作 A：进堆
```

频次清算：外层 for 循环一共要遍历 $N$ 个人（$N$ 个时间区间）。因此，这行 heappush 百分之百、雷打不动、不多不少刚好会被执行 $N$ 次！单步代价：小顶堆的底层是一个完全二叉树架构。每当你把一个新的死亡年份 death 拍进堆里时，这个数字为了维持小顶堆的秩序，必须在二叉树内部执行“向上气泡翻滚（Bubble Up）”。因为树的高度是 $\log N$，所以每一次 heappush 的维护成本是严格的 $\mathcal{O}(\log N)$！代数汇总：执行了 $N$ 次，每次花 $\mathcal{O}(\log N)$。👉 进货总账面成本 = $N \times \mathcal{O}(\log N) = \mathcal{O}(N \log N)$

## 🏛️ 账本二：while + heappop（出堆）的摊销做假账真相 ── 依然是 $N \log N$现在看内层让凡人产生恐慌的 while 循环部分：
```python
while myheap and birth >= myheap[0]:
    heapq.heappop(myheap)  # 🎰 动作 B：出堆
```

总次数被物理死锁（能量守恒）：在计算机宇宙里，有一条落子无悔的因果铁律：“一个元素如果想从堆里被弹出来（Pop），前提是它必须先被推进去（Push）！”我们刚才在账本一里清算过，全场一共只有 $N$ 个人，所以 heappush 的总数一共只有 $N$ 个。这就铁证如山地限制了：在整场战役中，内层 while 循环里的 heappop 哪怕把堆彻底吸干，总共、最多、也绝对只能被执行 $N$ 次！单步弹出代价：每次执行 heappop() 时，堆顶（当前全场最小值）被切掉，为了重新调整树形，单次树形下沉开销同样是 $\mathcal{O}(\log N)$！分摊（Amortized）做假账技术：虽然在某一格（比如某一年死了一大堆人），while 可能会疯狂连环弹出 5 个人，那一步确实花了 $5 \times \log N$ 的算力。但在随后的好几步里，while 连大门都没进过，开销直接为 0！我们将这整个历史长河中最多发生的 $N$ 次 heappop 总成本，平均分摊（Amortized）到全场 $N$ 步的每一步日常行军里。👉 单步平均分摊下来的出堆成本 = $\mathcal{O}(\log N)$👉 出货总账面成本 = $N \times \mathcal{O}(\log N) = \mathcal{O}(N \log N)$🪓 三、 终极总账合流（两项相加）现在我们把第二段 for 循环里“进堆”和“出堆”的总账合并清算：

```python
总时间开销 = 进堆总成本 + 出堆总成本
           = O(N log N) + O(N log N)
           = O(2N log N)
```

根据大 O 表示法无情抹杀一切常数项的钢铁原则，常数 2 直接被捏爆。所以结论盖棺定论：第二段 for 循环由于引进了 Heap 维护最值，它的总时间复杂度是严格的 $\mathcal{O}(N \log N)$！加上第一段排序（sort）耗费的 $\mathcal{O}(N \log N)$，两大主力平级并列，全场大盘的总时间复杂度最终稳稳卡死在 $\mathcal{O}(N \log N)$！

# 🪓 堆底层结构底层审计：为什么 heappush 和 heappop 都是 O(log N)？

堆（Heap）在底层的物理内存里虽然是用一个普通的连续**数组**来存放的，但在计算机的逻辑宇宙里，它被画成了一棵高度对称、匀称的 **【完全二叉树（Complete Binary Tree）】**。

---

## 🏛️ 一、 物理地基：树的高度被死死限制在 $\log N$

小顶堆拥有一个核心物理铁律：**任何一个父亲节点的值，都必须小于或等于它的两个亲儿子节点。** 这就保证了全场最小值永远雷打不动地卡在最顶端的**树根（即数组的 0 号位置）**。

对于一棵装有 $N$ 个元素的完全二叉树，其层数（高度）遵循细胞分裂金字塔规律：
* 第 1 层（树根）：能装 1 个元素。
* 第 2 层：能装 2 个元素。
* 第 3 层：能装 4 个元素。
* 第 $H$ 层：能装 $2^{H-1}$ 个元素。

当全场一共有 $N$ 个元素时，这棵树的总高度 $H$ 就会被底层的数学规律死死锁在 **$\log_2 N$**（简写为 $\log N$）。
> **底层的终极秘密**：无论你想从树底爬到树顶，还是从树顶跌落到树底，**在最坏情况下，你最多需要走过的路径长度（节点个数），就是这棵树的高度 —— $\log N$**！



---

## ⏱️ 二、 `heappush` (进堆) ── 弱肉强食的“向上气泡翻滚流” (Bubble Up)

当你执行 `heapq.heappush(myheap, val)` 往堆里塞入一个新元素时，内存底层会发生以下惊心动魄的对线翻滚画面：

1. **新兵强行入座**：新来的元素没有任何特权，首先被死死拍在树的**最底层、最右边的空位上（在代码层面，就是直接追加到当前数组的末尾）**。
2. **向上老爹对线（向上调整）**：
   * 新元素开始抬头看自己的直系老爹。如果发现自己比老爹的值还要小，这就破坏了小顶堆“父亲必须小于等于儿子”的规矩！
   * 于是，新元素和老爹**互换位置**，在树里**向上爬了一层**。
3. **熔断收工**：这个对线过程会一直持续，直到它在某一层遇到了一个比它还小的老爹（合规熔断），或者它一路掀翻所有人，自己坐上了**树根（堆顶）**的最高宝座。

### 🕵️‍♂️ 时间审计：
在这个“向上翻滚”的过程中，新元素每和老爹交换一次位置，它就往上爬一层。因为树的总高度只有 $\log N$，所以它就算底盘再逆天、一路从最底层杀到最顶层，**最多也只需要互换 $\log N$ 次**！
👉 **`heappush` 时间复杂度 = $\mathcal{O}(\log N)$**

---

## 🪓 三、 `heappop` (出堆) ── 群龙无首的“向下内卷下沉流” (Bubble Down)

当你执行 `heapq.heappop(myheap)` 时，资本家要拿走全场最小值（也就是树根位置的那个老哥）。树根被切走后，整棵树瞬间变成了“无头骑士”。

为了维持完全二叉树的完美队形，堆引擎玩了一手极其高能的**“狸猫换太子”**微操：

1. **抓最底层的壮丁顶包**：引擎直接把**树的最底层、最右边的最后一个无辜老哥（也就是当前数组的最后一个元素）**生生揪出来，强行按在**树根（堆顶）**的位置上！
2. **向下儿子比对（向下调整）**：
   * 这个被抓来顶包的老哥通常值很大，它坐在堆顶瑟瑟发抖。为了符合小顶堆规矩，它必须向下内卷。
   * 它低头看自己的左、右两个亲儿子，**挑出这两个儿子里“值更小的那一个”**进行比对。如果发现自己比这个最小的儿子还要大，它就和这个小儿子**互换位置**，自己**向下掉了一层**。
3. **持续内卷**：到了下一层，它继续挑当前层较小的儿子互换，直到它跌落到最底层，或者它周围的两个儿子都比它大（合规熔断）为止。

### 🕵️‍♂️ 时间审计：
在这个顶包老哥“向下下沉”的过程中，它每和较小的儿子交换一次位置，它就往下掉一层。同样由于整棵树的高度只有 $\log N$，它即便一路遭遇社会毒打、直接从最顶层一路掉到最底盘，**最坏也只需要互换 $\log N$ 次**！
👉 **`heappop` 时间复杂度 = $\mathcal{O}(\log N)$**

---

# 1109 VS 1854 差分数组方法

## 1109

# 🪓 一维差分真命母题：LeetCode 1109. Corporate Flight Bookings (航班预订座位)

## 📝 一、 题目描述 (Description)

这里有 `n` 个航班，标签从 `1` 到 `n`。

给你一个航班预订记录列表 `bookings` ，其中 `bookings[i] = [first_i, last_i, seats_i]` 表示第 `i` 条预订记录选择了从 `first_i` 到 `last_i` 的 **所有航班**（两端都包含），并为每个航班预订了 `seats_i` 个座位。

请你返回一个长度为 `n` 的数组 `answer` ，里面的第 `i` 个元素是每个航班 `i` 上预订的座位总数。

### 示例 1:
> **输入**: bookings = [[1,2,10],[2,3,20],[2,5,25]], n = 5
> **输出**: [10,55,45,25,25]
> **解释**:
> * 预订 1:  [1, 2, 10]  ➔ 航班 1, 2 各加 10 座位。
> * 预订 2:  [2, 3, 20]  ➔ 航班 2, 3 各加 20 座位。
> * 预订 3:  [2, 5, 25]  ➔ 航班 2, 3, 4, 5 各加 25 座位。
> * 总计座位数:
>   * 航班 1: 10
>   * 航班 2: 10 + 20 + 25 = 55
>   * 航班 3: 20 + 25 = 45
>   * 航班 4: 25
>   * 航班 5: 25
> * 最终返回 [10, 55, 45, 25, 25]。

## 🏛️ 二、 为什么用【一维差分数组/闭区间推迟核销】这个算法？

* **核心痛点**：如果对每一条预订记录，都用一个 `for` 循环去把从 `first` 到 `last` 的所有航班肉身轰炸加一遍座位，当数据量极大时，时间复杂度会暴烈退化成恐怖的 $\mathcal{O}(N \times \text{bookings.length})$，当场吃满 TLE（超出时间限制）轰出门！
* **物理破局点（两头记空账）**：
  我们拉起一个和航班长度对齐的账本数组 `delta`。
  `delta[i]` 存放的**不是该航班的绝对座位数，而是“当前航班相比于前一个航班，座位变动的增量（变动量）”**！
* **🚨 闭区间死穴控制线（为什么是 `last + 1` ？）**：
  题目明确说明是闭区间，意味着 **`last` 当天航班预订依然有效，座位必须被霸占！**
  所以座位不能在 `last` 当天被扣除，必须推迟到**下飞机后的当天（即 `last + 1`）**才能执行扣减释放：
  * **`delta[first] += seats`** ➔ 能量从 `first` 航班开始疯狂涌入。
  * **`delta[last + 1] -= seats`** ➔ 能量一直维持，直到 `last + 1` 航班才利索扣除释放！
* **最后总清算**：打完所有空账后，真身指针从左到右单向横扫一遍整个 `delta` 数组，一路**滚雪球累加（求前缀和 Prefix Sum）**，就能像变魔术一样，用极速还原出每天航班上最真实的并存总座位长龙！

## ⏱️ 三、 这个算法的时间和空间复杂度是多少？

* **Time Complexity (时间复杂度)：$\mathcal{O}(N + B)$** ── $B$ 为预订记录 `bookings` 的数量，$N$ 为航班总数。第一阶段定点打卡记空账耗时 $\mathcal{O}(B)$，第二阶段单向扫街累加前缀和耗时 $\mathcal{O}(N)$。全场无任何排序，无任何树形内卷，绝对纯净的双线性流！
* **Space Complexity (空间复杂度)：$\mathcal

---

## 1854

```python
class Solution:
    def maximumPopulation(self, logs: List[List[int]]) -> int:
        # 1. 别省那点内存！直接开到 2051 长度
        # 这样 1950 到 2050 年可以直接堂堂正正当作数组下标用，没有任何越界风险！
        delta = [0] * 2051

        # 2. 头加尾减记空账
        for birth, death in logs:
            delta[birth] += 1
            delta[death] -= 1  # 🚨 死亡当年不计入人口，因此在 death 当年直接就地注销扣减！
        
        max_num = 0
        cur_num = 0
        res = 1950  # 托底初始化为题目规定的最早年份

        # 3. 雷达单向扫街：真实年份是多少，range就写多少，绝不绕弯子！
        for year in range(1950, 2051):
            cur_num += delta[year]

            # 🌟 严格大于防线：打破记录才更新，平局时自动跳过，死锁最早年份
            if cur_num > max_num:
                max_num = cur_num
                res = year  # 🎰 此时 res 存的就是老老实实的【真实年份】，直接安全返回！
                
        return res
```
---

# 🪓 算法选型与工程决断：LeetCode 1854 两种神装解法时空复杂度终极对线

在实际大厂技术面试或工业级架构设计中，面对同一个副本的多个有效解法，高工绝对不能抛硬币做决定。我们必须拿出对数据的极度敏感度，根据业务的**值域特征**进行降维打击和选型调配。

## 📊 一、 双神装终极对账大盘

我们把针对区间重叠/增量修改副本克制的 **【解法 A：小顶堆排序流】** 和 **【解法 B：静态差分数组流】** 的核心账本拉出来进行全量对线：

| 维度指标 | 解法 A：小顶堆排序流 (Heap + Sort) | 解法 B：差分数组扫街流 (Difference Array) |
| :--- | :--- | :--- |
| **打法核心** | 先按起点严格排序，再用完全二叉树动态维护最值死期（动态抽屉）。 | 彻底放弃排序，利用“头加尾减”在固定时间轴两端定点记空账。 |
| **时间复杂度** | **$\mathcal{O}(N \log N)$** | **$\mathcal{O}(N + M)$** |
| **空间复杂度** | **$\mathcal{O}(N)$** | **$\mathcal{O}(M)$** |
| **核心变量含义** | $N$ = 全场总人数（日志条数）<br>$\log N$ = 小顶堆每次维护的树形调整高度 | $N$ = 全场总人数<br>$M$ = 固定年份轴跨度常数（$2050 - 1950 + 1 = 101$） |

## ⏱️ 二、 深度精算审计：不同业务战场下的生死博弈

### 🛑 战场一：年份轴跨度极小且紧凑 (e.g., 本题原题 1950 ~ 2050)
* **业务现状**：年份跨度固定只有 $M = 101$ 个格子。假设今天数据库里灌进去了 **10 万条** 人的生命日志（$N = 100,000$）。
  * **小顶堆排序流 (A)**：
    * 时间成本：$\mathcal{O}(N \log N)$ ➔ $100000 \times \log_2(100000) \approx 100000 \times 17 = 1,700,000$ 次树形调位操作。
    * 空间成本：堆里最多可能同时积压 10 万个死期，霸占 $\mathcal{O}(100,000)$ 的内存。
  * **静态差分数组流 (B)**：
    * 时间成本：$\mathcal{O}(N + M)$ ➔ $100000 + 101 = 100,101$ 次纯粹的线性加减。算力开销还不到小顶堆的十分之一！
    * 空间成本：仅需要一个死死固定长度为 2051 的静态一维数组，内存永远是完美的 $\mathcal{O}(101)$ 常数级。
* **⚔️ 战局决断**：**在本题极其紧凑的值域下，差分数组（解法 B）以绝对的暴风线性算力将小顶堆全面捏爆！它是最具降维打击艺术的满分标准解！**

### 🛑 战场二：时间轴跨度极其恐怖 (e.g., 高精度微秒时间戳 $0 \sim 10^9$)
* **业务现状**：如果面试官反手把提示条件改了，人的出生和死亡时间变成了微秒戳，跨度高达 $10^9$（$M = 10^9$），但全场总人数非常稀疏，只有 **100 个人**（$N = 100$）。
  * **静态差分数组流 (B)**：
    * 空间崩溃：代码为了开辟 `delta = [0] * 1000000000` 的超级大账本，**内存会当场发生 OOM (Out Of Memory) 惨烈大爆炸！**
    * 时间崩溃：为了求前缀和，代码得平推横扫 $10^9$ 次，在一望无际全是 0 的大数组荒漠里白白空转好几分钟，彻底残废！
  * **小顶堆排序流 (A)**：
    * 时间成本：$\mathcal{O}(N \log N)$ ➔ $100 \times \log_2(100) \approx 100 \times 7 = 700$ 次内部二叉树调整，快如闪电！
    * 空间成本：堆里最多装 100 个死期元素，廉价纯净到极致。
* **⚔️ 战局决断**：**当时间轴值域无限庞大、且数据分布极度稀疏时，小顶堆流（解法 A）凭借完全不依赖时间跨度的“离散化免疫力”，跨越时空夺回绝对王座！**

## 🪓 三、 大厂面试御前对线：架构师级别的精满通关话术

在面试官让你对这道题做出最终解法陈述时，切忌只干巴巴扔出一个代码，掏出这套**“特种兵高工控盘流”**话术，直接实施降维打击：

1. **破局（亮出局部最优）**：
   “由于本题给定的年份范围被硬性限定在 1950 到 2050 之间，值域空间极度狭窄且固定（$M = 101$）。因此，我首选 **【一维差分数组流】**。通过 $\mathcal{O}(N)$ 头加尾减定点记空账、以及 $\mathcal{O}(M)$ 线性横扫求前缀和，可以在没有任何排序开销的代价下，以**绝对双线性 $\mathcal{O}(N + M)$ 捏爆全场**。”
2. **升华（洞察边界死穴）**：
   “但是，差分数组的致命命门在于它高度依赖紧凑、可预测的值域。如果未来系统升级，年份变成了高精度的微秒时间戳，跨度暴涨至 $10^9$ 的大值域，差分数组会因为无法承载巨量连续空间而发生 **OOM 内存溢出**，或者在空数组中产生严重的**无用无效空转**。”
3. **收网（展现全局防御）**：
   “为了赋予系统工业级的‘大值域稀疏防御力’，我会无缝切换到 **【小顶堆排序流】**。虽然它多耗费了一趟 $\mathcal{O}(N \log N)$ 的按起点排序和完全二叉树动态核销成本，但它的时空开销**完全与时间轴的物理跨度脱钩**，在大值域分布式长周期场景下具备无可比拟的稳定性。”

---

https://www.hello-algo.com/chapter_heap/top_k/
