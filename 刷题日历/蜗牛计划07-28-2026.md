🟢 LeetCode 1929. Concatenation of Array (数组串联) 专题通关卡片

## 🎯 一、 核心考点与物理直觉

- **题目大意**：给你一个长度为 $n$ 的整数数组 nums，请你构建一个长度为 $2n$ 的答案数组 ans，其中对于所有 $0 \le i < n$ 的下标，满足 ans[i] == nums[i] 且 ans[i + n] == nums[i]。
- **物理直觉**：
    - 这本质上就是把原数组复制两遍拼在一起。
    - 可以通过直接的数组拼接，或者利用循环通过索引映射 i + n = i 的方式将元素放到对应位置。

## 💻 二、 Python 高效通关代码 (Solution)

```python
from typing import List

class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        # 解法一：Python 语法糖直接相加（底层高效内存拼接）
        return nums + nums

        # 解法二：标准索引赋值流（适合其他不支持 + 的语言，如 C++/Java）
        # n = len(nums)
        # ans = [0] * (2 * n)
        # for i, num in enumerate(nums):
        #     ans[i] = num
        #     ans[i + n] = num
        # return ans
```

三、 复杂度核验 (Complexity Analysis)

- 时间复杂度：O(n)，其中 n 是数组 nums 的长度。我们需要遍历一遍原数组或者对其进行一次拼接操作。
- 空间复杂度：O(n)，用于存储输出的新数组 ans。

---

